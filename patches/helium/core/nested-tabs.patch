--- a/chrome/app/settings_strings.grdp
+++ b/chrome/app/settings_strings.grdp
@@ -313,6 +313,9 @@
   <message name="IDS_SETTINGS_TAB_STRIP_RIGHT_ALIGN" desc="Label for the toggle that displays vertical tabs on the right side of the browser window.">
     Show vertical tabs on right side
   </message>
+  <message name="IDS_SETTINGS_TAB_STRIP_NESTING" desc="Label for toggle that enables vertical tab hierarchy organization.">
+    Enable tree tab hierarchy
+  </message>
   <message name="IDS_SETTINGS_CENTERED_LOCATION_BAR" desc="Label for the toggle to use a centered address bar.">
     Center the address bar
   </message>
--- a/chrome/browser/android/tab_android.cc
+++ b/chrome/browser/android/tab_android.cc
@@ -863,7 +863,8 @@ void TabAndroid::UpdateProperties() {
                            ->GetTabGroupId();
         break;
       case tabs::TabCollection::Type::SPLIT:
-        // Intentional fallthrough. Split tabs is currently desktop only.
+      case tabs::TabCollection::Type::NESTED:
+        // Intentional fallthrough. Split & nested tabs is currently desktop only.
       case tabs::TabCollection::Type::TABSTRIP:
       case tabs::TabCollection::Type::UNPINNED:
         break;
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -208,6 +208,7 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
   (*s_allowlist)[::prefs::kHeliumLayout] = settings_api::PrefType::kNumber;
   (*s_allowlist)[::prefs::kHeliumVerticalRightAligned] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kHeliumVerticalNesting] = settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kHeliumCenteredLocationBar] =
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kHeliumMinimalLocationBar] =
--- a/chrome/browser/resources/settings/appearance_page/appearance_page.html
+++ b/chrome/browser/resources/settings/appearance_page/appearance_page.html
@@ -110,6 +110,11 @@
               label="$i18n{tabStripRightAlign}">
           </settings-toggle-button>
           <settings-toggle-button
+              hidden="[[!showVerticalSettings_]]"
+              pref="{{prefs.helium.browser.vertical_nesting}}"
+              label="$i18n{tabStripNesting}">
+          </settings-toggle-button>
+          <settings-toggle-button
               hidden="[[!showCenteredLocationBarSetting_(
                   prefs.helium.browser.layout.value)]]"
               pref="{{prefs.helium.browser.centered_location_bar}}"
--- a/chrome/browser/sessions/session_restore.cc
+++ b/chrome/browser/sessions/session_restore.cc
@@ -11,9 +11,11 @@
 #include <list>
 #include <map>
 #include <memory>
+#include <ranges>
 #include <set>
 #include <string>
 #include <utility>
+#include <vector>
 
 #include "base/command_line.h"
 #include "base/containers/flat_map.h"
@@ -922,6 +924,10 @@ class SessionRestoreImpl : public Browse
     std::map<split_tabs::SplitTabId, std::vector<tabs::TabInterface*>>
         tabs_by_split_id;
 
+    std::map<SessionID, tabs::TabInterface*> saved_to_live;
+    std::map<nested_tabs::NestedTabId, std::vector<tabs::TabInterface*>>
+        tabs_by_nested_id;
+
     const base::Time epoch_time = base::Time::UnixEpoch();
     const base::TimeTicks epoch_time_ticks = base::TimeTicks::UnixEpoch();
     for (int i = 0; i < static_cast<int>(window.tabs.size()); ++i) {
@@ -941,46 +947,83 @@ class SessionRestoreImpl : public Browse
       // windows or when launching a hosted app from the app launcher.
       int tab_index = i + initial_tab_count;
       RestoreTab(tab, browser, is_active_browser, restored_tabs, new_group_ids,
-                 &tabs_by_split_id, tab_index, is_selected_tab,
-                 last_active_time_ticks, tab.last_active_time,
+                 &tabs_by_split_id, saved_to_live, tabs_by_nested_id, tab_index,
+                 is_selected_tab, last_active_time_ticks, tab.last_active_time,
                  did_show_browser);
     }
 
-      for (const auto& pair : tabs_by_split_id) {
-        const split_tabs::SplitTabId split_id = pair.first;
-        const std::vector<tabs::TabInterface*> tab_contents_list = pair.second;
-        std::vector<int> tab_index_list;
-        for (auto* tab : tab_contents_list) {
-          tab_index_list.push_back(
-              browser->GetTabStripModel()->GetIndexOfTab(tab));
-        }
+    for (const auto& pair : tabs_by_split_id) {
+      const split_tabs::SplitTabId split_id = pair.first;
+      const std::vector<tabs::TabInterface*> tab_contents_list = pair.second;
+      std::vector<int> tab_index_list;
+      for (auto* tab : tab_contents_list) {
+        tab_index_list.push_back(
+            browser->GetTabStripModel()->GetIndexOfTab(tab));
+      }
 
-        // The number of supported split tabs is 2. Do not attempt to restore
-        // more than this to future proof it.
-        if (tab_contents_list.size() == 2) {
-          browser->tab_strip_model()->RestoreSplit(
-              split_id, tab_index_list, split_tabs::SplitTabVisualData());
-        }
+      // The number of supported split tabs is 2. Do not attempt to restore
+      // more than this to future proof it.
+      if (tab_contents_list.size() == 2) {
+        browser->tab_strip_model()->RestoreSplit(
+            split_id, tab_index_list, split_tabs::SplitTabVisualData());
       }
+    }
+
+    std::vector<const sessions::SessionNestedTab*> ordered_nesteds =
+        window.nested_tabs |
+        std::views::transform([](const auto& tab) { return tab.get(); }) |
+        std::views::filter([&](const auto* snt) {
+            auto it = saved_to_live.find(snt->owner_tab_);
+            return it != saved_to_live.end() && it->second != nullptr;
+        }) |
+        std::ranges::to<std::vector<const sessions::SessionNestedTab*>>();
+
+    std::ranges::sort(
+        ordered_nesteds, [&](const sessions::SessionNestedTab* a,
+                             const sessions::SessionNestedTab* b) {
+          auto* a_owner = saved_to_live[a->owner_tab_];
+          auto* b_owner = saved_to_live[b->owner_tab_];
+          return browser->GetTabStripModel()->GetIndexOfTab(a_owner) <
+                 browser->GetTabStripModel()->GetIndexOfTab(b_owner);
+        });
+
+    for (const auto* nested : ordered_nesteds) {
+      auto owner_it = saved_to_live.find(nested->owner_tab_);
+      if (owner_it == saved_to_live.end()) {
+        // Owner was skipped (e.g., no navigations). Drop this nest entirely;
+        // children stay flat. Don't try to recover.
+        continue;
+      }
+      tabs::TabInterface* owner = owner_it->second;
+      std::vector<tabs::TabInterface*>& children =
+          tabs_by_nested_id[nested->id_];
+
+      browser->tab_strip_model()->RestoreNested(nested->id_, owner, children,
+                                                nested->nested_visual_data_);
+    }
   }
 
   // |tab_index| is ignored for pinned tabs which will always be pushed behind
   // the last existing pinned tab. If |is_selected_tab| is true the tab will be
   // shown after loading, otherwise it's loaded hidden. |last_active_time| is
   // the value to use to set the last time the WebContents was made active.
-  void RestoreTab(const sessions::SessionTab& tab,
-                  Browser* browser,
-                  bool is_active_browser,
-                  std::vector<RestoredTab>& restored_tabs,
-                  base::flat_map<tab_groups::TabGroupId,
-                                 tab_groups::TabGroupId>* new_group_ids,
-                  std::map<split_tabs::SplitTabId,
-                           std::vector<tabs::TabInterface*>>* tabs_by_split_id,
-                  const int tab_index,
-                  bool is_selected_tab,
-                  base::TimeTicks last_active_time_ticks,
-                  base::Time last_active_time,
-                  bool& did_show_browser) {
+  void RestoreTab(
+      const sessions::SessionTab& tab,
+      Browser* browser,
+      bool is_active_browser,
+      std::vector<RestoredTab>& restored_tabs,
+      base::flat_map<tab_groups::TabGroupId, tab_groups::TabGroupId>*
+          new_group_ids,
+      std::map<split_tabs::SplitTabId, std::vector<tabs::TabInterface*>>*
+          tabs_by_split_id,
+      std::map<SessionID, tabs::TabInterface*>& saved_to_live,
+      std::map<nested_tabs::NestedTabId, std::vector<tabs::TabInterface*>>&
+          tabs_by_nested_id,
+      const int tab_index,
+      bool is_selected_tab,
+      base::TimeTicks last_active_time_ticks,
+      base::Time last_active_time,
+      bool& did_show_browser) {
     // It's possible (particularly for foreign sessions) to receive a tab
     // without valid navigations. In that case, just skip it.
     // See crbug.com/40290475.
@@ -1028,14 +1071,21 @@ class SessionRestoreImpl : public Browse
                              new_group, tab.split_id);
     restored_tabs.push_back(restored_tab);
 
+    tabs::TabInterface* tab_interface =
+        browser->GetTabStripModel()->GetTabForWebContents(
+            restored_tab.contents());
+
+    saved_to_live[tab.tab_id] = tab_interface;
+
     if (tab.split_id) {
       // add tab to tabs_by_split_id.
-      tabs::TabInterface* tab_interface =
-          browser->GetTabStripModel()->GetTabForWebContents(
-              restored_tab.contents());
       (*tabs_by_split_id)[*tab.split_id].push_back(tab_interface);
     }
 
+    if (tab.nested_id) {
+      tabs_by_nested_id[*tab.nested_id].push_back(tab_interface);
+    }
+
     // If this isn't the selected tab, there's nothing else to do.
     if (!is_selected_tab) {
       return;
--- a/chrome/browser/sessions/session_service.cc
+++ b/chrome/browser/sessions/session_service.cc
@@ -6,6 +6,7 @@
 
 #include <stddef.h>
 
+#include <optional>
 #include <set>
 #include <string>
 #include <utility>
@@ -50,12 +51,16 @@
 #include "chrome/browser/ui/ui_features.h"
 #include "chrome/browser/web_applications/web_app_helpers.h"
 #include "chrome/common/chrome_switches.h"
+#include "components/nested_tabs/nested_tab_id.h"
+#include "components/nested_tabs/nested_tab_visual_data.h"
 #include "components/saved_tab_groups/public/tab_group_sync_service.h"
 #include "components/sessions/content/content_serialized_navigation_builder.h"
 #include "components/sessions/content/session_tab_helper.h"
 #include "components/sessions/core/command_storage_manager.h"
 #include "components/sessions/core/session_command.h"
 #include "components/sessions/core/session_constants.h"
+#include "components/sessions/core/session_id.h"
+#include "components/sessions/core/session_service_commands.h"
 #include "components/sessions/core/session_types.h"
 #include "components/sessions/core/tab_restore_service.h"
 #include "content/public/browser/navigation_details.h"
@@ -122,8 +127,9 @@ SessionService::SessionService(Profile*
           SessionServiceBase::SessionServiceType::kSessionRestore),
       is_first_session_service_(
           InstanceTracker::RegisterNewSessionService(profile)) {
-  if (is_first_session_service_)
+  if (is_first_session_service_) {
     LogSessionServiceStartEvent(profile, HasPendingUncleanExit(profile));
+  }
   closing_all_browsers_subscription_ = chrome::AddClosingAllBrowsersCallback(
       base::BindRepeating(&SessionService::OnClosingAllBrowsersChanged,
                           base::Unretained(this)));
@@ -149,8 +155,9 @@ SessionService::~SessionService() {
 
   // Certain code paths explicitly destroy the SessionService as part of
   // shutdown.
-  if (!did_log_exit_)
+  if (!did_log_exit_) {
     LogExitEvent();
+  }
 }
 
 // static
@@ -191,8 +198,9 @@ bool SessionService::ShouldRestore(Brows
     return false;
   }
 
-  if (!browser)
+  if (!browser) {
     return true;
+  }
 
   // App windows should not be restored.
   auto window_type = WindowTypeForBrowserType(browser->type());
@@ -274,11 +282,50 @@ void SessionService::SetSplitTabData(
       sessions::CreateSplitTabDataUpdateCommand(split, visual_data));
 }
 
+void SessionService::SetTabNested(
+    SessionID window_id,
+    SessionID tab_id,
+    std::optional<nested_tabs::NestedTabId> nested_id) {
+  if (!ShouldTrackChangesToWindow(window_id)) {
+    return;
+  }
+
+  // Tabs get unnested as they close. However, if the whole window is closing
+  // tabs should stay in their nested. So, ignore this call in that case.
+  if (pending_window_close_ids_.contains(window_id) ||
+      window_closing_ids_.contains(window_id)) {
+    return;
+  }
+
+  ScheduleCommand(
+      sessions::CreateNestedTabCommand(tab_id, std::move(nested_id)));
+}
+
+void SessionService::SetNestedTabData(
+    SessionID window_id,
+    const nested_tabs::NestedTabId& nested_id,
+    const nested_tabs::NestedTabVisualData* visual_data,
+    SessionID owner) {
+  if (!ShouldTrackChangesToWindow(window_id)) {
+    return;
+  }
+
+  // Any nested metadata changes happening in a closing window can be ignored.
+  if (pending_window_close_ids_.contains(window_id) ||
+      window_closing_ids_.contains(window_id)) {
+    return;
+  }
+
+  ScheduleCommand(sessions::CreateNestedTabDataUpdateCommand(
+      nested_id, visual_data, owner));
+}
+
 void SessionService::SetTabGroup(SessionID window_id,
                                  SessionID tab_id,
                                  std::optional<tab_groups::TabGroupId> group) {
-  if (!ShouldTrackChangesToWindow(window_id))
+  if (!ShouldTrackChangesToWindow(window_id)) {
     return;
+  }
 
   // Tabs get ungrouped as they close. However, if the whole window is closing
   // tabs should stay in their groups. So, ignore this call in that case.
@@ -342,8 +389,9 @@ void SessionService::AddTabExtraData(Ses
                                      SessionID tab_id,
                                      const char* key,
                                      const std::string& data) {
-  if (!ShouldTrackChangesToWindow(window_id))
+  if (!ShouldTrackChangesToWindow(window_id)) {
     return;
+  }
 
   ScheduleCommand(sessions::CreateAddTabExtraDataCommand(tab_id, key, data));
 }
@@ -351,23 +399,27 @@ void SessionService::AddTabExtraData(Ses
 void SessionService::AddWindowExtraData(SessionID window_id,
                                         const char* key,
                                         const std::string& data) {
-  if (!ShouldTrackChangesToWindow(window_id))
+  if (!ShouldTrackChangesToWindow(window_id)) {
     return;
+  }
 
   ScheduleCommand(
       sessions::CreateAddWindowExtraDataCommand(window_id, key, data));
 }
 
 void SessionService::TabClosed(SessionID window_id, SessionID tab_id) {
-  if (!tab_id.id())
+  if (!tab_id.id()) {
     return;  // Happens when the tab is replaced.
+  }
 
-  if (!ShouldTrackChangesToWindow(window_id))
+  if (!ShouldTrackChangesToWindow(window_id)) {
     return;
+  }
 
   auto i = tab_to_available_range()->find(tab_id);
-  if (i != tab_to_available_range()->end())
+  if (i != tab_to_available_range()->end()) {
     tab_to_available_range()->erase(i);
+  }
 
   if (pending_window_close_ids_.contains(window_id)) {
     // Tab is in last window and the window is being closed. Don't commit it
@@ -388,8 +440,9 @@ void SessionService::TabClosed(SessionID
 }
 
 void SessionService::WindowOpened(Browser* browser) {
-  if (!ShouldTrackBrowser(browser))
+  if (!ShouldTrackBrowser(browser)) {
     return;
+  }
 
   RestoreIfNecessary(StartupTabs(), browser, /* restore_apps */ false);
   SetWindowType(browser->session_id(), browser->type());
@@ -405,8 +458,9 @@ void SessionService::WindowOpened(Browse
 }
 
 void SessionService::WindowClosing(SessionID window_id) {
-  if (!ShouldTrackChangesToWindow(window_id))
+  if (!ShouldTrackChangesToWindow(window_id)) {
     return;
+  }
 
   // If Chrome is closed immediately after a history deletion, we have to
   // rebuild commands before this window is closed, otherwise these tabs would
@@ -474,8 +528,9 @@ void SessionService::WindowClosed(Sessio
 void SessionService::SetWindowType(SessionID window_id, Browser::Type type) {
   sessions::SessionWindow::WindowType window_type =
       WindowTypeForBrowserType(type);
-  if (!ShouldRestoreWindowOfType(window_type))
+  if (!ShouldRestoreWindowOfType(window_type)) {
     return;
+  }
 
   windows_tracking()->insert(window_id);
 
@@ -491,8 +546,9 @@ void SessionService::SetWindowType(Sessi
 
 void SessionService::SetWindowUserTitle(SessionID window_id,
                                         const std::string& user_title) {
-  if (!ShouldTrackChangesToWindow(window_id))
+  if (!ShouldTrackChangesToWindow(window_id)) {
     return;
+  }
 
   ScheduleCommand(
       sessions::CreateSetWindowUserTitleCommand(window_id, user_title));
@@ -507,8 +563,9 @@ void SessionService::OnErrorWritingSessi
   // necessitate some amount of snapshotting in memory when a window is closing.
   // The histogram should give us an idea of how often this happens in practice.
   const bool unrecoverable_write_error = !pending_window_close_ids_.empty();
-  if (unrecoverable_write_error)
+  if (unrecoverable_write_error) {
     ++unrecoverable_write_error_count_;
+  }
   LogSessionServiceWriteErrorEvent(profile(), unrecoverable_write_error);
   set_rebuild_on_next_save(true);
   RebuildCommandsIfRequired();
@@ -518,8 +575,9 @@ void SessionService::SetTabUserAgentOver
     SessionID window_id,
     SessionID tab_id,
     const sessions::SerializedUserAgentOverride& user_agent_override) {
-  if (!ShouldTrackChangesToWindow(window_id))
+  if (!ShouldTrackChangesToWindow(window_id)) {
     return;
+  }
 
   ScheduleCommand(sessions::CreateSetTabUserAgentOverrideCommand(
       tab_id, user_agent_override));
@@ -530,11 +588,13 @@ Browser::Type SessionService::GetDesired
 }
 
 void SessionService::DidScheduleCommand() {
-  if (did_schedule_command_)
+  if (did_schedule_command_) {
     return;
+  }
   did_schedule_command_ = true;
-  if (is_first_session_service_)
+  if (is_first_session_service_) {
     return;
+  }
 
   // TODO(crbug.com/40196304): A command has been scheduled for a SessionService
   // other than the first. Recreating the SessionService happens if shutdown is
@@ -603,11 +663,12 @@ void SessionService::BuildCommandsForTab
     int index_in_window,
     std::optional<tab_groups::TabGroupId> group,
     std::optional<split_tabs::SplitTabId> split,
+    std::optional<nested_tabs::NestedTabId> nested,
     bool is_pinned,
     IdToRange* tab_to_available_range) {
   DCHECK(is_saving_enabled());
   SessionServiceBase::BuildCommandsForTab(window_id, tab, index_in_window,
-                                          group, split, is_pinned,
+                                          group, split, nested, is_pinned,
                                           tab_to_available_range);
 
   sessions::SessionTabHelper* session_tab_helper =
@@ -636,6 +697,11 @@ void SessionService::BuildCommandsForTab
     command_storage_manager()->AppendRebuildCommand(
         sessions::CreateSplitTabCommand(session_id, std::move(split)));
   }
+
+  if (nested) {
+    command_storage_manager()->AppendRebuildCommand(
+        sessions::CreateNestedTabCommand(session_id, std::move(nested)));
+  }
 }
 
 void SessionService::ScheduleResetCommands() {
@@ -700,8 +766,9 @@ bool SessionService::IsOnlyOneTabLeft()
 }
 
 bool SessionService::HasOpenTrackableBrowsers(SessionID window_id) const {
-  if (profile()->AsTestingProfile())
+  if (profile()->AsTestingProfile()) {
     return has_open_trackable_browser_for_test_;
+  }
 
   bool has_open_trackable = false;
   ForEachCurrentBrowserWindowInterfaceOrderedByActivation(
@@ -726,14 +793,16 @@ void SessionService::RebuildCommandsIfRe
 }
 
 void SessionService::OnClosingAllBrowsersChanged(bool closing) {
-  if (closing)
+  if (closing) {
     LogExitEvent();
+  }
 }
 
 void SessionService::LogExitEvent() {
   // If there are pending closes, then we have already logged the exit.
-  if (!pending_window_close_ids_.empty())
+  if (!pending_window_close_ids_.empty()) {
     return;
+  }
 
   RemoveExitEvent();
   int browser_count = 0;
@@ -752,8 +821,9 @@ void SessionService::LogExitEvent() {
 }
 
 void SessionService::RemoveExitEvent() {
-  if (!did_log_exit_)
+  if (!did_log_exit_) {
     return;
+  }
 
   RemoveLastSessionServiceEventOfType(profile(),
                                       SessionServiceEventLogType::kExit);
--- a/chrome/browser/sessions/session_service.h
+++ b/chrome/browser/sessions/session_service.h
@@ -14,7 +14,10 @@
 #include "base/memory/weak_ptr.h"
 #include "chrome/browser/sessions/session_service_base.h"
 #include "chrome/browser/ui/browser.h"
+#include "components/nested_tabs/nested_tab_id.h"
+#include "components/nested_tabs/nested_tab_visual_data.h"
 #include "components/sessions/core/command_storage_manager_delegate.h"
+#include "components/sessions/core/session_id.h"
 #include "components/tab_groups/tab_group_id.h"
 #include "components/tab_groups/tab_group_visual_data.h"
 
@@ -56,6 +59,7 @@ using StartupTabs = std::vector<StartupT
 // make a pass in SessionService to remove app related code.
 class SessionService : public SessionServiceBase {
   friend class SessionServiceTestHelper;
+
  public:
   // Creates a SessionService for the specified profile.
   explicit SessionService(Profile* profile);
@@ -104,6 +108,15 @@ class SessionService : public SessionSer
                        const split_tabs::SplitTabId split,
                        const split_tabs::SplitTabVisualData* visual_data);
 
+  void SetTabNested(SessionID window_id,
+                    SessionID tab_id,
+                    std::optional<nested_tabs::NestedTabId> nested_id);
+
+  void SetNestedTabData(SessionID window_id,
+                        const nested_tabs::NestedTabId& nested_id,
+                        const nested_tabs::NestedTabVisualData* visual_data,
+                        SessionID owner);
+
   // Sets a tab's group ID, if any. Note that a group can't be split between
   // multiple windows.
   void SetTabGroup(SessionID window_id,
@@ -208,6 +221,7 @@ class SessionService : public SessionSer
                            int index_in_window,
                            std::optional<tab_groups::TabGroupId> group,
                            std::optional<split_tabs::SplitTabId> split,
+                           std::optional<nested_tabs::NestedTabId> nested,
                            bool is_pinned,
                            IdToRange* tab_to_available_range) override;
 
--- a/chrome/browser/sessions/session_service_base.cc
+++ b/chrome/browser/sessions/session_service_base.cc
@@ -6,6 +6,7 @@
 
 #include <stddef.h>
 
+#include <optional>
 #include <set>
 #include <utility>
 #include <vector>
@@ -34,6 +35,7 @@
 #include "chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_utils.h"
 #include "chrome/browser/ui/tabs/tab_group_model.h"
 #include "chrome/browser/ui/ui_features.h"
+#include "components/nested_tabs/nested_tab_id.h"
 #include "components/saved_tab_groups/public/tab_group_sync_service.h"
 #include "components/sessions/content/content_serialized_navigation_builder.h"
 #include "components/sessions/content/session_tab_helper.h"
@@ -41,9 +43,11 @@
 #include "components/sessions/core/session_command.h"
 #include "components/sessions/core/session_constants.h"
 #include "components/sessions/core/session_id.h"
+#include "components/sessions/core/session_service_commands.h"
 #include "components/sessions/core/session_types.h"
 #include "components/split_tabs/split_tab_id.h"
 #include "components/split_tabs/split_tab_visual_data.h"
+#include "components/tabs/public/nested_tab_data.h"
 #include "components/tabs/public/split_tab_data.h"
 #include "components/tabs/public/tab_group.h"
 #include "content/public/browser/navigation_details.h"
@@ -318,7 +322,7 @@ void SessionServiceBase::TabRestored(Web
   }
 
   BuildCommandsForTab(session_tab_helper->window_id(), tab, -1, std::nullopt,
-                      std::nullopt, pinned, nullptr);
+                      std::nullopt, std::nullopt, pinned, nullptr);
   command_storage_manager()->StartSaveTimer();
 }
 
@@ -595,6 +599,7 @@ void SessionServiceBase::BuildCommandsFo
     int index_in_window,
     std::optional<tab_groups::TabGroupId> group,
     std::optional<split_tabs::SplitTabId> split,
+    std::optional<nested_tabs::NestedTabId> nested,
     bool is_pinned,
     IdToRange* tab_to_available_range) {
   DCHECK(tab);
@@ -739,26 +744,39 @@ void SessionServiceBase::BuildCommandsFo
     }
   }
 
-    for (split_tabs::SplitTabId split_id : tab_strip->ListSplits()) {
-      command_storage_manager()->AppendRebuildCommand(
-          sessions::CreateSplitTabDataUpdateCommand(
-              split_id, tab_strip->GetSplitData(split_id)->visual_data()));
-    }
-
-    int index = 0;
-    for (const tabs::TabInterface* tab_interface : *tab_strip) {
-      WebContents* tab = tab_interface->GetContents();
-      DCHECK(tab);
-      const std::optional<tab_groups::TabGroupId> group_id =
-          tab_strip->GetTabGroupForTab(index);
-      const std::optional<split_tabs::SplitTabId> split_id =
-          tab_strip->GetSplitForTab(index);
+  for (split_tabs::SplitTabId split_id : tab_strip->ListSplits()) {
+    command_storage_manager()->AppendRebuildCommand(
+        sessions::CreateSplitTabDataUpdateCommand(
+            split_id, tab_strip->GetSplitData(split_id)->visual_data()));
+  }
 
-      BuildCommandsForTab(browser->session_id(), tab, index, group_id, split_id,
-                          tab_interface->IsPinned(), tab_to_available_range);
+  for (nested_tabs::NestedTabId nested_id : tab_strip->ListNesteds()) {
+    auto* data = tab_strip->GetNestingData(nested_id);
+    CHECK(data);
+    auto* owner = data->owner_tab().Get();
+    CHECK(owner);
+    command_storage_manager()->AppendRebuildCommand(
+        sessions::CreateNestedTabDataUpdateCommand(
+            nested_id, data->visual_data(),
+            sessions::SessionTabHelper::IdForTab(owner->GetContents())));
+  }
+
+  int index = 0;
+  for (const tabs::TabInterface* tab_interface : *tab_strip) {
+    WebContents* tab = tab_interface->GetContents();
+    DCHECK(tab);
+    const std::optional<tab_groups::TabGroupId> group_id =
+        tab_strip->GetTabGroupForTab(index);
+    const std::optional<split_tabs::SplitTabId> split_id =
+        tab_strip->GetSplitForTab(index);
+    const auto nested_id = tab_strip->GetEnclosingNestedId(index);
+
+    BuildCommandsForTab(browser->session_id(), tab, index, group_id, split_id,
+                        nested_id, tab_interface->IsPinned(),
+                        tab_to_available_range);
 
-      index++;
-    }
+    index++;
+  }
 
   windows_to_track->insert(browser->session_id());
 }
--- a/chrome/browser/sessions/session_service_base.h
+++ b/chrome/browser/sessions/session_service_base.h
@@ -21,6 +21,7 @@
 #include "chrome/browser/ui/browser_finder.h"
 #include "chrome/browser/ui/browser_window/public/browser_collection_observer.h"
 #include "components/keyed_service/core/keyed_service.h"
+#include "components/nested_tabs/nested_tab_id.h"
 #include "components/sessions/content/session_tab_helper_delegate.h"
 #include "components/sessions/core/command_storage_manager_delegate.h"
 #include "components/sessions/core/session_service_commands.h"
@@ -249,6 +250,7 @@ class SessionServiceBase : public sessio
                                    int index_in_window,
                                    std::optional<tab_groups::TabGroupId> group,
                                    std::optional<split_tabs::SplitTabId> split,
+                                   std::optional<nested_tabs::NestedTabId> nested,
                                    bool is_pinned,
                                    IdToRange* tab_to_available_range);
 
--- a/chrome/browser/ui/android/tab_model/tab_collection_tab_model_impl.cc
+++ b/chrome/browser/ui/android/tab_model/tab_collection_tab_model_impl.cc
@@ -292,6 +292,7 @@ int TabCollectionTabModelImpl::MoveTabGr
   tab_strip_collection_->MoveTabsRecursive(
       tab_indices, static_cast<size_t>(to_index),
       /*new_group_id=*/std::nullopt,
+      std::nullopt,
       /*new_pinned_state=*/false, kRetainCollectionTypes);
 
   return base::checked_cast<int>(to_index);
--- a/chrome/browser/ui/browser.cc
+++ b/chrome/browser/ui/browser.cc
@@ -140,6 +140,7 @@
 #include "chrome/browser/ui/tabs/tab_group_model.h"
 #include "chrome/browser/ui/tabs/tab_menu_model.h"
 #include "chrome/browser/ui/tabs/tab_strip_model.h"
+#include "chrome/browser/ui/tabs/tab_strip_model_observer.h"
 #include "chrome/browser/ui/tabs/tab_utils.h"
 #include "chrome/browser/ui/ui_features.h"
 #include "chrome/browser/ui/unload_controller.h"
@@ -185,6 +186,7 @@
 #include "components/keep_alive_registry/keep_alive_registry.h"
 #include "components/keep_alive_registry/keep_alive_types.h"
 #include "components/keep_alive_registry/scoped_keep_alive.h"
+#include "components/nested_tabs/nested_tab_visual_data.h"
 #include "components/optimization_guide/content/browser/page_content_proto_provider.h"
 #include "components/page_load_metrics/browser/metrics_web_contents_observer.h"
 #include "components/page_load_metrics/common/page_load_metrics.mojom.h"
@@ -199,6 +201,7 @@
 #include "components/split_tabs/split_tab_id.h"
 #include "components/split_tabs/split_tab_visual_data.h"
 #include "components/startup_metric_utils/browser/startup_metric_utils.h"
+#include "components/tabs/public/nested_tab_data.h"
 #include "components/tabs/public/split_tab_data.h"
 #include "components/tabs/public/tab_collection.h"
 #include "components/tabs/public/tab_group.h"
@@ -603,7 +606,8 @@ Browser::Browser(const CreateParams& par
       should_trigger_session_restore_(params.should_trigger_session_restore),
       cancel_download_confirmation_state_(
           CancelDownloadConfirmationState::kNotPrompted),
-      close_multitab_confirmation_state_(CancelDownloadConfirmationState::kNotPrompted),
+      close_multitab_confirmation_state_(
+          CancelDownloadConfirmationState::kNotPrompted),
       override_bounds_(params.initial_bounds),
       initial_show_state_(params.initial_show_state),
       initial_workspace_(params.initial_workspace),
@@ -1017,7 +1021,8 @@ bool Browser::TryToCloseWindow(
 void Browser::ResetTryToCloseWindow() {
   cancel_download_confirmation_state_ =
       CancelDownloadConfirmationState::kNotPrompted;
-  close_multitab_confirmation_state_ = CancelDownloadConfirmationState::kNotPrompted;
+  close_multitab_confirmation_state_ =
+      CancelDownloadConfirmationState::kNotPrompted;
   unload_controller_.ResetTryToCloseWindow();
 }
 
@@ -1720,6 +1725,82 @@ void Browser::UpdateSplitTabSessionVisua
   session_service->SetSplitTabData(session_id(), split_id, visual_data);
 }
 
+void Browser::OnNestedTabChanged(const NestedTabChange& change) {
+  switch (change.type) {
+    case NestedTabChange::Type::kCreated: {
+      for (const auto& child : change.GetCreatedChange()->initial_children) {
+        UpdateNestedTabSessionData(child.Get(), change.nested_id);
+      }
+
+      UpdateNestedTabSessionVisualData(change.nested_id);
+      break;
+    }
+
+    case NestedTabChange::Type::kVisualsChanged: {
+      UpdateNestedTabSessionVisualData(change.nested_id);
+      break;
+    }
+
+    case NestedTabChange::Type::kMoved: {
+      // No need to do anything here since nested is still present and no
+      // visual/ownership information changed.
+      break;
+    }
+
+    case NestedTabChange::Type::kContentsChanged: {
+      for (const auto& child : change.GetContentsChange()->children_added) {
+        UpdateNestedTabSessionData(child.Get(), change.nested_id);
+      }
+      for (const auto& removed : change.GetContentsChange()->children_removed) {
+        UpdateNestedTabSessionData(removed.Get(), std::nullopt);
+      }
+      break;
+    }
+
+    case NestedTabChange::Type::kDestroyed: {
+      for (const auto& removed :
+           change.GetDestroyedChange()->orphaned_children) {
+        UpdateNestedTabSessionData(removed.Get(), std::nullopt);
+      }
+      break;
+    }
+  }
+}
+
+void Browser::UpdateNestedTabSessionData(
+    tabs::TabInterface* tab,
+    std::optional<nested_tabs::NestedTabId> nested_id) {
+  DCHECK(!IsRelevantToAppSessionService(type_));
+  SessionService* const session_service =
+      SessionServiceFactory::GetForProfile(profile_);
+  if (!session_service) {
+    return;
+  }
+
+  session_service->SetTabNested(
+      session_id(), sessions::SessionTabHelper::IdForTab(tab->GetContents()),
+      std::move(nested_id));
+}
+
+void Browser::UpdateNestedTabSessionVisualData(
+    const nested_tabs::NestedTabId& nested_id) {
+  SessionService* const session_service =
+      SessionServiceFactory::GetForProfile(profile());
+  if (!session_service) {
+    return;
+  }
+
+  const auto* data = tab_strip_model()->GetNestingData(nested_id);
+  CHECK(data);
+  auto* owner = data->owner_tab().Get();
+  CHECK(owner);
+  const nested_tabs::NestedTabVisualData* visual_data = data->visual_data();
+  session_service->SetNestedTabData(
+      session_id(), nested_id, visual_data,
+      sessions::SessionTabHelper::IdForTab(
+          owner->GetContents()));
+}
+
 void Browser::SetTopControlsShownRatio(content::WebContents* web_contents,
                                        float ratio) {
   window_->SetTopControlsShownRatio(web_contents, ratio);
@@ -2014,6 +2095,29 @@ WebContents* Browser::OpenURLFromTab(
 
   content::WebContents* navigated_or_inserted_contents =
       popup_delegate->nav_params()->navigated_or_inserted_contents;
+
+  // Auto-nest the new tab under |source| for link-driven new tab opens.
+  // At this point Navigate() has completed and the new tab is in the strip;
+  // we're outside any in-flight TabStripModel transaction.
+  if (navigated_or_inserted_contents && source &&
+      navigated_or_inserted_contents != source &&  // skip in-place navigations
+      (params.disposition == WindowOpenDisposition::NEW_BACKGROUND_TAB ||
+       params.disposition == WindowOpenDisposition::NEW_FOREGROUND_TAB) &&
+      profile()->GetPrefs()->GetBoolean(prefs::kHeliumVerticalNesting)) {
+    tabs::TabInterface* source_tab =
+        tab_strip_model()->GetTabForWebContents(source);
+    tabs::TabInterface* new_tab =
+        tab_strip_model()->GetTabForWebContents(navigated_or_inserted_contents);
+    if (source_tab && new_tab && !source_tab->IsPinned() && !source_tab->IsSplit()) {
+      int source_idx = tab_strip_model()->GetIndexOfTab(source_tab);
+      int new_idx = tab_strip_model()->GetIndexOfTab(new_tab);
+      if (source_idx != TabStripModel::kNoTab &&
+          new_idx != TabStripModel::kNoTab) {
+        tab_strip_model()->Nest(source_idx, {new_idx});
+      }
+    }
+  }
+
   if (is_popup && navigated_or_inserted_contents) {
     auto* tracker = blocked_content::PopupTracker::CreateForWebContents(
         navigated_or_inserted_contents, source, params.disposition);
@@ -2146,9 +2250,32 @@ content::WebContents* Browser::AddNewCon
     }
   }
 
-  return chrome::AddWebContents(this, source, std::move(new_contents),
-                                target_url, disposition, window_features,
-                                window_action, user_gesture);
+  auto result = chrome::AddWebContents(this, source, std::move(new_contents),
+                                       target_url, disposition, window_features,
+                                       window_action, user_gesture);
+
+  // Auto-nest navigation-from-tab into the opener's NESTED.
+  if (result && source &&
+      (disposition == WindowOpenDisposition::NEW_FOREGROUND_TAB ||
+       disposition == WindowOpenDisposition::NEW_BACKGROUND_TAB) &&
+      profile()->GetPrefs()->GetBoolean(prefs::kHeliumVerticalNesting)) {
+    TabStripModel* tsm = tab_strip_model();
+    tabs::TabInterface* source_tab =
+        tabs::TabInterface::MaybeGetFromContents(source);
+    tabs::TabInterface* new_tab =
+        tabs::TabInterface::MaybeGetFromContents(result);
+    if (source_tab && new_tab && !source_tab->IsPinned() &&
+        !source_tab->IsSplit()) {
+      const int source_idx = tsm->GetIndexOfTab(source_tab);
+      const int new_idx = tsm->GetIndexOfTab(new_tab);
+      if (source_idx != TabStripModel::kNoTab &&
+          new_idx != TabStripModel::kNoTab) {
+        tsm->Nest(source_idx, {new_idx});
+      }
+    }
+  }
+
+  return result;
 }
 
 void Browser::ActivateContents(WebContents* contents) {
@@ -3537,48 +3664,62 @@ bool Browser::CanCloseWithInProgressDown
 }
 
 bool Browser::CanCloseWithMultipleTabs() {
-  if (!base::CommandLine::ForCurrentProcess()->HasSwitch("close-confirmation"))
+  if (!base::CommandLine::ForCurrentProcess()->HasSwitch(
+          "close-confirmation")) {
     return true;
+  }
 
   // If we've prompted, we need to hear from the user before we
   // can close.
-  if (close_multitab_confirmation_state_ != CancelDownloadConfirmationState::kNotPrompted)
-    return close_multitab_confirmation_state_ != CancelDownloadConfirmationState::kWaitingForResponse;
+  if (close_multitab_confirmation_state_ !=
+      CancelDownloadConfirmationState::kNotPrompted) {
+    return close_multitab_confirmation_state_ !=
+           CancelDownloadConfirmationState::kWaitingForResponse;
+  }
 
   // If we're not running a full browser process with a profile manager
   // (testing), it's ok to close the browser.
-  if (!g_browser_process->profile_manager())
+  if (!g_browser_process->profile_manager()) {
     return true;
+  }
 
   // Figure out how many windows are open total
   int total_window_count = 0;
-  GlobalBrowserCollection::GetInstance()->ForEach([&total_window_count, this](BrowserWindowInterface* browser) {
-    // Don't count this browser window or any other in the process of closing.
-    // Window closing may be delayed, and windows that are in the process of
-    // closing don't count against our totals.
-    if (browser != this && !browser->capabilities()->IsAttemptingToCloseBrowser()) {
-      total_window_count++;
-    }
-    return true;
-  });
+  GlobalBrowserCollection::GetInstance()->ForEach(
+      [&total_window_count, this](BrowserWindowInterface* browser) {
+        // Don't count this browser window or any other in the process of
+        // closing. Window closing may be delayed, and windows that are in the
+        // process of closing don't count against our totals.
+        if (browser != this &&
+            !browser->capabilities()->IsAttemptingToCloseBrowser()) {
+          total_window_count++;
+        }
+        return true;
+      });
 
-  const auto flag_value = base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII("close-confirmation");
+  const auto flag_value =
+      base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
+          "close-confirmation");
   bool show_confirmation_last_window = flag_value == "last";
 
   if (show_confirmation_last_window) {
-    if (total_window_count >= 1 || this->tab_strip_model()->count() <= 1)
+    if (total_window_count >= 1 || this->tab_strip_model()->count() <= 1) {
       return true;
+    }
   } else {
-    if (total_window_count == 0)
+    if (total_window_count == 0) {
       return true;
-    if (this->tab_strip_model()->count() == 0)
+    }
+    if (this->tab_strip_model()->count() == 0) {
       tab_strip_model_delegate_->AddTabAt(GURL(), -1, true);
+    }
   }
 
-  close_multitab_confirmation_state_ = CancelDownloadConfirmationState::kWaitingForResponse;
+  close_multitab_confirmation_state_ =
+      CancelDownloadConfirmationState::kWaitingForResponse;
 
-  auto callback = base::BindOnce(&Browser::MultitabResponse,
-                                 weak_factory_.GetWeakPtr());
+  auto callback =
+      base::BindOnce(&Browser::MultitabResponse, weak_factory_.GetWeakPtr());
   MessageBoxDialog::Show(window_->GetNativeWindow(),
                          u"Do you want to close this window?", std::u16string(),
                          chrome::MESSAGE_BOX_TYPE_QUESTION, u"Close", u"Cancel",
@@ -3614,7 +3755,8 @@ void Browser::InProgressDownloadResponse
 
 void Browser::MultitabResponse(chrome::MessageBoxResult result) {
   if (result == chrome::MESSAGE_BOX_RESULT_YES) {
-    close_multitab_confirmation_state_ = CancelDownloadConfirmationState::kResponseReceived;
+    close_multitab_confirmation_state_ =
+        CancelDownloadConfirmationState::kResponseReceived;
     std::move(warn_before_closing_callback_)
         .Run(WarnBeforeClosingResult::kOkToClose);
     return;
@@ -3622,7 +3764,8 @@ void Browser::MultitabResponse(chrome::M
 
   // Sets the confirmation state to kNotPrompted so that if the user tries to
   // close again we'll show the warning again.
-  close_multitab_confirmation_state_ = CancelDownloadConfirmationState::kNotPrompted;
+  close_multitab_confirmation_state_ =
+      CancelDownloadConfirmationState::kNotPrompted;
 
   std::move(warn_before_closing_callback_)
       .Run(WarnBeforeClosingResult::kDoNotClose);
--- a/chrome/browser/ui/browser.h
+++ b/chrome/browser/ui/browser.h
@@ -9,6 +9,7 @@
 
 #include <map>
 #include <memory>
+#include <optional>
 #include <string>
 #include <vector>
 
@@ -37,6 +38,7 @@
 #include "chrome/browser/ui/tabs/tab_strip_model.h"
 #include "chrome/browser/ui/tabs/tab_strip_model_observer.h"
 #include "chrome/browser/ui/unload_controller.h"
+#include "components/nested_tabs/nested_tab_id.h"
 #include "components/paint_preview/buildflags/buildflags.h"
 #include "components/prefs/pref_change_registrar.h"
 #include "components/sessions/core/session_id.h"
@@ -679,6 +681,7 @@ class Browser : public TabStripModelObse
                               int index) override;
   void TabStripEmpty() override;
   void OnSplitTabChanged(const SplitTabChange& change) override;
+  void OnNestedTabChanged(const NestedTabChange& change) override;
 
   // Overridden from content::WebContentsDelegate:
   void ActivateContents(content::WebContents* contents) override;
@@ -1219,6 +1222,13 @@ class Browser : public TabStripModelObse
 
   void UpdateSplitTabSessionVisualData(const split_tabs::SplitTabId& split_id);
 
+  void UpdateNestedTabSessionData(
+      tabs::TabInterface* tab,
+      std::optional<nested_tabs::NestedTabId> nested_id);
+
+  void UpdateNestedTabSessionVisualData(
+      const nested_tabs::NestedTabId& nested_id);
+
   // Create `FindBarController` if it does not exist.
   // TODO(crbug.com/423956131): Convert to `GetFindBarController` which returns
   // existing `FindBarController`.
--- a/chrome/browser/ui/browser_live_tab_context.cc
+++ b/chrome/browser/ui/browser_live_tab_context.cc
@@ -4,10 +4,13 @@
 
 #include "chrome/browser/ui/browser_live_tab_context.h"
 
+#include <algorithm>
 #include <memory>
 #include <numeric>
 #include <optional>
+#include <tuple>
 #include <utility>
+#include <vector>
 
 #include "base/check_deref.h"
 #include "base/token.h"
@@ -28,22 +31,28 @@
 #include "chrome/browser/ui/browser_window.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_features.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_interface.h"
+#include "chrome/browser/ui/browser_window/public/browser_window_interface_iterator.h"
 #include "chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_utils.h"
 #include "chrome/browser/ui/tabs/tab_group_model.h"
 #include "chrome/browser/ui/tabs/tab_strip_model.h"
 #include "chrome/browser/web_applications/web_app_helpers.h"
+#include "components/nested_tabs/nested_tab_id.h"
 #include "components/saved_tab_groups/public/features.h"
 #include "components/saved_tab_groups/public/saved_tab_group.h"
 #include "components/saved_tab_groups/public/tab_group_sync_service.h"
 #include "components/saved_tab_groups/public/types.h"
 #include "components/sessions/content/content_live_tab.h"
 #include "components/sessions/content/content_platform_specific_tab_data.h"
+#include "components/sessions/content/session_tab_helper.h"
 #include "components/sessions/core/live_tab_context.h"
 #include "components/sessions/core/session_types.h"
 #include "components/sessions/core/tab_restore_service.h"
+#include "components/sessions/core/tab_restore_types.h"
 #include "components/tab_groups/tab_group_id.h"
 #include "components/tab_groups/tab_group_visual_data.h"
+#include "components/tabs/public/nested_tab_data.h"
 #include "components/tabs/public/tab_group.h"
+#include "components/tabs/public/tab_interface.h"
 #include "content/public/browser/navigation_controller.h"
 #include "content/public/browser/session_storage_namespace.h"
 #include "content/public/browser/web_contents.h"
@@ -82,6 +91,19 @@ sessions::LiveTabContext* GetLiveTabCont
              : nullptr;
 }
 
+tabs::TabInterface* FindTabWithSessionID(SessionID desired_id) {
+  for (BrowserWindowInterface* browser : GetAllBrowserWindowInterfaces()) {
+    TabStripModel* tsm = browser->GetTabStripModel();
+    for (int i = 0; i < tsm->count(); ++i) {
+      if (sessions::SessionTabHelper::IdForTab(tsm->GetWebContentsAt(i)) ==
+          desired_id) {
+        return tsm->GetTabAtIndex(i);
+      }
+    }
+  }
+  return nullptr;
+}
+
 }  // namespace
 
 BrowserLiveTabContext::BrowserLiveTabContext(BrowserWindowInterface* browser,
@@ -226,6 +248,118 @@ void BrowserLiveTabContext::SetVisualDat
   tab_strip_model_->ChangeTabGroupVisuals(group, std::move(visual_data));
 }
 
+std::optional<sessions::tab_restore::NestedTabChildRestorationInfo>
+BrowserLiveTabContext::GetParentSessionIdAndChildIndexForTab(int index) const {
+  std::optional<int> parent_idx = tab_strip_model_->GetParentIndex(index);
+  if (!parent_idx) {
+    return std::nullopt;
+  }
+  tabs::TabInterface* parent_tab = tab_strip_model_->GetTabAtIndex(*parent_idx);
+  CHECK(parent_tab && parent_tab->GetContents());
+
+  // Position among parent's direct children (NOT recursive).
+  std::vector<int> child_indices =
+      tab_strip_model_->GetChildIndices(*parent_idx);
+  auto it = std::ranges::find(child_indices, index);
+  CHECK(it != child_indices.end());
+  size_t pos = it - child_indices.begin();
+
+  return sessions::tab_restore::NestedTabChildRestorationInfo{
+      sessions::SessionTabHelper::IdForTab(parent_tab->GetContents()), pos};
+}
+
+SessionID BrowserLiveTabContext::GetSessionIdAtIndex(int index) const {
+  const auto tab = tab_strip_model_->GetTabAtIndex(index);
+  CHECK(tab);
+  return sessions::SessionTabHelper::IdForTab(tab->GetContents());
+}
+
+std::optional<int> BrowserLiveTabContext::GetParentIndex(int index) const {
+  return tab_strip_model_->GetParentIndex(index);
+}
+
+std::vector<int> BrowserLiveTabContext::GetDirectChildIndices(int index) const {
+  return tab_strip_model_->GetChildIndices(index);
+}
+
+std::vector<SessionID> BrowserLiveTabContext::GetSessionIdsInGroup(
+    const tab_groups::TabGroupId& group) const {
+  std::vector<SessionID> ids;
+  TabGroupModel* group_model = tab_strip_model_->group_model();
+  if (!group_model || !group_model->ContainsTabGroup(group)) {
+    // Group not present in this context (e.g. its local id changed after a
+    // saved-group restore opened it under a new id). Caller is responsible
+    // for resolving the current local id; we just bail safely here.
+    return ids;
+  }
+  TabGroup* tab_group = group_model->GetTabGroup(group);
+  gfx::Range range = tab_group->ListTabs();
+  ids.reserve(range.length());
+  for (uint32_t i = range.start(); i < range.end(); ++i) {
+    ids.push_back(GetSessionIdAtIndex(static_cast<int>(i)));
+  }
+  return ids;
+}
+
+namespace {
+
+// Recursively applies the nesting encoded in |entry| onto live tabs starting
+// at live_ids[live_idx]. Advances |live_idx| past this entry's subtree
+// (1 + recursive descendant count). Each non-empty children vector triggers
+// Nest calls (idempotent, if already nested, the move is a no-op).
+void NestEntrySubtree(const sessions::tab_restore::Tab& entry,
+                      const std::vector<SessionID>& live_ids,
+                      size_t& live_idx,
+                      TabStripModel& tab_strip_model) {
+  if (live_idx >= live_ids.size()) {
+    return;
+  }
+  SessionID this_id = live_ids[live_idx++];
+  tabs::TabInterface* this_tab = FindTabWithSessionID(this_id);
+
+  for (const auto& child_entry : entry.children) {
+    if (live_idx >= live_ids.size()) {
+      return;
+    }
+    SessionID child_id = live_ids[live_idx];
+    // Recurse first so grandchildren are nested under this child before
+    // the child itself is nested under |this_tab|.
+    NestEntrySubtree(*child_entry, live_ids, live_idx, tab_strip_model);
+
+    if (!this_tab) {
+      continue;
+    }
+    tabs::TabInterface* child_tab = FindTabWithSessionID(child_id);
+    if (!child_tab) {
+      continue;
+    }
+    if (this_tab->IsPinned() || this_tab->IsSplit()) {
+      continue;  // can't NEST under a pinned/split tab
+    }
+    const int parent_idx = tab_strip_model.GetIndexOfTab(this_tab);
+    const int child_strip_idx = tab_strip_model.GetIndexOfTab(child_tab);
+    if (parent_idx < 0 || child_strip_idx < 0) {
+      continue;
+    }
+    tab_strip_model.Nest(parent_idx, {child_strip_idx}, std::nullopt,
+                         std::nullopt);
+  }
+}
+
+}  // namespace
+
+void BrowserLiveTabContext::NestTabsByStructure(
+    const std::vector<SessionID>& live_tab_ids,
+    const std::vector<std::unique_ptr<sessions::tab_restore::Tab>>& entries) {
+  size_t live_idx = 0;
+  for (const auto& entry : entries) {
+    if (live_idx >= live_tab_ids.size()) {
+      return;
+    }
+    NestEntrySubtree(*entry, live_tab_ids, live_idx, *tab_strip_model_);
+  }
+}
+
 const gfx::Rect BrowserLiveTabContext::GetRestoredBounds() const {
   return base_window_->GetRestoredBounds();
 }
@@ -243,7 +377,8 @@ sessions::LiveTab* BrowserLiveTabContext
     int tab_index,
     bool select,
     bool is_restoring_group_or_window,
-    sessions::tab_restore::Type original_session_type) {
+    sessions::tab_restore::Type original_session_type,
+    std::optional<SessionID> parent_override) {
   tab_groups::TabGroupSyncService* tab_group_service =
       tab_groups::TabGroupSyncServiceFactory::GetForProfile(&profile_.get());
   CHECK(tab_group_service);
@@ -323,9 +458,14 @@ sessions::LiveTab* BrowserLiveTabContext
       return nullptr;
     }
 
-    // Add the saved tab to the end of group.
+    // Subtree roots (Tab entries with children) restore at their original
+    // tabstrip index so children can nest under them at the correct
+    // location; single saved-group tabs continue to append at end of group
+    // per Chromium's default behavior.
+    const int saved_group_insert_index =
+        !tab.children.empty() ? tab_index : tab_strip_model_->count();
     web_contents = chrome::AddRestoredTab(
-        browser, tab.navigations, tab_strip_model_->count(),
+        browser, tab.navigations, saved_group_insert_index,
         tab.normalized_navigation_index(), tab.extension_app_id, group_id,
         select, tab.pinned, base::TimeTicks(), base::Time(), storage_namespace,
         tab.user_agent_override, tab.extra_data,
@@ -340,6 +480,30 @@ sessions::LiveTab* BrowserLiveTabContext
     // Load the tab manually if there's no BackgroundTabLoadingPolicy.
     web_contents->GetController().LoadIfNecessary();
   }
+
+  // Resolve nesting target. parent_override takes precedence over the stored
+  // parent_session_id, since stored ids point to pre-close parents that no
+  // longer exist when restoring a subtree.
+  tabs::TabInterface* parent_tab = nullptr;
+  std::optional<size_t> child_idx;
+  if (parent_override.has_value()) {
+    parent_tab = FindTabWithSessionID(*parent_override);
+    child_idx = tab.parent_session_id
+                    ? std::optional<size_t>(tab.parent_session_id->child_index)
+                    : std::nullopt;
+  } else if (tab.parent_session_id.has_value()) {
+    parent_tab = FindTabWithSessionID(tab.parent_session_id->parent_session_id);
+    child_idx = tab.parent_session_id->child_index;
+  }
+
+  if (parent_tab && !parent_tab->IsSplit() && !parent_tab->IsPinned()) {
+    int parent_idx = tab_strip_model_->GetIndexOfTab(parent_tab);
+    int restored_idx = tab_strip_model_->GetIndexOfWebContents(web_contents);
+    tab_strip_model_->Nest(parent_idx, {restored_idx}, std::nullopt, child_idx);
+  }
+  // else: unnest fallback (parent gone / pinned / split). Restored tab stays
+  // where the normal insert path put it.
+
   return sessions::ContentLiveTab::GetOrCreateForWebContents(web_contents);
 }
 
--- a/chrome/browser/ui/browser_live_tab_context.h
+++ b/chrome/browser/ui/browser_live_tab_context.h
@@ -6,6 +6,7 @@
 #define CHROME_BROWSER_UI_BROWSER_LIVE_TAB_CONTEXT_H_
 
 #include <map>
+#include <optional>
 #include <string>
 #include <vector>
 
@@ -81,6 +82,17 @@ class BrowserLiveTabContext : public ses
   void SetVisualDataForGroup(
       const tab_groups::TabGroupId& group,
       const tab_groups::TabGroupVisualData& visual_data) override;
+  std::optional<sessions::tab_restore::NestedTabChildRestorationInfo>
+  GetParentSessionIdAndChildIndexForTab(int index) const override;
+  SessionID GetSessionIdAtIndex(int index) const override;
+  std::vector<int> GetDirectChildIndices(int index) const override;
+  std::optional<int> GetParentIndex(int index) const override;
+  std::vector<SessionID> GetSessionIdsInGroup(
+      const tab_groups::TabGroupId& group) const override;
+  void NestTabsByStructure(
+      const std::vector<SessionID>& live_tab_ids,
+      const std::vector<std::unique_ptr<sessions::tab_restore::Tab>>& entries)
+      override;
   const gfx::Rect GetRestoredBounds() const override;
   ui::mojom::WindowShowState GetRestoredState() const override;
   std::string GetWorkspace() const override;
@@ -89,7 +101,8 @@ class BrowserLiveTabContext : public ses
       int tab_index,
       bool select,
       bool is_restoring_group_or_window,
-      sessions::tab_restore::Type original_session_type) override;
+      sessions::tab_restore::Type original_session_type,
+      std::optional<SessionID> parent_override) override;
   sessions::LiveTab* ReplaceRestoredTab(
       const sessions::tab_restore::Tab& tab) override;
   void CloseTab() override;
--- a/chrome/browser/ui/browser_tab_strip_model_delegate.cc
+++ b/chrome/browser/ui/browser_tab_strip_model_delegate.cc
@@ -275,6 +275,38 @@ void BrowserTabStripModelDelegate::Group
   }
 }
 
+void BrowserTabStripModelDelegate::WillCloseNestedSubtree(int parent_index) {
+  if (!BrowserSupportsHistoricalEntries()) {
+    return;
+  }
+  sessions::TabRestoreService* service =
+      TabRestoreServiceFactory::GetForProfile(browser_->profile());
+  if (service) {
+    sessions::LiveTabContext* context =
+        BrowserLiveTabContext::FindContextForWebContents(
+            browser_->GetTabStripModel()->GetWebContentsAt(parent_index));
+    service->CreateHistoricalNestedSubtree(context, parent_index);
+  }
+}
+
+void BrowserTabStripModelDelegate::NestedSubtreeClosed(
+    SessionID parent_session_id) {
+  sessions::TabRestoreService* service =
+      TabRestoreServiceFactory::GetForProfile(browser_->profile());
+  if (service) {
+    service->NestedSubtreeClosed(parent_session_id);
+  }
+}
+
+void BrowserTabStripModelDelegate::NestedSubtreeCloseStopped(
+    SessionID parent_session_id) {
+  sessions::TabRestoreService* service =
+      TabRestoreServiceFactory::GetForProfile(browser_->profile());
+  if (service) {
+    service->NestedSubtreeCloseStopped(parent_session_id);
+  }
+}
+
 bool BrowserTabStripModelDelegate::RunUnloadListenerBeforeClosing(
     content::WebContents* contents) {
   return browser_->RunUnloadListenerBeforeClosing(contents);
--- a/chrome/browser/ui/browser_tab_strip_model_delegate.h
+++ b/chrome/browser/ui/browser_tab_strip_model_delegate.h
@@ -13,6 +13,7 @@
 #include "chrome/browser/glic/host/glic.mojom.h"                    // nogncheck
 #include "chrome/browser/glic/public/glic_keyed_service_factory.h"  // nogncheck
 #include "chrome/browser/ui/tabs/tab_strip_model_delegate.h"
+#include "components/sessions/core/session_id.h"
 
 class GURL;
 
@@ -63,6 +64,9 @@ class BrowserTabStripModelDelegate : pub
   void GroupAdded(const tab_groups::TabGroupId& group) override;
   void WillCloseGroup(const tab_groups::TabGroupId& group) override;
   void GroupCloseStopped(const tab_groups::TabGroupId& group) override;
+  void WillCloseNestedSubtree(int parent_index) override;
+  void NestedSubtreeClosed(SessionID parent_session_id) override;
+  void NestedSubtreeCloseStopped(SessionID parent_session_id) override;
   bool RunUnloadListenerBeforeClosing(content::WebContents* contents) override;
   bool ShouldRunUnloadListenerBeforeClosing(
       content::WebContents* contents) override;
--- a/chrome/browser/ui/tabs/saved_tab_groups/local_tab_group_listener.cc
+++ b/chrome/browser/ui/tabs/saved_tab_groups/local_tab_group_listener.cc
@@ -67,7 +67,7 @@ void LocalTabGroupListener::ResumeTracki
                               : std::vector<SavedTabGroupTab>();
 
   const std::vector<tabs::TabInterface*> local_tabs =
-      SavedTabGroupUtils::GetTabsInGroup(local_id_);
+      SavedTabGroupUtils::GetNonNestedTabsInGroup(local_id_);
 
   CHECK_EQ(saved_tabs.size(), local_tabs.size());
   for (size_t i = 0; i < saved_tabs.size(); ++i) {
@@ -105,6 +105,11 @@ void LocalTabGroupListener::AddTabFromLo
     return;
   }
 
+  // Nested children have no saved counterpart; their owner brings them along.
+  if (SavedTabGroupUtils::IsNestedChild(local_tab, tab_strip_model)) {
+    return;
+  }
+
   CHECK(service_->GetGroup(saved_guid_).has_value());
   CHECK(tab_strip_model->group_model()->ContainsTabGroup(local_id_));
 
@@ -152,10 +157,14 @@ void LocalTabGroupListener::MoveWebConte
     return;
   }
 
-  const LocalTabID local_tab_id =
-      tab_strip_model->GetTabForWebContents(web_contents)
-          ->GetHandle()
-          .raw_value();
+  tabs::TabInterface* const moved_tab =
+      tab_strip_model->GetTabForWebContents(web_contents);
+  // Nested children have no saved counterpart.
+  if (SavedTabGroupUtils::IsNestedChild(moved_tab, tab_strip_model)) {
+    return;
+  }
+
+  const LocalTabID local_tab_id = moved_tab->GetHandle().raw_value();
   auto saved_group = service_->GetGroup(local_id_);
   CHECK(saved_group);
   const auto* saved_tab = saved_group->GetTab(local_tab_id);
@@ -181,14 +190,13 @@ void LocalTabGroupListener::MoveWebConte
       tab_strip_model->GetIndexOfTab(first_tab_in_group);
   CHECK_NE(tabstrip_index_of_first_tab_in_group, TabStripModel::kNoTab);
 
-  // Count the number of tabs that are actually in the group between
-  // `tabstrip_index_of_first_tab_in_group` and `tabstrip_index_of_moved_tab`.
-  // We must do this because a tab group may not be contiguous in intermediate
-  // states such as when dragging a group by its header.
+  // Count saved-visible group members (i.e. non-nested) between the group's
+  // first tab and the moved tab; a group may be non-contiguous mid-drag.
   int index_in_group = 0;
   for (int i = tabstrip_index_of_first_tab_in_group;
        i < tabstrip_index_of_moved_tab; i++) {
-    if (tab_strip_model->GetTabGroupForTab(i) == local_id_) {
+    if (tab_strip_model->GetTabGroupForTab(i) == local_id_ &&
+        !tab_strip_model->GetEnclosingNestedId(i).has_value()) {
       index_in_group++;
     }
   }
@@ -288,20 +296,28 @@ LocalTabGroupListener::Liveness LocalTab
 
   // Parallel iterate over saved tabs and local indices. For each saved tab and
   // index, ensure the corresponding local tab is at that index and in the
-  // correct state.
+  // correct state. Nested children occupy tabstrip slots inside the group
+  // range but have no saved counterpart, so skip past them when advancing.
+  const int range_end = static_cast<int>(group_index_range.end());
   int next_index_in_tab_strip = group_index_range.start();
   for (const SavedTabGroupTab& saved_tab : saved_group->saved_tabs()) {
+    while (next_index_in_tab_strip < range_end &&
+           tab_strip_model->GetEnclosingNestedId(next_index_in_tab_strip)
+               .has_value()) {
+      ++next_index_in_tab_strip;
+    }
     tabs::TabInterface* tab =
         saved_tab.local_tab_id().has_value()
             ? tabs::TabHandle(saved_tab.local_tab_id().value()).Get()
             : nullptr;
     MatchLocalTabToSavedTab(saved_tab, tab, tab_strip_model,
                             next_index_in_tab_strip);
-    next_index_in_tab_strip++;
+    ++next_index_in_tab_strip;
   }
 
   RemoveLocalWebContentsNotInSavedGroup();
-  CHECK_EQ(local_tab_group->ListTabs().length(),
+  // Nested children remain in the group but have no saved counterpart.
+  CHECK_EQ(SavedTabGroupUtils::GetNonNestedTabCountInGroup(local_id_),
            saved_group->saved_tabs().size());
 
   ResumeTracking();
@@ -368,9 +384,9 @@ void LocalTabGroupListener::RemoveLocalW
   const std::optional<SavedTabGroup> saved_group =
       service_->GetGroup(saved_guid_);
   CHECK(saved_group.has_value());
-  const std::vector<tabs::TabInterface*> tabs_in_local_group =
-      SavedTabGroupUtils::GetTabsInGroup(local_id_);
-  for (tabs::TabInterface* const local_tab : tabs_in_local_group) {
+  // GetNonNestedTabsInGroup filters nested children (no saved counterpart).
+  for (tabs::TabInterface* const local_tab :
+       SavedTabGroupUtils::GetNonNestedTabsInGroup(local_id_)) {
     if (!saved_group->ContainsTab(local_tab->GetHandle().raw_value())) {
       RemoveTabFromSync(local_tab, /*should_close_tab=*/true);
     }
--- a/chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_model_listener.cc
+++ b/chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_model_listener.cc
@@ -11,6 +11,7 @@
 #include "chrome/browser/ui/browser.h"
 #include "chrome/browser/ui/browser_window/public/browser_collection.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_interface_iterator.h"
+#include "chrome/browser/ui/browser_window/public/desktop_browser_window_capabilities.h"
 #include "chrome/browser/ui/browser_window/public/profile_browser_collection.h"
 #include "chrome/browser/ui/tabs/saved_tab_groups/local_tab_group_listener.h"
 #include "chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_utils.h"
@@ -138,6 +139,20 @@ void SavedTabGroupModelListener::TabGrou
     std::optional<tab_groups::TabGroupId> new_local_group_id,
     tabs::TabInterface* tab,
     int index) {
+  // Skip group-membership reconciliation during browser shutdown. The
+  // nested-tabs cascade-close machinery can emit per-tab "left group" events
+  // ahead of WillCloseAllTabs (which is what normally disconnects listeners),
+  // which would otherwise drain the saved group's tab list right before the
+  // browser closes. Suppressing these events preserves the saved group across
+  // restart.
+  if (tab) {
+    BrowserWindowInterface* const browser_window =
+        tab->GetBrowserWindowInterface();
+    if (browser_window &&
+        browser_window->capabilities()->IsAttemptingToCloseBrowser()) {
+      return;
+    }
+  }
   // Remove `contents` from its current saved group, if it's in one.
   for (auto& [local_group_id, listener] : local_tab_group_listeners_) {
     if (local_group_id != new_local_group_id) {
@@ -240,10 +255,10 @@ void SavedTabGroupModelListener::Connect
 
   // `tab_guid_mapping` should have one entry per tab in the local group. This
   // may not equal the saved group's size, if the saved group contains invalid
-  // URLs.
-  const size_t local_group_size =
-      SavedTabGroupUtils::GetTabGroupWithId(local_group_id)->tab_count();
-  CHECK_EQ(local_group_size, tab_guid_mapping.size());
+  // URLs. Nested children are local-only and aren't paired with saved tabs,
+  // so exclude them from the count.
+  CHECK_EQ(SavedTabGroupUtils::GetNonNestedTabCountInGroup(local_group_id),
+           tab_guid_mapping.size());
 
   auto [iterator, success] = local_tab_group_listeners_.try_emplace(
       local_group_id, local_group_id, saved_tab_group.saved_guid(), service_,
@@ -336,10 +351,10 @@ SavedTabGroupModelListener::CreateSavedT
       /*pinned=*/tab_groups::SavedTabGroupUtils::ShouldAutoPinNewTabGroups(
           profile_));
 
-  const gfx::Range tab_range = tab_group->ListTabs();
+  // Nested children have no saved counterpart.
   std::map<tabs::TabInterface*, base::Uuid> tab_guid_mapping;
   for (tabs::TabInterface* tab :
-       tab_strip_model->GetTabsAtIndices(tab_range.ToIntVector())) {
+       SavedTabGroupUtils::GetNonNestedTabsInGroup(group_id)) {
     CHECK(tab);
 
     tab_groups::SavedTabGroupTab saved_tab_group_tab =
--- a/chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_utils.cc
+++ b/chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_utils.cc
@@ -590,6 +590,47 @@ std::vector<tabs::TabInterface*> SavedTa
 }
 
 // static
+std::vector<tabs::TabInterface*> SavedTabGroupUtils::GetNonNestedTabsInGroup(
+    tab_groups::TabGroupId group_id) {
+  Browser* browser = GetBrowserWithTabGroupId(group_id);
+  if (!browser) {
+    return {};
+  }
+
+  TabStripModel* const tsm = browser->tab_strip_model();
+  const gfx::Range range =
+      SavedTabGroupUtils::GetTabGroupWithId(group_id)->ListTabs();
+  std::vector<tabs::TabInterface*> result;
+  for (size_t i = 0; i < range.length(); ++i) {
+    const int idx = static_cast<int>(range.start() + i);
+    if (tsm->GetEnclosingNestedId(idx).has_value()) {
+      continue;
+    }
+    result.push_back(tsm->GetTabAtIndex(idx));
+  }
+  return result;
+}
+
+// static
+size_t SavedTabGroupUtils::GetNonNestedTabCountInGroup(
+    tab_groups::TabGroupId group_id) {
+  return GetNonNestedTabsInGroup(group_id).size();
+}
+
+// static
+bool SavedTabGroupUtils::IsNestedChild(const tabs::TabInterface* tab,
+                                       TabStripModel* tab_strip_model) {
+  if (!tab || !tab_strip_model) {
+    return false;
+  }
+  const int idx = tab_strip_model->GetIndexOfTab(tab);
+  if (idx == TabStripModel::kNoTab) {
+    return false;
+  }
+  return tab_strip_model->GetEnclosingNestedId(idx).has_value();
+}
+
+// static
 SavedTabGroup SavedTabGroupUtils::CreateSavedTabGroupFromLocalId(
     const tab_groups::LocalTabGroupID& local_id) {
   Browser* browser = GetBrowserWithTabGroupId(local_id);
@@ -604,12 +645,12 @@ SavedTabGroup SavedTabGroupUtils::Create
       tab_groups::SavedTabGroupUtils::ShouldAutoPinNewTabGroups(
           browser->profile()));
 
-  const std::vector<content::WebContents*>& web_contentses =
-      tab_groups::SavedTabGroupUtils::GetWebContentsesInGroup(local_id);
-  for (content::WebContents* web_contents : web_contentses) {
+  // Nested children have no saved counterpart.
+  for (tabs::TabInterface* tab :
+       SavedTabGroupUtils::GetNonNestedTabsInGroup(local_id)) {
     tab_groups::SavedTabGroupTab saved_tab_group_tab =
         tab_groups::SavedTabGroupUtils::CreateSavedTabGroupTabFromWebContents(
-            web_contents, saved_tab_group.saved_guid());
+            tab->GetContents(), saved_tab_group.saved_guid());
     saved_tab_group.AddTabLocally(std::move(saved_tab_group_tab));
   }
 
--- a/chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_utils.h
+++ b/chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_utils.h
@@ -125,6 +125,20 @@ class SavedTabGroupUtils {
   static std::vector<tabs::TabInterface*> GetTabsInGroup(
       tab_groups::TabGroupId group_id);
 
+  // Like GetTabsInGroup, but excludes nested children. Nested children are
+  // local-only structural decoration and have no saved-group representation,
+  // so saved-tab-group code that compares live state against saved state
+  // should iterate / count via these helpers rather than the raw versions.
+  static std::vector<tabs::TabInterface*> GetNonNestedTabsInGroup(
+      tab_groups::TabGroupId group_id);
+  static size_t GetNonNestedTabCountInGroup(tab_groups::TabGroupId group_id);
+
+  // True if `tab` is a non-owner nested child in `tab_strip_model`. Saved-
+  // tab-group observers should early-return on such tabs because they have
+  // no saved counterpart.
+  static bool IsNestedChild(const tabs::TabInterface* tab,
+                            TabStripModel* tab_strip_model);
+
   // TODO(crbug.com/350514491) remove this once all cases are handled by
   // GetTabsInGroup. Prefer GetTabsInGroup over this method.
   // Returns the list of WebContentses in the local group `group_id` in order.
--- a/chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_web_contents_listener.cc
+++ b/chrome/browser/ui/tabs/saved_tab_groups/saved_tab_group_web_contents_listener.cc
@@ -238,6 +238,13 @@ void SavedTabGroupWebContentsListener::D
     return;
   }
 
+  // Tabs that aren't tracked by the saved group (e.g., nested children, which
+  // are local-only structural decoration with no saved counterpart) shouldn't
+  // produce saved-group navigation updates.
+  if (!group->GetTab(local_tab_id())) {
+    return;
+  }
+
   TabGroupSyncUtils::RecordSavedTabGroupNavigationUkmMetrics(
       local_tab_id(),
       group->collaboration_id() ? SavedTabGroupType::SHARED
--- a/chrome/browser/ui/tabs/saved_tab_groups/tab_group_sync_delegate_desktop.cc
+++ b/chrome/browser/ui/tabs/saved_tab_groups/tab_group_sync_delegate_desktop.cc
@@ -95,14 +95,15 @@ void RemoveExtraTabsFromLocalGroupBefore
   TabGroup* tab_group = tab_strip_model->group_model()->GetTabGroup(group_id);
   CHECK(tab_group);
 
-  // Collect the tabs to close because the range and tab indexes may change
-  // during removing the tabs from the group.
-  gfx::Range tab_range = tab_group->ListTabs();
+  // Nested children are local-only and have no saved counterpart; they ride
+  // along with their owner regardless of saved-vs-live count, so iterate via
+  // the non-nested helper.
+  const std::vector<tabs::TabInterface*> non_nested_tabs =
+      SavedTabGroupUtils::GetNonNestedTabsInGroup(group_id);
   std::vector<const tabs::TabInterface*> tabs_to_close;
-  for (size_t i = saved_group.saved_tabs().size(); i < tab_range.length();
+  for (size_t i = saved_group.saved_tabs().size(); i < non_nested_tabs.size();
        ++i) {
-    tabs_to_close.push_back(
-        tab_strip_model->GetTabAtIndex(i + tab_range.start()));
+    tabs_to_close.push_back(non_nested_tabs[i]);
   }
   for (const tabs::TabInterface* const tab : tabs_to_close) {
     CHECK(tab);
@@ -221,13 +222,15 @@ void TabGroupSyncDelegateDesktop::Connec
   CHECK(tab_group);
 
   // Associate all the local tabs with the saved group, note that it's done on
-  // best effort without verifying if they match or valid.
-  gfx::Range tab_range = tab_group->ListTabs();
-  CHECK_LE(tab_range.length(), group.saved_tabs().size());
+  // best effort without verifying if they match or valid. Nested children are
+  // local-only and have no saved entry to pair with, so skip them; their
+  // owners are paired normally.
+  const std::vector<tabs::TabInterface*> non_nested_tabs =
+      SavedTabGroupUtils::GetNonNestedTabsInGroup(group_id);
+  CHECK_LE(non_nested_tabs.size(), group.saved_tabs().size());
   std::map<tabs::TabInterface*, base::Uuid> tab_guid_mapping;
-  for (size_t i = 0; i < tab_range.length(); ++i) {
-    tabs::TabInterface* const tab =
-        tab_strip_model->GetTabAtIndex(tab_range.start() + i);
+  for (size_t i = 0; i < non_nested_tabs.size(); ++i) {
+    tabs::TabInterface* const tab = non_nested_tabs[i];
     CHECK(tab);
     tab_guid_mapping.emplace(tab, group.saved_tabs()[i].saved_tab_guid());
   }
--- a/chrome/browser/ui/tabs/tab_strip_api/tab_strip_model_impl/converters/tab_converters.cc
+++ b/chrome/browser/ui/tabs/tab_strip_api/tab_strip_model_impl/converters/tab_converters.cc
@@ -16,6 +16,7 @@
 #include "components/tabs/public/split_tab_collection.h"
 #include "components/tabs/public/split_tab_data.h"
 #include "components/tabs/public/tab_alert.h"
+#include "components/tabs/public/tab_collection.h"
 #include "components/tabs/public/tab_group.h"
 #include "components/tabs/public/tab_group_tab_collection.h"
 #include "components/tabs/public/tab_interface.h"
@@ -110,7 +111,9 @@ tabs_api::mojom::DataPtr BuildMojoTabCol
       mojo_pinned_tabs->id = node_id;
       return tabs_api::mojom::Data::NewPinnedTabs(std::move(mojo_pinned_tabs));
     }
-    case tabs::TabCollection::Type::UNPINNED: {
+    case tabs::TabCollection::Type::UNPINNED:
+    // Not dealing with this right now, unused and useless.
+    case tabs::TabCollection::Type::NESTED: {
       auto mojo_unpinned_tabs = tabs_api::mojom::UnpinnedTabs::New();
       mojo_unpinned_tabs->id = node_id;
       return tabs_api::mojom::Data::NewUnpinnedTabs(
--- a/chrome/browser/ui/tabs/tab_strip_api/tab_strip_model_impl/tab_strip_model_adapter_impl.cc
+++ b/chrome/browser/ui/tabs/tab_strip_api/tab_strip_model_impl/tab_strip_model_adapter_impl.cc
@@ -141,8 +141,10 @@ void TabStripModelAdapterImpl::MoveTab(t
       to_position =
           tab_strip_model_->IndexOfFirstNonPinnedTab() + position.index();
       break;
-    // TODO(crbug.com/412709271) Callers can not move a Tab within TabStrip and
-    // SplitTab collections. This should return an error to the client.
+    // TODO(crbug.com/412709271) Callers can not move a Tab within TabStrip,
+    // NestedTab, and SplitTab collections. This should return an error to the
+    // client.
+    case tabs::TabCollection::Type::NESTED:
     case tabs::TabCollection::Type::TABSTRIP:
     case tabs::TabCollection::Type::SPLIT:
       NOTIMPLEMENTED();
@@ -186,6 +188,8 @@ void TabStripModelAdapterImpl::MoveColle
                                     std::nullopt);
       break;
     }
+    // Moving a NESTED collection doesn't make sense, you always move the parent tab
+    case tabs::TabCollection::Type::NESTED:
     case tabs::TabCollection::Type::PINNED:
     case tabs::TabCollection::Type::UNPINNED:
     case tabs::TabCollection::Type::TABSTRIP:
--- a/chrome/browser/ui/tabs/tab_strip_model.cc
+++ b/chrome/browser/ui/tabs/tab_strip_model.cc
@@ -7,13 +7,16 @@
 #include <algorithm>
 #include <cstddef>
 #include <cstdint>
+#include <functional>
 #include <iterator>
 #include <map>
 #include <memory>
 #include <optional>
+#include <ranges>
 #include <set>
 #include <string>
 #include <utility>
+#include <variant>
 #include <vector>
 
 #include "base/check.h"
@@ -22,10 +25,12 @@
 #include "base/containers/adapters.h"
 #include "base/containers/flat_map.h"
 #include "base/containers/span.h"
+#include "base/containers/to_vector.h"
 #include "base/dcheck_is_on.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback.h"
+#include "base/logging.h"
 #include "base/memory/raw_ptr.h"
 #include "base/memory/weak_ptr.h"
 #include "base/metrics/histogram_functions.h"
@@ -94,16 +99,22 @@
 #include "components/content_settings/core/common/content_settings.h"
 #include "components/content_settings/core/common/content_settings_types.h"
 #include "components/feature_engagement/public/feature_constants.h"
+#include "components/nested_tabs/nested_tab_id.h"
+#include "components/nested_tabs/nested_tab_visual_data.h"
 #include "components/performance_manager/public/performance_manager.h"
 #include "components/prefs/pref_service.h"
 #include "components/reading_list/core/reading_list_model.h"
+#include "components/sessions/content/session_tab_helper.h"
 #include "components/split_tabs/split_tab_id.h"
 #include "components/split_tabs/split_tab_visual_data.h"
 #include "components/tab_groups/tab_group_id.h"
 #include "components/tab_groups/tab_group_visual_data.h"
+#include "components/tabs/public/nested_tab_collection.h"
+#include "components/tabs/public/nested_tab_data.h"
 #include "components/tabs/public/pinned_tab_collection.h"
 #include "components/tabs/public/split_tab_collection.h"
 #include "components/tabs/public/split_tab_data.h"
+#include "components/tabs/public/tab_collection.h"
 #include "components/tabs/public/tab_group_tab_collection.h"
 #include "components/tabs/public/tab_interface.h"
 #include "components/tabs/public/tab_strip_collection.h"
@@ -196,8 +207,8 @@ base::WeakPtr<performance_manager::PageN
   }
 
   base::WeakPtr<performance_manager::PageNode> page_node =
-      performance_manager::PerformanceManager::
-        GetPrimaryPageNodeForWebContents(contents);
+      performance_manager::PerformanceManager::GetPrimaryPageNodeForWebContents(
+          contents);
 
   if (!page_node) {
     return nullptr;
@@ -234,7 +245,8 @@ std::unique_ptr<TabGroupModel> TabGroupM
 
 DetachedTabCollection::DetachedTabCollection(
     std::variant<std::unique_ptr<tabs::TabGroupTabCollection>,
-                 std::unique_ptr<tabs::SplitTabCollection>> collection,
+                 std::unique_ptr<tabs::SplitTabCollection>,
+                 std::unique_ptr<tabs::NestedTabCollection>> collection,
     std::optional<int> active_index,
     bool pinned)
     : collection_(std::move(collection)),
@@ -292,6 +304,10 @@ struct TabStripModel::DetachNotification
 ///////////////////////////////////////////////////////////////////////////////
 // TabStripModel, public:
 
+std::string TabStripModel::DebugDumpCollections() const {
+  return contents_data_->DebugDump();
+}
+
 constexpr int TabStripModel::kNoTab;
 
 TabStripModel::TabStripModel(TabStripModelDelegate* delegate,
@@ -526,6 +542,10 @@ TabStripModel::DetachTabsAndCollectionsF
     const std::vector<int>& tab_indices) {
   const std::vector<tab_groups::TabGroupId> groups_to_move =
       GetGroupsDestroyedFromRemovingIndices(tab_indices);
+  const std::vector<nested_tabs::NestedTabId> nesteds_to_move =
+      GetNestedsDestroyedFromRemovingIndices(tab_indices);
+  const std::set<nested_tabs::NestedTabId> nesteds_to_move_set(
+      nesteds_to_move.begin(), nesteds_to_move.end());
 
   std::vector<tabs::TabInterface*> tab_interfaces =
       GetTabsAtIndices(tab_indices);
@@ -537,11 +557,25 @@ TabStripModel::DetachTabsAndCollectionsF
   for (const tabs::TabInterface* tab_interface : tab_interfaces) {
     const int index = GetIndexOfTab(tab_interface);
     if (index == TabStripModel::kNoTab) {
-      // If this is a tab, we already moved it as part of its group.
+      // If this is a tab, we already moved it as part of its group/nested.
       // If this is a header, we will move it when we get to its first tab.
       continue;
     }
 
+    // A NESTED is detached as a unit whenever its owner is in the move set.
+    // Non-owner children whose owner is in the set will already have been
+    // moved by their NESTED's detach (and skipped above via kNoTab). Check
+    // NESTED before group/split because a NESTED inside a group can be
+    // drained without the group being drained.
+    const std::optional<nested_tabs::NestedTabId> owned_nested =
+        GetOwnedNestedId(tab_interface);
+    if (owned_nested.has_value() &&
+        nesteds_to_move_set.contains(*owned_nested)) {
+      owned_tabs_and_collections.emplace_back(
+          DetachNestedForInsertion(*owned_nested));
+      continue;
+    }
+
     const std::optional<tab_groups::TabGroupId> group =
         tab_interface->GetGroup();
     if (std::find(groups_to_move.begin(), groups_to_move.end(), group) !=
@@ -640,6 +674,43 @@ TabStripModel::DetachTabGroupForInsertio
 }
 
 std::unique_ptr<DetachedTabCollection>
+TabStripModel::DetachNestedForInsertion(
+    const nested_tabs::NestedTabId nested_id) {
+  ReentrancyCheck reentrancy_check(&reentrancy_guard_);
+
+  tabs::NestedTabCollection* nested_collection =
+      contents_data_->GetNestedTabCollection(nested_id);
+  CHECK(nested_collection);
+
+  std::optional<int> active_index_in_collection = std::nullopt;
+  int index = 0;
+  for (tabs::TabInterface* tab : *nested_collection) {
+    if (tab->IsActivated()) {
+      active_index_in_collection = index;
+      break;
+    }
+    index++;
+  }
+
+  // No NestedTabChange on detach: kDestroyed would make listeners think the
+  // NESTED is permanently gone and try to look up detached tabs' indices.
+  // The destination's NotifyNestedAttached re-establishes it in the new
+  // model; source views react via TabStripModelChange::Remove.
+  std::unique_ptr<tabs::TabCollection> detached_collection =
+      DetachTabCollectionImpl(
+          nested_collection,
+          base::BindOnce(&tabs::TabStripCollection::RemoveTabCollection,
+                         base::Unretained(contents_data_.get()),
+                         nested_collection),
+          base::DoNothing());
+
+  return std::make_unique<DetachedTabCollection>(
+      base::WrapUnique(static_cast<tabs::NestedTabCollection*>(
+          detached_collection.release())),
+      active_index_in_collection, /*pinned=*/false);
+}
+
+std::unique_ptr<DetachedTabCollection>
 TabStripModel::DetachSplitTabForInsertion(
     const split_tabs::SplitTabId split_id) {
   ReentrancyCheck reentrancy_check(&reentrancy_guard_);
@@ -748,6 +819,40 @@ gfx::Range TabStripModel::InsertDetached
                      base::Unretained(this), group_collection));
 }
 
+gfx::Range TabStripModel::InsertDetachedNestedAt(
+    std::unique_ptr<DetachedTabCollection> nested,
+    int index,
+    std::optional<tab_groups::TabGroupId> group_id) {
+  ReentrancyCheck reentrancy_check(&reentrancy_guard_);
+  CHECK(std::holds_alternative<std::unique_ptr<tabs::NestedTabCollection>>(
+      nested->collection_));
+
+  std::unique_ptr<tabs::NestedTabCollection> nested_collection_unique_ptr =
+      std::move(std::get<std::unique_ptr<tabs::NestedTabCollection>>(
+          nested->collection_));
+
+  tabs::NestedTabCollection* nested_collection =
+      nested_collection_unique_ptr.get();
+
+  // A NESTED with the same id must not already exist in this tabstrip.
+  CHECK(!contents_data_->GetNestedTabCollection(
+      nested_collection->GetNestedTabId()));
+
+  // Notify tab is added to model.
+  for (tabs::TabInterface* tab : *nested_collection) {
+    static_cast<tabs::TabModel*>(tab)->OnAddedToModel(this);
+  }
+
+  return InsertDetachedCollectionImpl(
+      nested_collection, nested->active_index_,
+      base::BindOnce(&tabs::TabStripCollection::InsertTabCollectionAt,
+                     base::Unretained(contents_data_.get()),
+                     std::move(nested_collection_unique_ptr), index,
+                     /*pinned=*/false, group_id),
+      base::BindOnce(&TabStripModel::NotifyNestedAttached,
+                     base::Unretained(this), nested_collection));
+}
+
 tabs::TabModel* TabStripModel::GetTabModelAtIndex(int index) const {
   return static_cast<tabs::TabModel*>(GetTabAtIndex(index));
 }
@@ -1139,7 +1244,8 @@ int TabStripModel::MoveWebContentsAt(
 
 void TabStripModel::MoveSelectedTabsTo(
     int index,
-    std::optional<tab_groups::TabGroupId> into_group) {
+    std::optional<tab_groups::TabGroupId> into_group,
+    std::optional<nested_tabs::NestedTabId> enclosing_nested) {
   ReentrancyCheck reentrancy_check(&reentrancy_guard_);
 
   const int pinned_tab_count = IndexOfFirstNonPinnedTab();
@@ -1162,7 +1268,7 @@ void TabStripModel::MoveSelectedTabsTo(
                  count() - static_cast<int>(unpinned_selected_indices.size()));
 
   MoveTabsToIndexImpl(unpinned_selected_indices, first_unpinned_index,
-                      into_group);
+                      into_group, enclosing_nested);
 }
 
 void TabStripModel::MoveGroupTo(const tab_groups::TabGroupId& group,
@@ -1227,7 +1333,8 @@ void TabStripModel::MoveSplitTo(
       tab_indices, to_index,
       base::BindOnce(&tabs::TabStripCollection::MoveTabsRecursive,
                      base::Unretained(contents_data_.get()), tab_indices,
-                     to_index, group_id, pinned, kRetainCollectionTypes));
+                     to_index, group_id, std::nullopt, pinned,
+                     kRetainCollectionTypes));
 }
 
 void TabStripModel::MoveGroupToImpl(const tab_groups::TabGroupId& group,
@@ -1241,14 +1348,16 @@ void TabStripModel::MoveGroupToImpl(cons
   }
 
   static constexpr tabs::TabCollection::TypeEnumSet kRetainCollectionTypes = {
-      tabs::TabCollection::Type::SPLIT, tabs::TabCollection::Type::GROUP};
+      tabs::TabCollection::Type::SPLIT, tabs::TabCollection::Type::GROUP,
+      tabs::TabCollection::Type::NESTED};
 
   // Remove all the tabs from the model.
   MoveTabsWithNotifications(
       tab_indices, to_index,
       base::BindOnce(&tabs::TabStripCollection::MoveTabsRecursive,
                      base::Unretained(contents_data_.get()), tab_indices,
-                     to_index, std::nullopt, false, kRetainCollectionTypes));
+                     to_index, std::nullopt, std::nullopt, false,
+                     kRetainCollectionTypes));
 
   NotifyTabGroupMoved(group);
 }
@@ -1372,10 +1481,11 @@ void TabStripModel::CloseAllTabsInGroupI
 
 void TabStripModel::CloseWebContentsAt(int index, uint32_t close_types) {
   CHECK(ContainsIndex(index));
-  if (base::CommandLine::ForCurrentProcess()->
-          GetSwitchValueASCII("close-window-with-last-tab") == "never" &&
-          !closing_all_ && count() < 2)
+  if (base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
+          "close-window-with-last-tab") == "never" &&
+      !closing_all_ && count() < 2) {
     delegate()->AddTabAt(GURL(), -1, true);
+  }
   CloseTabs({GetWebContentsAt(index)}, close_types);
 }
 
@@ -1862,6 +1972,10 @@ void TabStripModel::AddTab(std::unique_p
   }
   tabs::TabModel* tab_ptr = tab.get();
   tab->OnAddedToModel(this);
+
+  // Make sure this can't be inserted between a NESTED and its parent
+  index = MaybeBumpIndexPastNested(index);
+
   InsertTabAtImpl(index, std::move(tab),
                   add_types | (inherit_opener ? ADD_INHERIT_OPENER : 0), group);
   // Reset the index, just in case insert ended up moving it on us.
@@ -1893,6 +2007,22 @@ void TabStripModel::AddTab(std::unique_p
   }
 }
 
+int TabStripModel::MaybeBumpIndexPastNested(int index) const {
+  if (index <= 0 || index > count()) {
+    return index;
+  }
+  tabs::TabInterface* prev_tab = GetTabAtIndex(index - 1);
+  if (!prev_tab) {
+    return index;
+  }
+  auto* nested = contents_data_->GetNestedTabCollection(prev_tab);
+  if (!nested) {
+    return index;
+  }
+  const int nested_end = (index - 1) + nested->TabCountRecursive();
+  return std::max(index, nested_end);
+}
+
 void TabStripModel::CloseSelectedTabs() {
   auto get_tabs = base::BindRepeating(
       [](const tabs::TabStripModelSelectionState* selection_model) {
@@ -2199,6 +2329,802 @@ void TabStripModel::RemoveSplit(split_ta
   CompleteModelUpdateTransaction();
 }
 
+void TabStripModel::ResolveCascade(tabs::TabInterface* root,
+                                   std::set<content::WebContents*>& expanded) {
+  // Pass 1: enumerate all tabs in cascade and find refusers.
+  auto* owned = contents_data_->GetNestedTabCollection(root);
+  std::vector<tabs::TabInterface*> all_tabs =
+      owned ? owned->GetTabsRecursive()
+            : std::vector<tabs::TabInterface*>{root};
+
+  base::flat_set<tabs::TabInterface*> must_stay;
+  for (auto* t : all_tabs) {
+    if (!IsTabClosable(t->GetContents())) {
+      must_stay.insert(t);
+    }
+  }
+
+  // Pass 2: propagate must_stay up through ancestors.
+  // For each refuser, walk parent → grandparent → ... until we exit `root`'s
+  // subtree. Mark each visited tab as must_stay.
+  base::flat_set<tabs::TabInterface*> cascade_members(all_tabs.begin(),
+                                                      all_tabs.end());
+  for (auto* refuser : must_stay) {
+    auto* current = refuser;
+    while (current && current != root) {
+      auto* parent_coll = current->GetParentCollection();
+      // Skip past SPLIT/GROUP to find the enclosing NESTED.
+      while (parent_coll &&
+             parent_coll->type() != tabs::TabCollection::Type::NESTED) {
+        parent_coll = parent_coll->GetParentCollection();
+      }
+      if (!parent_coll) {
+        break;  // no NESTED ancestor
+      }
+
+      auto* nested = static_cast<const tabs::NestedTabCollection*>(parent_coll);
+      auto* parent_tab = nested->data()->owner_tab().Get();
+      if (!parent_tab || !cascade_members.contains(parent_tab)) {
+        break;
+      }
+      must_stay.insert(parent_tab);
+      current = parent_tab;
+    }
+  }
+
+  // Pass 3: tabs not in must_stay → cascade-close them.
+  for (auto* t : all_tabs) {
+    if (!must_stay.contains(t)) {
+      expanded.insert(t->GetContents());
+    }
+  }
+
+  // If the root itself stays, fire OnTabCloseCancelled for the user-directed
+  // tab. The descendants weren't user-directed, so don't fire for them.
+  if (must_stay.contains(root)) {
+    for (auto& obs : observers_) {
+      obs.OnTabCloseCancelled(root);
+    }
+  }
+}
+
+std::optional<nested_tabs::NestedTabId> TabStripModel::Nest(
+    std::optional<int> parent,
+    std::vector<int> children,
+    std::optional<nested_tabs::NestedTabVisualData> visual_data,
+    std::optional<size_t> target_child_idx) {
+  ReentrancyCheck reentrancy_check(&reentrancy_guard_);
+  auto result = NestImpl(parent, children, visual_data, target_child_idx);
+  // Flush pending TabCollectionObserver notifications queued by
+  // FindOrCreateNestedFor / MoveTabToNested. Without this they would only
+  // dispatch on the next mutation, leaving the view tree one event behind.
+  CompleteModelUpdateTransaction();
+  return result;
+}
+
+void TabStripModel::RestoreNested(
+    const nested_tabs::NestedTabId& nested_id,
+    tabs::TabInterface* owner,
+    const std::vector<tabs::TabInterface*>& children,
+    const nested_tabs::NestedTabVisualData& visual_data) {
+  ReentrancyCheck reentrancy_check(&reentrancy_guard_);
+  int owner_idx = GetIndexOfTab(owner);
+  auto children_idxs =
+      children |
+      std::views::transform([&](auto tab) { return GetIndexOfTab(tab); }) |
+      std::ranges::to<std::vector<int>>();
+  if (std::ranges::is_sorted(children_idxs) &&
+      (children.empty() || owner_idx < children_idxs.front())) {
+    std::ignore =
+        NestImpl(owner_idx, children_idxs, visual_data, {}, nested_id);
+  } else {
+      LOG(ERROR) << "Nested precondition failed on restore, falling through";
+  }
+
+  CompleteModelUpdateTransaction();
+}
+
+std::optional<nested_tabs::NestedTabId> TabStripModel::NestImpl(
+    std::optional<int> parent,
+    std::vector<int> children,
+    std::optional<nested_tabs::NestedTabVisualData> visual_data,
+    std::optional<size_t> target_child_idx,
+    std::optional<nested_tabs::NestedTabId> id_override) {
+  if (children.empty()) {
+    return std::nullopt;
+  }
+  tabs::TabInterface* parent_tab = parent ? GetTabAtIndex(*parent) : nullptr;
+  CHECK(!parent || parent_tab) << "Unable to find parent tab at index!";
+  CHECK(!parent_tab->IsPinned()) << "Pinned tabs cannot participate in NESTED!";
+
+  // Need to filter tabs from the same SPLIT
+  std::set<split_tabs::SplitTabId> seen_splits = {};
+  std::vector<tabs::TabInterface*> children_tabs =
+      children | std::views::transform([&](auto idx) {
+        return this->GetTabAtIndex(idx);
+      }) |
+      std::views::filter([&](auto tab) {
+        if (!tab) {
+          return false;
+        }
+
+        auto split = tab->GetSplit();
+        if (split) {
+          if (seen_splits.contains(*split)) {
+            return false;
+          }
+          seen_splits.insert(*split);
+        }
+
+        return true;
+      }) |
+      std::ranges::to<std::vector>();
+
+  // Check for possible cycles
+  // A cycle is defined as the parent being within any child's tree
+  CHECK(!parent ||
+        !IsDescendantOfAny(*parent, std::set(children.begin(), children.end())))
+      << "Cycle detected while trying to nest tabs.";
+
+  // No cycles detected, proceed with nest.
+
+  // Snapshot per source: id, owner (in case destroyed), and removed tabs.
+  struct SourceSnapshot {
+    nested_tabs::NestedTabId id;
+    tabs::TabHandle owner;
+    std::vector<tabs::TabHandle> removed_children;
+  };
+  base::flat_map<nested_tabs::NestedTabId, SourceSnapshot> sources;
+
+  for (auto* child : children_tabs) {
+    auto* p = child->GetParentCollection();
+    if (p && p->type() == tabs::TabCollection::Type::SPLIT) {
+      p = p->GetParentCollection();
+    }
+    if (p && p->type() == tabs::TabCollection::Type::NESTED) {
+      auto* nested = static_cast<const tabs::NestedTabCollection*>(p);
+      auto& snap = sources
+                       .try_emplace(nested->data()->id(),
+                                    SourceSnapshot{nested->data()->id(),
+                                                   nested->data()->owner_tab(),
+                                                   {}})
+                       .first->second;
+      snap.removed_children.push_back(child->GetHandle());
+    }
+  }
+
+  //  Do the moves.
+  std::optional<nested_tabs::NestedTabId> retval;
+  if (parent_tab) {
+    const auto [parent_nested, did_create] =
+        contents_data_->FindOrCreateNestedFor(parent_tab, visual_data,
+                                              id_override);
+
+    // Remove the destination from the sources map AFTER it's known (now that
+    // parent_nested is in scope), there's no net change for tabs already
+    // there.
+    sources.erase(parent_nested->data()->id());
+
+    // |target_child_idx| is the position among the parent's non-owner
+    // children (matches GetChildIndices() semantics). The owner occupies
+    // the NESTED collection's children[0], so shift by 1 when computing the
+    // collection-level insertion index.
+    size_t offset = 0;
+    for (auto* child : children_tabs) {
+      contents_data_->MoveTabToNested(
+          child, parent_nested->data()->id(),
+          target_child_idx.transform([&](auto i) { return i + offset + 1; }));
+      if (target_child_idx) {
+        offset++;
+      }
+      // ignore the returned NestedDestruction, handled in the dispatch loop
+    }
+
+    auto child_handles =
+        children_tabs |
+        std::views::transform([](auto t) { return t->GetHandle(); }) |
+        std::ranges::to<std::vector>();
+
+    if (did_create) {
+      observers_.Notify(
+          &TabStripModelObserver::OnNestedTabChanged,
+          NestedTabChange(this, parent_nested->data()->id(),
+                          NestedTabChange::CreatedChange(
+                              parent_tab->GetHandle(), std::move(child_handles),
+                              visual_data.value_or({}))));
+    } else {
+      observers_.Notify(&TabStripModelObserver::OnNestedTabChanged,
+                        NestedTabChange(this, parent_nested->data()->id(),
+                                        NestedTabChange::ContentsChange(
+                                            std::move(child_handles), {})));
+    }
+
+    retval = parent_nested->data()->id();
+  } else {
+    for (auto* child : children_tabs) {
+      contents_data_->MoveTabToTopLevel(child);
+    }
+    retval = std::nullopt;
+  }
+
+  // Single dispatch loop for every snapshot source.
+  for (auto& [id, snap] : sources) {
+    if (contents_data_->GetNestedTabCollection(id)) {
+      // Source still alive: contents changed.
+      observers_.Notify(
+          &TabStripModelObserver::OnNestedTabChanged,
+          NestedTabChange(this, id,
+                          NestedTabChange::ContentsChange(
+                              {}, std::move(snap.removed_children))));
+    } else {
+      // Source destroyed during moves.
+      observers_.Notify(
+          &TabStripModelObserver::OnNestedTabChanged,
+          NestedTabChange(this, id,
+                          NestedTabChange::DestroyedChange(
+                              snap.owner, std::move(snap.removed_children))));
+    }
+  }
+
+  return retval;
+}
+
+void TabStripModel::Unnest(const nested_tabs::NestedTabId& nested_id) {
+  ReentrancyCheck reentrancy_check(&reentrancy_guard_);
+
+  UnnestImpl(nested_id);
+  CompleteModelUpdateTransaction();
+}
+
+void TabStripModel::UnnestImpl(const nested_tabs::NestedTabId& nested_id) {
+  tabs::NestedTabCollection* nested_coll =
+      contents_data_->GetNestedTabCollection(nested_id);
+  CHECK(nested_coll);
+
+  tabs::TabHandle destroyed = nested_coll->data()->owner_tab();
+  std::vector<tabs::TabHandle> orphans;
+  for (const auto& child : nested_coll->GetChildren()) {
+    if (auto* t = std::get_if<std::unique_ptr<tabs::TabInterface>>(&child)) {
+      orphans.push_back((*t)->GetHandle());
+    } else {
+      auto* coll = std::get<std::unique_ptr<tabs::TabCollection>>(child).get();
+      if (coll->type() == tabs::TabCollection::Type::SPLIT) {
+        // Tabs inside SPLIT also lose their nesting affiliation under this
+        // NESTED, even though they live one layer deep in the storage tree.
+        for (const auto& split_child : coll->GetChildren()) {
+          if (auto* st = std::get_if<std::unique_ptr<tabs::TabInterface>>(
+                  &split_child)) {
+            orphans.push_back((*st)->GetHandle());
+          }
+        }
+      }
+      // Sub-NESTEDs: their tabs are still nested (just under a sub-owner that's
+      // now in grandparent). They didn't lose their nesting. Skip.
+    }
+  }
+
+  // Promote all children to NESTED's parent at NESTED's slot, then destroy
+  // the NESTED. Preserves child order: owner first (it's children[0]), then
+  // non-owner members in their existing order.
+  contents_data_->UnnestCollection(nested_coll);
+
+  observers_.Notify(&TabStripModelObserver::OnNestedTabChanged,
+                    NestedTabChange(this, nested_id,
+                                    NestedTabChange::DestroyedChange(
+                                        destroyed, std::move(orphans))));
+}
+
+void TabStripModel::TryCompletePendingCascade(content::WebContents* descendant,
+                                              bool cancelled) {
+  // First, walk pending_cascades_ and either cancel cascades (cancelled=true)
+  // or remove this descendant from any cascade that's waiting on it. Collect
+  // the parents of cascades that just emptied, DO NOT call CloseTabs from
+  // inside this loop. CloseTabs synchronously closes the parent, which fires
+  // its own WebContentsDestroyed → re-entrant call into this function →
+  // mutates pending_cascades_ → invalidates our outer iterator.
+  struct CompletedCascade {
+    content::WebContents* parent;
+    uint32_t close_types;
+    SessionID parent_session_id;
+  };
+  std::vector<CompletedCascade> completed;
+
+  for (auto it = pending_cascades_.begin(); it != pending_cascades_.end();) {
+    auto& pending = *it;
+    auto desc_it = pending->descendants.find(descendant);
+    if (desc_it == pending->descendants.end()) {
+      ++it;
+      continue;
+    }
+
+    if (cancelled) {
+      // User cancelled beforeunload for this descendant. Abort the entire
+      // cascade, parent stays alive. PendingCascade destructor releases
+      // all sibling observers automatically.
+      if ((*it)->close_types & TabCloseTypes::CLOSE_CREATE_HISTORICAL_TAB) {
+        delegate_->NestedSubtreeCloseStopped((*it)->parent_session_id);
+        // Owner won't destruct (the close was cancelled), so the observer
+        // must be torn down explicitly here.
+        nested_subtree_owner_observers_.erase((*it)->parent);
+      }
+      it = pending_cascades_.erase(it);
+      continue;
+    }
+
+    pending->descendants.erase(desc_it);
+
+    if (pending->descendants.empty()) {
+      completed.push_back({pending->parent.get(), pending->close_types,
+                           pending->parent_session_id});
+      it = pending_cascades_.erase(it);
+      continue;
+    }
+
+    ++it;
+  }
+
+  // Now safe to recurse. Re-entry into TryCompletePendingCascade from any of
+  // these CloseTabs calls will iterate a freshly-stable pending_cascades_.
+  for (auto& c : completed) {
+    if (c.parent) {
+      CloseTabs({c.parent}, c.close_types);
+      // NestedSubtreeClosed cleanup is fired by NestedSubtreeOwnerObserver
+      // when the owner's WebContents actually destructs. Firing it here
+      // would clear the suppression set before the owner's
+      // CreateHistoricalTab runs (the close may be deferred by an unload
+      // listener).
+    }
+  }
+}
+
+void TabStripModel::OnNestedSubtreeOwnerDestroyed(
+    content::WebContents* contents,
+    SessionID parent_session_id) {
+  delegate_->NestedSubtreeClosed(parent_session_id);
+  nested_subtree_owner_observers_.erase(contents);
+}
+
+std::optional<int> TabStripModel::GetParentIndex(int child_idx) const {
+  tabs::TabInterface* tab = GetTabAtIndex(child_idx);
+  CHECK(tab);
+
+  // Find the first enclosing NESTED whose owner is NOT `tab` itself
+  // (an owner's immediate parent IS its own NESTED, which isn't its parent
+  // conceptually).
+  for (const tabs::TabCollection* p = tab->GetParentCollection(); p;
+       p = p->GetParentCollection()) {
+    if (p->type() == tabs::TabCollection::Type::TABSTRIP) {
+      return std::nullopt;
+    }
+    if (p->type() != tabs::TabCollection::Type::NESTED) {
+      continue;
+    }
+    tabs::TabInterface* owner = static_cast<const tabs::NestedTabCollection*>(p)
+                                    ->data()
+                                    ->owner_tab()
+                                    .Get();
+    if (owner != tab) {
+      return GetIndexOfTab(owner);
+    }
+  }
+  return std::nullopt;
+}
+
+std::vector<int> TabStripModel::GetChildIndices(int parent_idx) const {
+  tabs::TabInterface* tab = GetTabAtIndex(parent_idx);
+  CHECK(tab);
+
+  std::vector<int> children;
+  if (tabs::NestedTabCollection* nested =
+          contents_data_->GetNestedTabCollection(tab)) {
+    // Skip children[0] (the owner itself) and only return non-owner members.
+    const auto& all_children = nested->GetChildren();
+    for (size_t i = 1; i < all_children.size(); ++i) {
+      const auto& child = all_children[i];
+      if (const auto& child_coll =
+              std::get_if<std::unique_ptr<tabs::TabCollection>>(&child)) {
+        tabs::TabCollection* coll = child_coll->get();
+        if (coll->type() == tabs::TabCollection::Type::SPLIT) {
+          for (const auto& split_child : coll->GetChildren()) {
+            if (auto scti = std::get_if<std::unique_ptr<tabs::TabInterface>>(
+                    &split_child)) {
+              children.push_back(GetIndexOfTab(scti->get()));
+            }
+          }
+        } else if (coll->type() == tabs::TabCollection::Type::NESTED) {
+          // Sub-NESTED's owner is conceptually a direct child of `parent`.
+          // Return the owner's index, not the bare collection.
+          tabs::TabInterface* owner =
+              static_cast<tabs::NestedTabCollection*>(coll)
+                  ->data()
+                  ->owner_tab()
+                  .Get();
+          children.push_back(GetIndexOfTab(owner));
+        }
+      } else {
+        children.push_back(GetIndexOfTab(
+            std::get<std::unique_ptr<tabs::TabInterface>>(child).get()));
+      }
+    }
+  } else {
+    return {};
+  }
+
+  return children;
+}
+
+bool TabStripModel::IsDescendantOfAny(int subject,
+                                      const std::set<int>& maybe_ancestor_idxs,
+                                      bool self_is_ancestor) const {
+  tabs::TabInterface* subject_tab = GetTabAtIndex(subject);
+  std::set<tabs::TabInterface*> ancestor_tabs =
+      maybe_ancestor_idxs | std::views::transform([&](auto idx) {
+        return this->GetTabAtIndex(idx);
+      }) |
+      std::views::filter([](auto tab) { return tab; }) |
+      std::ranges::to<std::set>();
+
+  if (!subject_tab || ancestor_tabs.empty()) {
+    return false;
+  }
+
+  if (ancestor_tabs.contains(subject_tab) && self_is_ancestor) {
+    return true;
+  }
+
+  for (const tabs::TabCollection* c = subject_tab->GetParentCollection(); c;
+       c = c->GetParentCollection()) {
+    if (c->type() != tabs::TabCollection::Type::NESTED) {
+      continue;
+    }
+    const auto* nested = static_cast<const tabs::NestedTabCollection*>(c);
+    tabs::TabInterface* nested_owner = nested->data()->owner_tab().Get();
+    // Subject's own NESTED isn't an ancestor that "owns" it.
+    if (nested_owner == subject_tab) {
+      continue;
+    }
+    if (ancestor_tabs.contains(nested_owner)) {
+      return true;
+    }
+  }
+
+  return false;
+}
+
+bool TabStripModel::IsSubtreeCollapsed(
+    nested_tabs::NestedTabId nested_id) const {
+  // View-layer callers can query during transient states (e.g., the NESTED
+  // is mid-attach to a destination tabstrip during a cross-window drag).
+  // Default to not-collapsed when the NESTED isn't visible to this model.
+  tabs::NestedTabCollection* ntc =
+      contents_data_->GetNestedTabCollection(nested_id);
+  return ntc && ntc->IsCollapsed();
+}
+
+void TabStripModel::SetSubtreeCollapsed(nested_tabs::NestedTabId nested_id,
+                                        bool is_collapsed) {
+  ReentrancyCheck reentrancy_check(&reentrancy_guard_);
+  SetSubtreeCollapsedImpl(nested_id, is_collapsed);
+  CompleteModelUpdateTransaction();
+}
+
+void TabStripModel::SetSubtreeCollapsedImpl(nested_tabs::NestedTabId nested_id,
+                                            bool is_collapsed) {
+  tabs::NestedTabCollection* ntc =
+      contents_data_->GetNestedTabCollection(nested_id);
+  CHECK(ntc);
+
+  bool should_notify = ntc->IsCollapsed() != is_collapsed;
+
+  if (should_notify) {
+    nested_tabs::NestedTabVisualData old = *ntc->data()->visual_data();
+    ntc->SetIsCollapsed(is_collapsed);
+    observers_.Notify(&TabStripModelObserver::OnNestedTabChanged,
+                      NestedTabChange(this, nested_id,
+                                      NestedTabChange::VisualsChange(
+                                          old, *ntc->data()->visual_data())));
+  }
+}
+
+std::set<nested_tabs::NestedTabId> TabStripModel::ListNesteds() const {
+  return contents_data_->ListNesteds();
+}
+
+const nested_tabs::NestedTabData* TabStripModel::GetNestingData(
+    nested_tabs::NestedTabId nested_id) const {
+  tabs::NestedTabCollection* ntc =
+      contents_data_->GetNestedTabCollection(nested_id);
+
+  return ntc ? ntc->data() : nullptr;
+}
+
+std::optional<nested_tabs::NestedTabId> TabStripModel::GetEnclosingNestedId(
+    int index) const {
+  tabs::TabInterface* tab = GetTabAtIndex(index);
+  if (!tab) {
+    return {};
+  }
+
+  // Deepest NESTED containing `tab` as a non-owner child. Self-owning
+  // NESTEDs are skipped, see GetInnermostNestedId for the inclusive walk.
+  for (const tabs::TabCollection* c = tab->GetParentCollection(); c;
+       c = c->GetParentCollection()) {
+    if (c->type() != tabs::TabCollection::Type::NESTED) {
+      continue;
+    }
+    const auto* nested = static_cast<const tabs::NestedTabCollection*>(c);
+    if (nested->data()->owner_tab().Get() == tab) {
+      continue;
+    }
+    return nested->GetNestedTabId();
+  }
+  return {};
+}
+
+std::optional<nested_tabs::NestedTabId> TabStripModel::GetInnermostNestedId(
+    int index) const {
+  tabs::TabInterface* tab = GetTabAtIndex(index);
+  if (!tab) {
+    return {};
+  }
+
+  for (const tabs::TabCollection* c = tab->GetParentCollection(); c;
+       c = c->GetParentCollection()) {
+    if (c->type() == tabs::TabCollection::Type::NESTED) {
+      return static_cast<const tabs::NestedTabCollection*>(c)->GetNestedTabId();
+    }
+  }
+  return {};
+}
+
+std::optional<nested_tabs::NestedTabId> TabStripModel::GetOwnedNestedId(
+    const tabs::TabInterface* tab) const {
+  tabs::NestedTabCollection* maybe_ntc =
+      contents_data_->GetNestedTabCollection(tab);
+  if (maybe_ntc) {
+    return maybe_ntc->GetNestedTabId();
+  }
+  return std::nullopt;
+}
+
+bool TabStripModel::AllCanNestInto(const std::set<int>& subject_idxs,
+                                   int target_idx) const {
+  if (subject_idxs.empty()) {
+    return false;
+  }
+  tabs::TabInterface* target_tab = GetTabAtIndex(target_idx);
+  if (!target_tab || target_tab->IsPinned() || target_tab->IsSplit()) {
+    return false;
+  }
+  if (std::ranges::any_of(subject_idxs, [&](auto subject_idx) {
+        tabs::TabInterface* subject_tab = GetTabAtIndex(subject_idx);
+        if (!subject_tab) {
+          // Invalid tab -> bail
+          return true;
+        }
+        return subject_tab->IsPinned();
+      })) {
+    return false;
+  }
+  if (IsDescendantOfAny(target_idx, subject_idxs)) {
+    return false;
+  }
+
+  return true;
+}
+
+void TabStripModel::MoveNestedTo(
+    const nested_tabs::NestedTabId& nested_id,
+    int to_index,
+    std::optional<tab_groups::TabGroupId> target_group) {
+  ReentrancyCheck reentrancy_check(&reentrancy_guard_);
+  MoveNestedToImpl(nested_id, to_index, target_group);
+}
+
+void TabStripModel::MoveNestedToImpl(
+    const nested_tabs::NestedTabId& nested_id,
+    int to_index,
+    std::optional<tab_groups::TabGroupId> target_group) {
+  auto* ntc = contents_data_->GetNestedTabCollection(nested_id);
+  CHECK(ntc);
+  const int old_index = GetIndexOfTab(ntc->data()->owner_tab().Get());
+  // Owner lives inside its NESTED as children[0], so GetTabsRecursive()
+  // already includes it and returns sorted recursive-index order.
+  std::vector<int> tab_indices = ntc->GetTabsRecursive() |
+                                 std::views::transform([&](const auto& tab) {
+                                   return GetIndexOfTab(tab);
+                                 }) |
+                                 std::ranges::to<std::vector<int>>();
+
+  const int sz_moving = static_cast<int>(tab_indices.size());
+  // Drag-handler callers subtract `tab_drag_data_`'s size from a pre-removal
+  // target, but for NESTED drags `tab_drag_data_` holds only the owner slot.
+  // Clamp so the FindMovePositionRecursive walk can actually match the
+  // destination instead of overshooting and returning nullopt.
+  to_index = std::clamp(to_index, 0, count() - sz_moving);
+
+  // Self-overlap is a no-op: moving the NESTED into its own range doesn't
+  // change anything, and `FindMovePositionRecursive` can't resolve a target
+  // that's currently occupied by the moving collection itself.
+  if (to_index >= old_index && to_index < old_index + sz_moving) {
+    return;
+  }
+
+  // Cycle guard: dest must not be inside the moving NESTED's subtree.
+  if (IsDescendantOfAny(to_index, {old_index})) {
+    return;
+  }
+
+  // Walk out of any enclosing NESTED that is the moving NESTED itself, owns
+  // the destination slot (would displace children[0]), conflicts with a
+  // caller-specified target group, or doesn't actually contain the
+  // post-removal destination. `to_index_pre` shifts past the contiguous
+  // moving range so it can index the pre-removal model.
+  int to_index_pre = to_index;
+  if (to_index_pre >= old_index) {
+    to_index_pre += sz_moving;
+  }
+  std::optional<nested_tabs::NestedTabId> enclosing =
+      (to_index_pre >= 0 && to_index_pre < count())
+          ? GetInnermostNestedId(to_index_pre)
+          : std::nullopt;
+  while (enclosing) {
+    auto* enc_ntc = contents_data_->GetNestedTabCollection(*enclosing);
+    if (!enc_ntc) {
+      enclosing = std::nullopt;
+      break;
+    }
+    bool inside_moving = false;
+    for (const tabs::TabCollection* c = enc_ntc; c;
+         c = c->GetParentCollection()) {
+      if (c == static_cast<tabs::TabCollection*>(ntc)) {
+        inside_moving = true;
+        break;
+      }
+    }
+    // Boundary: landing exactly on the enclosing owner's slot would force
+    // the moved NESTED into children[0]'s position, breaking the invariant.
+    const bool boundary =
+        !inside_moving &&
+        to_index_pre == GetIndexOfTab(enc_ntc->data()->owner_tab().Get());
+    // If caller specified a target group, the enclosing NESTED must lie
+    // inside it; otherwise dst_collection would resolve to a NESTED that
+    // overrides target_group.
+    bool outside_target_group = false;
+    if (target_group.has_value()) {
+      outside_target_group = true;
+      for (const tabs::TabCollection* c = enc_ntc; c;
+           c = c->GetParentCollection()) {
+        if (c->type() != tabs::TabCollection::Type::GROUP) {
+          continue;
+        }
+        const auto* group_coll =
+            static_cast<const tabs::TabGroupTabCollection*>(c);
+        if (group_coll->GetTabGroup() &&
+            group_coll->GetTabGroup()->id() == *target_group) {
+          outside_target_group = false;
+          break;
+        }
+      }
+    }
+    // Dest must land inside enclosing's post-removal range, else the walk
+    // overshoots `destination_index` while still in enclosing's subtree.
+    gfx::Range enc_pre_range = enc_ntc->data()->GetIndexRange();
+    int sz_before_enc = 0;
+    int sz_inside_enc = 0;
+    for (int idx : tab_indices) {
+      if (idx < static_cast<int>(enc_pre_range.GetMin())) {
+        ++sz_before_enc;
+      } else if (idx < static_cast<int>(enc_pre_range.GetMax())) {
+        ++sz_inside_enc;
+      }
+    }
+    const int enc_post_start =
+        static_cast<int>(enc_pre_range.GetMin()) - sz_before_enc;
+    const int enc_post_end = static_cast<int>(enc_pre_range.GetMax()) -
+                             sz_before_enc - sz_inside_enc;
+    const bool dest_outside_enc =
+        (to_index < enc_post_start || to_index >= enc_post_end);
+    if (!inside_moving && !boundary && !outside_target_group &&
+        !dest_outside_enc) {
+      break;  // safe enclosing
+    }
+    tabs::TabCollection* parent = enc_ntc->GetParentCollection();
+    if (!parent || parent->type() != tabs::TabCollection::Type::NESTED) {
+      // Walking out into a GROUP means the move should land inside that
+      // group as a sibling of the enclosing NESTED; propagate it so dst
+      // picks the group instead of falling back to unpinned.
+      if (parent && parent->type() == tabs::TabCollection::Type::GROUP) {
+        const auto* gc =
+            static_cast<const tabs::TabGroupTabCollection*>(parent);
+        if (gc->GetTabGroup() && !target_group.has_value()) {
+          target_group = gc->GetTabGroup()->id();
+        }
+      }
+      enclosing = std::nullopt;
+      break;
+    }
+    enclosing =
+        static_cast<tabs::NestedTabCollection*>(parent)->GetNestedTabId();
+  }
+
+  // Promote target_group if the destination tab lives inside a GROUP and
+  // none was explicitly chosen; otherwise dst falls back to unpinned and
+  // the walk overshoots while still inside the group.
+  if (!enclosing.has_value() && !target_group.has_value() &&
+      to_index_pre >= 0 && to_index_pre < count()) {
+    tabs::TabInterface* dest_tab = GetTabAtIndex(to_index_pre);
+    for (const tabs::TabCollection* c =
+             dest_tab ? dest_tab->GetParentCollection() : nullptr;
+         c; c = c->GetParentCollection()) {
+      if (c->type() != tabs::TabCollection::Type::GROUP) {
+        continue;
+      }
+      const auto* gc = static_cast<const tabs::TabGroupTabCollection*>(c);
+      if (gc->GetTabGroup()) {
+        target_group = gc->GetTabGroup()->id();
+      }
+      break;
+    }
+  }
+
+  static constexpr tabs::TabCollection::TypeEnumSet kRetainCollectionTypes = {
+      tabs::TabCollection::Type::SPLIT, tabs::TabCollection::Type::NESTED};
+
+  // Clamp `to_index` against whatever dst_collection resolves to (enclosing
+  // NESTED or target group), so the walk can match inside dst instead of
+  // overshooting.
+  if (enclosing.has_value()) {
+    const auto* enc_coll = contents_data_->GetNestedTabCollection(*enclosing);
+    if (enc_coll) {
+      gfx::Range enc_pre = enc_coll->data()->GetIndexRange();
+      int sz_inside = 0;
+      for (int idx : tab_indices) {
+        if (idx >= static_cast<int>(enc_pre.GetMin()) &&
+            idx < static_cast<int>(enc_pre.GetMax())) {
+          ++sz_inside;
+        }
+      }
+      const int min_dest = static_cast<int>(enc_pre.GetMin()) + 1;
+      const int max_dest = static_cast<int>(enc_pre.GetMax()) - sz_inside;
+      if (max_dest >= min_dest) {
+        to_index = std::clamp(to_index, min_dest, max_dest);
+      }
+    }
+  } else if (target_group.has_value()) {
+    const tabs::TabGroupTabCollection* group_coll =
+        contents_data_->GetTabGroupCollection(*target_group);
+    if (group_coll) {
+      gfx::Range group_pre = group_coll->GetTabGroup()->ListTabs();
+      int sz_inside = 0;
+      for (int idx : tab_indices) {
+        if (idx >= static_cast<int>(group_pre.GetMin()) &&
+            idx < static_cast<int>(group_pre.GetMax())) {
+          ++sz_inside;
+        }
+      }
+      const int min_dest = static_cast<int>(group_pre.GetMin());
+      const int max_dest = static_cast<int>(group_pre.GetMax()) - sz_inside;
+      if (max_dest >= min_dest) {
+        to_index = std::clamp(to_index, min_dest, max_dest);
+      }
+    }
+  }
+
+  MoveTabsWithNotifications(
+      tab_indices, to_index,
+      base::BindOnce(&tabs::TabStripCollection::MoveTabsRecursive,
+                     base::Unretained(contents_data_.get()), tab_indices,
+                     to_index, target_group, enclosing, false,
+                     kRetainCollectionTypes));
+
+  NestedTabChange change(this, nested_id,
+                         NestedTabChange::MovedChange(old_index, to_index));
+  for (auto& observer : observers_) {
+    observer.OnNestedTabChanged(change);
+  }
+}
+
 // Returns the ID of the group that is focused. If no group is focused,
 // returns nullopt.
 std::optional<tab_groups::TabGroupId> TabStripModel::GetFocusedGroup() const {
@@ -2434,6 +3360,49 @@ void TabStripModel::NotifySplitTabAttach
       *GetSplitData(split_id)->visual_data());
 }
 
+void TabStripModel::NotifyNestedDetached(
+    tabs::NestedTabCollection* nested_collection) {
+  // Build orphaned_children = all non-owner tabs in DFS order so listeners
+  // see the full set that left the tabstrip.
+  const tabs::TabHandle owner_handle =
+      nested_collection->data()->owner_tab();
+  std::vector<tabs::TabHandle> orphaned_children;
+  for (tabs::TabInterface* tab : nested_collection->GetTabsRecursive()) {
+    if (tab->GetHandle() == owner_handle) {
+      continue;
+    }
+    orphaned_children.push_back(tab->GetHandle());
+  }
+
+  NestedTabChange change(this, nested_collection->GetNestedTabId(),
+                         NestedTabChange::DestroyedChange(
+                             owner_handle, std::move(orphaned_children)));
+  for (auto& observer : observers_) {
+    observer.OnNestedTabChanged(change);
+  }
+}
+
+void TabStripModel::NotifyNestedAttached(
+    tabs::NestedTabCollection* nested_collection) {
+  const tabs::TabHandle owner_handle =
+      nested_collection->data()->owner_tab();
+  std::vector<tabs::TabHandle> initial_children;
+  for (tabs::TabInterface* tab : nested_collection->GetTabsRecursive()) {
+    if (tab->GetHandle() == owner_handle) {
+      continue;
+    }
+    initial_children.push_back(tab->GetHandle());
+  }
+
+  NestedTabChange change(this, nested_collection->GetNestedTabId(),
+                         NestedTabChange::CreatedChange(
+                             owner_handle, std::move(initial_children),
+                             *nested_collection->data()->visual_data()));
+  for (auto& observer : observers_) {
+    observer.OnNestedTabChanged(change);
+  }
+}
+
 TabStripModel::TabIterator TabStripModel::begin() const {
   return contents_data_->begin();
 }
@@ -2546,8 +3515,14 @@ bool TabStripModel::IsContextMenuCommand
 
     case CommandAddToSplit:
     case CommandSwapWithActiveSplit:
-    case CommandArrangeSplit:
-      return true;
+    case CommandArrangeSplit: {
+      const std::vector<int> indices = GetIndicesForCommand(context_index);
+      return !std::ranges::any_of(indices, [&](auto idx) {
+        tabs::TabInterface* tab = GetTabAtIndex(idx);
+        CHECK(tab);
+        return contents_data_->GetNestedTabCollection(tab);
+      });
+    }
 
     case CommandRemoveFromGroup:
       return SupportsTabGroups();
@@ -3249,16 +4224,38 @@ TabStripModel::GetGroupsDestroyedFromRem
   return destroyed_groups;
 }
 
+std::vector<nested_tabs::NestedTabId>
+TabStripModel::GetNestedsDestroyedFromRemovingIndices(
+    const std::vector<int>& indices) const {
+  const std::vector<tabs::TabInterface*> tabs = GetTabsAtIndices(indices);
+  const absl::flat_hash_set<const tabs::TabInterface*> tabs_set(tabs.begin(),
+                                                                tabs.end());
+
+  std::vector<nested_tabs::NestedTabId> destroyed;
+  for (const nested_tabs::NestedTabId& nested_id : ListNesteds()) {
+    const nested_tabs::NestedTabData* const data = GetNestingData(nested_id);
+    if (!data) {
+      continue;
+    }
+    const tabs::TabInterface* const owner = data->owner_tab().Get();
+    if (owner && tabs_set.contains(owner)) {
+      destroyed.push_back(nested_id);
+    }
+  }
+  return destroyed;
+}
+
 void TabStripModel::ExecuteCloseTabs(
     base::RepeatingCallback<std::vector<tabs::TabInterface*>()>
         get_tabs_to_close,
     uint32_t close_types) {
   const std::vector<tabs::TabInterface*> tabs_to_close =
       std::move(get_tabs_to_close).Run();
-  if (base::CommandLine::ForCurrentProcess()->
-          GetSwitchValueASCII("close-window-with-last-tab") == "never" &&
-          static_cast<int>(tabs_to_close.size()) == count())
-      delegate()->AddTabAt(GURL(), -1, true);
+  if (base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
+          "close-window-with-last-tab") == "never" &&
+      static_cast<int>(tabs_to_close.size()) == count()) {
+    delegate()->AddTabAt(GURL(), -1, true);
+  }
   ReentrancyCheck reentrancy_check(&reentrancy_guard_);
   std::vector<content::WebContents*> web_contents_to_close;
   for (tabs::TabInterface* t : tabs_to_close) {
@@ -3558,10 +4555,10 @@ std::vector<int> TabStripModel::GetIndic
                                            .rbegin()
                                     : index;
   } else if (id == CommandCloseTabsToLeft) {
-    first_unclosed_tab =
-        is_selected
-            ? *selection_model_.GetListSelectionModel().selected_indices().begin()
-            : index;
+    first_unclosed_tab = is_selected ? *selection_model_.GetListSelectionModel()
+                                            .selected_indices()
+                                            .begin()
+                                     : index;
   }
 
   // If the tab that the context menu command is invoked on is not selected and
@@ -3768,8 +4765,129 @@ tabs::TabInterface* TabStripModel::GetTa
 
 void TabStripModel::CloseTabs(base::span<content::WebContents* const> items,
                               uint32_t close_types) {
+  // Pass 1a: scan `items` and collect every owner -> descendants relationship.
+  // Do NOT mutate the tab strip in this pass, synchronous recursion during
+  // cascade processing would destroy WebContents whose pointers still live
+  // later in `items`.
+  struct CascadeInfo {
+    content::WebContents* parent;
+    std::vector<content::WebContents*> descendants;
+  };
+  std::vector<CascadeInfo> all_cascades;
+  std::set<content::WebContents*> all_descendants;
+
+  for (auto* wc : items) {
+    auto* tab = GetTabForWebContents(wc);
+    if (!tab) {
+      continue;
+    }
+    auto* owned = contents_data_->GetNestedTabCollection(tab);
+    if (!owned) {
+      continue;
+    }
+    std::vector<content::WebContents*> descendant_contents;
+    for (auto* d : owned->GetTabsRecursive()) {
+      // Owner is INSIDE its NESTED as the first child; skip it here so we
+      // don't double-count the cascade root in its own descendants list.
+      if (d == tab) {
+        continue;
+      }
+      auto* dwc = d->GetContents();
+      descendant_contents.push_back(dwc);
+      all_descendants.insert(dwc);
+    }
+    if (descendant_contents.empty()) {
+      continue;
+    }
+    all_cascades.push_back({wc, std::move(descendant_contents)});
+  }
+
+  // Pass 1b: keep only TOP-LEVEL cascades, those whose root is not itself a
+  // descendant of another cascade in this batch. Multi-layer nesting (A owns
+  // B, B owns C) would otherwise register cascades for both A and B at this
+  // level; A's recursive CloseTabs already handles B's subtree, so a
+  // standalone cascade for B would (a) be redundant and (b) iterate over
+  // freed WebContents pointers once A's cascade has run to completion.
+  std::vector<CascadeInfo> top_cascades;
+  std::set<content::WebContents*> cascade_roots;
+  std::set<content::WebContents*> cascade_managed;
+  for (auto& info : all_cascades) {
+    if (all_descendants.count(info.parent)) {
+      continue;
+    }
+    cascade_roots.insert(info.parent);
+    for (auto* d : info.descendants) {
+      cascade_managed.insert(d);
+    }
+    top_cascades.push_back(std::move(info));
+  }
+
+  // Build the normal-close set: items minus cascade roots (held back until
+  // their descendants finish) and minus descendants of those roots (their
+  // owners will recurse for them).
+  std::vector<content::WebContents*> normal_items;
+  normal_items.reserve(items.size());
+  for (auto* wc : items) {
+    if (cascade_roots.count(wc) || cascade_managed.count(wc)) {
+      continue;
+    }
+    normal_items.push_back(wc);
+  }
+
+  // Pass 2: register the top-level pending cascades and kick off recursive
+  // closes for their descendants. Sub-cascades (e.g. cascade for B inside
+  // A's subtree) get registered naturally when the recursive call's own
+  // Pass 1 encounters them.
+  for (auto& info : top_cascades) {
+    auto pending = std::make_unique<PendingCascade>();
+    pending->parent = info.parent;
+    pending->close_types = close_types;
+    pending->parent_session_id =
+        sessions::SessionTabHelper::IdForTab(info.parent);
+    for (auto* d : info.descendants) {
+      pending->descendants.emplace(d, std::make_unique<CascadeCloseObserver>(
+                                          d, weak_factory_.GetWeakPtr()));
+    }
+
+    // Fire historical-subtree snapshot before descendants close. Skip if
+    // this parent is already a descendant of an outer pending cascade.
+    // That outer cascade has already snapshotted the whole subtree
+    // including this parent. Otherwise we'd emit duplicate historical
+    // entries (one per nested-of-nested level), each requiring its own
+    // cmd-shift-t press to restore.
+    bool already_covered_by_outer = false;
+    for (const auto& outer : pending_cascades_) {
+      if (outer->descendants.contains(info.parent)) {
+        already_covered_by_outer = true;
+        break;
+      }
+    }
+    if (!already_covered_by_outer &&
+        (close_types & TabCloseTypes::CLOSE_CREATE_HISTORICAL_TAB)) {
+      int parent_idx = GetIndexOfWebContents(info.parent);
+      if (parent_idx != kNoTab) {
+        delegate_->WillCloseNestedSubtree(parent_idx);
+        // Observe the owner WebContents so we can fire NestedSubtreeClosed
+        // when it actually destructs (handles deferred unload-listener
+        // close). Stored on the model, not in PendingCascade, so the
+        // observer outlives the cascade's lifecycle.
+        nested_subtree_owner_observers_[info.parent] =
+            std::make_unique<NestedSubtreeOwnerObserver>(
+                info.parent, weak_factory_.GetWeakPtr(),
+                pending->parent_session_id);
+      }
+    }
+
+    pending_cascades_.push_back(std::move(pending));
+    CloseTabs(info.descendants, close_types);
+  }
+
+  if (normal_items.empty()) {
+    return;
+  }
+
   std::vector<content::WebContents*> filtered_items;
-  for (content::WebContents* contents : items) {
+  for (content::WebContents* contents : normal_items) {
     if (IsTabClosable(contents)) {
       filtered_items.push_back(contents);
     } else {
@@ -4130,13 +5248,21 @@ void TabStripModel::MoveTabRelative(TabR
   tabs::TabInterface* active_tab = GetTabAtIndex(active_tab_index);
 
   // The range of indices being moved. This will either be the active tab, or
-  // all the tabs in the same split as the active tab. These are guaranteed to
-  // be all have the same pinned state and group membership.
+  // all the tabs in the same split/nested as the active tab. These are
+  // guaranteed to be all have the same pinned state and group membership.
   // TODO: this needs to be updated for multi-selection.
-  const gfx::Range moving_index_range =
-      active_tab->IsSplit()
-          ? GetIndexRangeOfSplit(active_tab->GetSplit().value())
-          : gfx::Range{active_tab_index, active_tab_index + 1};
+  gfx::Range moving_index_range;
+  if (active_tab->IsSplit()) {
+    moving_index_range = GetIndexRangeOfSplit(active_tab->GetSplit().value());
+  } else if (auto* nested =
+                 contents_data_->GetNestedTabCollection(active_tab)) {
+    // Owner now lives INSIDE its NESTED as the first child;
+    // TabCountRecursive already includes it.
+    moving_index_range = gfx::Range{
+        active_tab_index, active_tab_index + nested->TabCountRecursive()};
+  } else {
+    moving_index_range = gfx::Range{active_tab_index, active_tab_index + 1};
+  }
 
   // Calculate the target index the tabs needs to moved to. This will be the
   // destination index of the current tab at moving_index_range.start().
@@ -4150,6 +5276,53 @@ void TabStripModel::MoveTabRelative(TabR
     if (tabs::TabInterface* neighbor = GetTabAtIndex(neighbor_index);
         neighbor->IsSplit()) {
       offset = GetIndexRangeOfSplit(neighbor->GetSplit().value()).length();
+    } else if (auto* neighbor_nested =
+                   contents_data_->GetNestedTabCollection(neighbor)) {
+      // Neighbor OWNS a NESTED. Two sub-cases:
+      // 1. Moving range is inside that NESTED → "neighbor owns me." Step out
+      //    to before neighbor's NESTED in its parent. target = neighbor_flat;
+      //    the walk-out in MoveNestedToImpl turns this into a sibling move
+      //    in the parent.
+      // 2. Moving range is outside → step over the whole NESTED unit.
+      bool moving_inside = false;
+      for (const tabs::TabCollection* a = active_tab->GetParentCollection(); a;
+           a = a->GetParentCollection()) {
+        if (a == neighbor_nested) {
+          moving_inside = true;
+          break;
+        }
+      }
+      if (moving_inside) {
+        offset = moving_index_range.start() - neighbor_index;
+      } else {
+        // Owner now lives INSIDE its NESTED as the first child, so
+        // TabCountRecursive already includes the owner.
+        offset = neighbor_nested->TabCountRecursive();
+      }
+    } else if (neighbor->GetParentCollection()->type() ==
+               tabs::TabCollection::Type::NESTED) {
+      // Neighbor is INSIDE a NESTED. Two sub-cases:
+      // 1. Moving range is inside the SAME NESTED → swap with neighbor
+      //    within the NESTED. Default offset=1.
+      // 2. Moving range is outside → step past the whole NESTED. Target =
+      //    NESTED's owner flat (its first tab in flat); the walk-out in
+      //    MoveNestedToImpl turns this into "land in NESTED's parent."
+      auto* parent_nested = static_cast<const tabs::NestedTabCollection*>(
+          neighbor->GetParentCollection());
+      bool moving_inside = false;
+      for (const tabs::TabCollection* a = active_tab->GetParentCollection(); a;
+           a = a->GetParentCollection()) {
+        if (a == parent_nested) {
+          moving_inside = true;
+          break;
+        }
+      }
+      if (!moving_inside) {
+        tabs::TabInterface* owner = parent_nested->data()->owner_tab().Get();
+        const int owner_idx = GetIndexOfTab(owner);
+        offset = moving_index_range.start() - owner_idx;
+      }
+      // else: keep default offset=1 (swap within NESTED).
     }
     target_index +=
         (direction == TabRelativeDirection::kNext ? 1 : -1) * offset;
@@ -4187,6 +5360,16 @@ void TabStripModel::MoveTabRelative(TabR
       }
     }
   }
+  // If the active tab owns a NESTED, the moving unit is the whole NESTED.
+  // Route through MoveNestedTo, which knows how to land the unit at a
+  // position that may fall inside another NESTED (the generic
+  // MoveTabsToIndexImpl path resolves dst to unpinned/pinned only, which
+  // breaks moves within an outer NESTED).
+  if (auto nested_id = GetOwnedNestedId(active_tab)) {
+    MoveNestedToImpl(*nested_id, target_index, target_group);
+    return;
+  }
+
   MoveTabsToIndexImpl(moving_index_range.ToIntVector(), target_index,
                       target_group);
 }
@@ -4307,6 +5490,9 @@ split_tabs::SplitTabId TabStripModel::Ad
     int pivot_index,
     split_tabs::SplitTabVisualData visual_data,
     SplitTabChange::SplitTabAddReason reason) {
+  static constexpr tabs::TabCollection::TypeEnumSet kRetainCollectionTypes = {
+      tabs::TabCollection::Type::SPLIT};
+
   std::vector<tabs::TabInterface*> tabs = {};
   for (int i : indices) {
     tabs::TabInterface* tab = GetTabAtIndex(i);
@@ -4314,10 +5500,18 @@ split_tabs::SplitTabId TabStripModel::Ad
     tabs.push_back(tab);
   }
 
+  int destination_index =
+      pivot_index - CountEffectiveMovedTabsLeftOfDestination(
+                        indices, pivot_index, kRetainCollectionTypes);
+
   // Add the tabs to a split with the active index.
-  MoveTabsAndSetPropertiesImpl(indices, pivot_index,
-                               GetTabGroupForTab(pivot_index),
-                               IsTabPinned(pivot_index));
+  MoveTabsWithNotifications(
+      indices, destination_index,
+      base::BindOnce(&tabs::TabStripCollection::MoveTabsRecursive,
+                     base::Unretained(contents_data_.get()), indices,
+                     destination_index, GetTabGroupForTab(pivot_index),
+                     GetEnclosingNestedId(pivot_index),
+                     IsTabPinned(pivot_index), kRetainCollectionTypes));
 
   contents_data_->CreateSplit(split_id, tabs, visual_data);
   selection_model_.InvalidateListSelectionModel(base::PassKey<TabStripModel>());
@@ -4538,6 +5732,16 @@ void TabStripModel::AddToNewGroupImpl(
       continue;
     }
 
+    // Grouping within nested tabs is never valid.
+    {
+      tabs::TabInterface* tab = GetTabAtIndex(i);
+      // Placing on a child
+      if (tab->GetParentCollection()->type() ==
+          tabs::TabCollection::Type::NESTED) {
+        continue;
+      }
+    }
+
     // Otherwise, grouping is valid if the destination is not in the middle of a
     // different group.
     std::optional<tab_groups::TabGroupId> destination_group =
@@ -4616,6 +5820,64 @@ void TabStripModel::AddToExistingGroupIm
   }
 }
 
+size_t TabStripModel::CountEffectiveMovedTabsLeftOfDestination(
+    const std::vector<int>& indices,
+    int destination_index,
+    const tabs::TabCollection::TypeEnumSet& retain) const {
+  absl::flat_hash_set<int> indices_set(indices.begin(), indices.end());
+  size_t num_effective_moved_to_left_of_destination = 0;
+
+  for (int i : indices) {
+    if (i >= destination_index) {
+      break;
+    }
+
+    tabs::TabInterface* tab = GetTabAtIndex(i);
+
+    // If this tab is inside a NESTED whose owner is also in `indices` and
+    // NESTED retain is on, it'll be moved via the NESTED, not as an individual
+    // tab. Don't count it here. Note: an owner tab IS structurally inside its
+    // own NESTED, but it isn't "covered" by that NESTED, it's the seed. Only
+    // count coverage by some OTHER NESTED's owner.
+    if (retain.Has(tabs::TabCollection::Type::NESTED)) {
+      bool covered_by_owner = false;
+      for (const tabs::TabCollection* c = tab->GetParentCollection(); c;
+           c = c->GetParentCollection()) {
+        if (c->type() != tabs::TabCollection::Type::NESTED) {
+          continue;
+        }
+        auto* nested = static_cast<const tabs::NestedTabCollection*>(c);
+        tabs::TabInterface* nested_owner = nested->data()->owner_tab().Get();
+        if (nested_owner == tab) {
+          // `tab` owns this NESTED. Skip past, look for an outer one.
+          continue;
+        }
+        const int owner_idx = GetIndexOfTab(nested_owner);
+        if (indices_set.contains(owner_idx)) {
+          covered_by_owner = true;
+          break;
+        }
+      }
+      if (covered_by_owner) {
+        continue;
+      }
+    }
+
+    num_effective_moved_to_left_of_destination++;
+
+    // If this tab owns a NESTED and NESTED retain is on, the NESTED's children
+    // are part of the move set even though they're not in `indices`. The owner
+    // itself is already counted above, so only add the non-owner descendants.
+    if (retain.Has(tabs::TabCollection::Type::NESTED)) {
+      if (auto* nested = contents_data_->GetNestedTabCollection(tab)) {
+        num_effective_moved_to_left_of_destination +=
+            nested->TabCountRecursive() - 1;
+      }
+    }
+  }
+  return num_effective_moved_to_left_of_destination;
+}
+
 void TabStripModel::MoveTabsAndSetPropertiesImpl(
     const std::vector<int>& indices,
     int destination_index,
@@ -4626,24 +5888,19 @@ void TabStripModel::MoveTabsAndSetProper
   }
 
   static constexpr tabs::TabCollection::TypeEnumSet kRetainCollectionTypes = {
-      tabs::TabCollection::Type::SPLIT};
+      tabs::TabCollection::Type::SPLIT, tabs::TabCollection::Type::NESTED};
   // TabStripCollection::MoveTabsRecursive moves tabs to the destination index
   // after the tabs are removed, so adjust `destination_index` by subtracting
   // the number of tabs to the left of it.
-  size_t num_tabs_to_left_of_destination = 0;
-  for (auto i : indices) {
-    if (i >= destination_index) {
-      break;
-    }
-    num_tabs_to_left_of_destination++;
-  }
-  destination_index -= num_tabs_to_left_of_destination;
+  destination_index -= CountEffectiveMovedTabsLeftOfDestination(
+      indices, destination_index, kRetainCollectionTypes);
 
   MoveTabsWithNotifications(
       indices, destination_index,
       base::BindOnce(&tabs::TabStripCollection::MoveTabsRecursive,
                      base::Unretained(contents_data_.get()), indices,
-                     destination_index, group, pinned, kRetainCollectionTypes));
+                     destination_index, group, std::nullopt, pinned,
+                     kRetainCollectionTypes));
 }
 
 void TabStripModel::AddToReadLaterImpl(const std::vector<int>& indices) {
@@ -4702,6 +5959,16 @@ std::unique_ptr<tabs::TabModel> TabStrip
     int index,
     tabs::TabInterface::DetachReason tab_detach_reason) {
   tabs::TabModel* const tab_to_remove = GetTabModelAtIndex(index);
+
+  // Bookkeeping for nested destruction observer
+  std::optional<tabs::TabStripCollection::NestedDestruction> dying_nested;
+  if (auto* parent = tab_to_remove->GetParentCollection();
+      parent && parent->type() == tabs::TabCollection::Type::NESTED &&
+      parent->TabCountRecursive() == 1) {
+    auto* nested = static_cast<const tabs::NestedTabCollection*>(parent);
+    dying_nested = {nested->data()->id(), nested->data()->owner_tab()};
+  }
+
   const std::optional<tab_groups::TabGroupId> old_group =
       tab_to_remove->GetGroup();
   std::optional<int> next_selected_index =
@@ -4761,6 +6028,14 @@ std::unique_ptr<tabs::TabModel> TabStrip
     TabGroupStateChanged(index, tab_to_remove, old_group, std::nullopt);
   }
 
+  if (dying_nested) {
+    observers_.Notify(&TabStripModelObserver::OnNestedTabChanged,
+                      NestedTabChange(this, dying_nested->old_id,
+                                      NestedTabChange::DestroyedChange(
+                                          dying_nested->old_owner,
+                                          {})));
+  }
+
   return old_data;
 }
 
@@ -4844,13 +6119,15 @@ void TabStripModel::MoveTabToIndexImpl(
 void TabStripModel::MoveTabsToIndexImpl(
     const std::vector<int>& tab_indices,
     int destination_index,
-    const std::optional<tab_groups::TabGroupId> group) {
+    const std::optional<tab_groups::TabGroupId> group,
+    std::optional<nested_tabs::NestedTabId> enclosing_nested) {
   if (tab_indices.empty()) {
     return;
   }
 
   static constexpr tabs::TabCollection::TypeEnumSet kRetainCollectionTypes = {
-      tabs::TabCollection::Type::SPLIT, tabs::TabCollection::Type::GROUP};
+      tabs::TabCollection::Type::SPLIT, tabs::TabCollection::Type::GROUP,
+      tabs::TabCollection::Type::NESTED};
 
   const int pinned_tab_count = IndexOfFirstNonPinnedTab();
   const bool pin = IsTabPinned(tab_indices[0]);
@@ -4864,12 +6141,116 @@ void TabStripModel::MoveTabsToIndexImpl(
   CHECK(all_tabs_pinned || all_tabs_unpinned);
   CHECK(std::ranges::is_sorted(tab_indices));
 
+  // Clamp `destination_index` into the enclosing NESTED's post-removal
+  // range. Position 0 in a NESTED is the owner's slot, so the lower bound
+  // bumps past it; tabs moved from before/inside shift the range.
+  auto clamp_destination_for_nested = [&](const nested_tabs::NestedTabId& id) {
+    const auto* coll = contents_data_->GetNestedTabCollection(id);
+    if (!coll) {
+      return;
+    }
+    const gfx::Range pre_range = coll->data()->GetIndexRange();
+    int sz_before = 0;
+    int sz_inside = 0;
+    for (int idx : tab_indices) {
+      if (idx < static_cast<int>(pre_range.GetMin())) {
+        ++sz_before;
+      } else if (idx < static_cast<int>(pre_range.GetMax())) {
+        ++sz_inside;
+      }
+    }
+    const int min_dest = static_cast<int>(pre_range.GetMin()) - sz_before + 1;
+    const int max_dest =
+        static_cast<int>(pre_range.GetMax()) - sz_before - sz_inside;
+    if (max_dest < min_dest) {
+      return;
+    }
+    destination_index = std::clamp(destination_index, min_dest, max_dest);
+  };
+  if (enclosing_nested.has_value()) {
+    clamp_destination_for_nested(*enclosing_nested);
+  }
+
+  // Auto-detect enclosing NESTED if all moving tabs share one as an
+  // ancestor and `destination_index` lands inside its post-removal range;
+  // pick the deepest such NESTED so the walk doesn't overshoot.
+  if (!enclosing_nested.has_value() && !tab_indices.empty()) {
+    tabs::TabInterface* first_tab = GetTabAtIndex(tab_indices.front());
+    if (first_tab) {
+      for (const tabs::TabCollection* c = first_tab->GetParentCollection(); c;
+           c = c->GetParentCollection()) {
+        if (c->type() != tabs::TabCollection::Type::NESTED) {
+          continue;
+        }
+        const auto* nested = static_cast<const tabs::NestedTabCollection*>(c);
+        bool all_inside = std::ranges::all_of(tab_indices, [&](int idx) {
+          tabs::TabInterface* t = GetTabAtIndex(idx);
+          for (const tabs::TabCollection* p = t ? t->GetParentCollection()
+                                                : nullptr;
+               p; p = p->GetParentCollection()) {
+            if (p == c) {
+              return true;
+            }
+          }
+          return false;
+        });
+        if (!all_inside) {
+          continue;
+        }
+        gfx::Range pre_range = nested->data()->GetIndexRange();
+        const int sz_inside = static_cast<int>(tab_indices.size());
+        const int post_start = static_cast<int>(pre_range.GetMin()) + 1;
+        const int post_end = static_cast<int>(pre_range.GetMax()) - sz_inside;
+        if (destination_index >= post_start && destination_index <= post_end) {
+          enclosing_nested = nested->GetNestedTabId();
+          break;
+        }
+      }
+    }
+  }
+
+  // Promote target_group if the destination tab lives inside a GROUP and
+  // none was supplied; `destination_index` is post-removal, so translate to
+  // a pre-removal model index by skipping moving tabs.
+  std::optional<tab_groups::TabGroupId> effective_group = group;
+  if (!enclosing_nested.has_value() && !effective_group.has_value()) {
+    const std::set<int> moving(tab_indices.begin(), tab_indices.end());
+    const int total = count();
+    int probe = 0;
+    int post = 0;
+    while (probe < total && post < destination_index) {
+      if (!moving.contains(probe)) {
+        ++post;
+      }
+      ++probe;
+    }
+    while (probe < total && moving.contains(probe)) {
+      ++probe;
+    }
+    if (probe < total) {
+      tabs::TabInterface* dest_tab = GetTabAtIndex(probe);
+      for (const tabs::TabCollection* c =
+               dest_tab ? dest_tab->GetParentCollection() : nullptr;
+           c; c = c->GetParentCollection()) {
+        if (c->type() != tabs::TabCollection::Type::GROUP) {
+          continue;
+        }
+        const auto* gc = static_cast<const tabs::TabGroupTabCollection*>(c);
+        if (gc->GetTabGroup()) {
+          effective_group = gc->GetTabGroup()->id();
+        }
+        break;
+      }
+    }
+  }
+
   // Update `contents_data`.
   MoveTabsWithNotifications(
       tab_indices, destination_index,
       base::BindOnce(&tabs::TabStripCollection::MoveTabsRecursive,
                      base::Unretained(contents_data_.get()), tab_indices,
-                     destination_index, group, pin, kRetainCollectionTypes));
+                     destination_index, effective_group, enclosing_nested, pin,
+                     kRetainCollectionTypes));
 }
 
 void TabStripModel::TabGroupStateChanged(
@@ -5115,13 +6496,31 @@ std::vector<std::pair<int, int>> TabStri
 std::vector<TabStripModel::MoveNotification>
 TabStripModel::PrepareTabsToMoveToIndex(const std::vector<int>& tab_indices,
                                         int destination_index) {
+  std::vector<int> expanded;
+  expanded.reserve(tab_indices.size());
+  absl::flat_hash_set<int> seen;
+  for (int idx : tab_indices) {
+    if (seen.insert(idx).second) {
+      expanded.push_back(idx);
+    }
+    tabs::TabInterface* tab = GetTabAtIndex(idx);
+    if (auto* nested = contents_data_->GetNestedTabCollection(tab)) {
+      for (tabs::TabInterface* child : *nested) {
+        int child_idx = GetIndexOfTab(child);
+        if (seen.insert(child_idx).second) {
+          expanded.push_back(child_idx);
+        }
+      }
+    }
+  }
+
   const std::vector<std::pair<int, int>> moved_indices =
-      CalculateIncrementalTabMoves(tab_indices, destination_index);
+      CalculateIncrementalTabMoves(expanded, destination_index);
   std::vector<MoveNotification> notifications;
 
   ui::ListSelectionModel old_selection_model(
       selection_model().GetListSelectionModel());
-  UpdateSelectionModelForMoves(tab_indices, destination_index);
+  UpdateSelectionModelForMoves(expanded, destination_index);
   for (std::pair<int, int> move : moved_indices) {
     if (move.first != move.second) {
       FixOpeners(move.first);
@@ -5196,6 +6595,15 @@ void TabStripModel::SetTabsPinned(std::v
 }
 
 int TabStripModel::SetTabPinnedImpl(int index, bool pinned) {
+  if (pinned) {
+    tabs::TabInterface* tab = GetTabAtIndex(index);
+    if (auto* nested = contents_data_->GetNestedTabCollection(tab)) {
+      UnnestImpl(nested->GetNestedTabId());
+      index = GetIndexOfTab(tab);  // re-resolve after possible shifting
+      CHECK(index != kNoTab);
+    }
+  }
+
   const int final_index =
       pinned ? IndexOfFirstNonPinnedTab() : IndexOfFirstNonPinnedTab() - 1;
 
@@ -5220,7 +6628,7 @@ void TabStripModel::SetSplitPinnedImpl(t
       tab_indices, destination_index,
       base::BindOnce(&tabs::TabStripCollection::MoveTabsRecursive,
                      base::Unretained(contents_data_.get()), tab_indices,
-                     destination_index, std::nullopt, pinned,
+                     destination_index, std::nullopt, std::nullopt, pinned,
                      kRetainCollectionTypes));
 }
 
--- a/chrome/browser/ui/tabs/tab_strip_model.h
+++ b/chrome/browser/ui/tabs/tab_strip_model.h
@@ -30,9 +30,12 @@
 #include "chrome/browser/ui/tabs/tab_strip_scrubbing_metrics.h"
 #include "chrome/browser/ui/tabs/tab_strip_user_gesture_details.h"
 #include "chrome/common/buildflags.h"
+#include "components/nested_tabs/nested_tab_id.h"
+#include "components/nested_tabs/nested_tab_visual_data.h"
 #include "components/sessions/core/session_id.h"
 #include "components/tab_groups/tab_group_id.h"
 #include "components/tab_groups/tab_group_visual_data.h"
+#include "components/tabs/public/nested_tab_collection.h"
 #include "components/tabs/public/tab_collection.h"
 #include "components/tabs/public/tab_interface.h"
 #include "third_party/perfetto/include/perfetto/tracing/traced_value_forward.h"
@@ -89,7 +92,8 @@ class TabGroupModelFactory {
 struct DetachedTabCollection {
   DetachedTabCollection(
       std::variant<std::unique_ptr<tabs::TabGroupTabCollection>,
-                   std::unique_ptr<tabs::SplitTabCollection>> collection,
+                   std::unique_ptr<tabs::SplitTabCollection>,
+                   std::unique_ptr<tabs::NestedTabCollection>> collection,
       std::optional<int> active_index,
       bool pinned_);
   DetachedTabCollection(const DetachedTabCollection&) = delete;
@@ -97,7 +101,8 @@ struct DetachedTabCollection {
   ~DetachedTabCollection();
   DetachedTabCollection(DetachedTabCollection&&);
   std::variant<std::unique_ptr<tabs::TabGroupTabCollection>,
-               std::unique_ptr<tabs::SplitTabCollection>>
+               std::unique_ptr<tabs::SplitTabCollection>,
+               std::unique_ptr<tabs::NestedTabCollection>>
       collection_;
   // Store the index of tab that was active in the detached group.
   std::optional<int> active_index_ = std::nullopt;
@@ -193,6 +198,8 @@ class TabStripModel {
  public:
   using TabIterator = tabs::TabCollection::TabIterator;
 
+  std::string DebugDumpCollections() const;
+
   // TODO(crbug.com/40881446): Remove this, and use std::optional<size_t> (or at
   // least std::optional<int>) in its place.
   static constexpr int kNoTab = -1;
@@ -315,6 +322,21 @@ class TabStripModel {
       bool pinned,
       std::optional<tab_groups::TabGroupId> group_id = std::nullopt);
 
+  // Removes the nested collection (owner + all children) from the collection
+  // hierarchy and passes it to the client. The client can re-insert into
+  // another tabstrip using `InsertDetachedNestedAt` without destroying the
+  // nest. Used for cross-tabstrip drags of an entire NESTED.
+  std::unique_ptr<DetachedTabCollection> DetachNestedForInsertion(
+      const nested_tabs::NestedTabId nested_id);
+
+  // Inserts a detached NESTED into the tabstrip starting at `index`. The
+  // owner sits at slot 0 of the NESTED, with its children after it. An
+  // optional `group_id` places the NESTED inside that group.
+  gfx::Range InsertDetachedNestedAt(
+      std::unique_ptr<DetachedTabCollection> nested,
+      int index,
+      std::optional<tab_groups::TabGroupId> group_id = std::nullopt);
+
   // Closes the WebContents at the specified index. This causes the
   // WebContents to be destroyed, but it may not happen immediately.
   // |close_types| is a bitmask of CloseTypes.
@@ -404,8 +426,10 @@ class TabStripModel {
   // index were 3, then the result would be [b c A f D F]. A, being pinned, can
   // move no further than index 2. The non-pinned tabs are moved to the target
   // index + selected-pinned tab-count (3 + 1).
-  void MoveSelectedTabsTo(int index,
-                          std::optional<tab_groups::TabGroupId> group);
+  void MoveSelectedTabsTo(
+      int index,
+      std::optional<tab_groups::TabGroupId> group,
+      std::optional<nested_tabs::NestedTabId> enclosing_nested = std::nullopt);
 
   // Moves all tabs in `group` to `to_index`. This has no checks to make sure
   // the position is valid for a group to move to.
@@ -696,6 +720,68 @@ class TabStripModel {
   // maintain their group and pin properties.
   void RemoveSplit(split_tabs::SplitTabId split_id);
 
+  // For a single owner_tab subtree, determine which tabs close.
+  // Adds closing tabs' WebContents to `expanded` (the close set).
+  void ResolveCascade(tabs::TabInterface* root,
+                      std::set<content::WebContents*>& expanded);
+
+  // Nests |children| to |parent| at the end of the parent's nesting,
+  // or unnests if null (or parent isn't found). Creates a collection if
+  // it doesn't already exist. Passing empty |children| is a no-op. This
+  // function does cycle checking and HARD ERRORS if one is found. Use
+  // IsAncestorOf() to check before calling this function. When |children| are
+  // moved, if they are the last ones out of a nested collection, that
+  // collection is destroyed. Returns a |NestedTabId| iff |parent| is not |null|
+  std::optional<nested_tabs::NestedTabId> Nest(
+      std::optional<int> parent,
+      std::vector<int> children,
+      std::optional<nested_tabs::NestedTabVisualData> visual_data = {},
+      std::optional<size_t> target_child_idx = {});
+
+  void RestoreNested(const nested_tabs::NestedTabId& nested_id,
+                     tabs::TabInterface* owner,
+                     const std::vector<tabs::TabInterface*>& children,
+                     const nested_tabs::NestedTabVisualData& visual_data);
+
+  // Unnests all tabs that are contained in the nested collection |nested_id|.
+  // The tabs are automatically rehomed as siblings to the owning tab.
+  void Unnest(const nested_tabs::NestedTabId& nested_id);
+
+  // Gets direct parent or nullopt if top-level
+  std::optional<int> GetParentIndex(int child_idx) const;
+  // Gets direct children
+  std::vector<int> GetChildIndices(int parent_idx) const;
+  // Returns true if |subject| is a descendant in the tree of any tab named in
+  // |maybe_ancestor_idxs|. Invalid indices (in either argument) are silently
+  // ignored. If |self_is_ancestor| is true (the default), a tab in
+  // |maybe_ancestor_idxs| is considered an ancestor of itself. Useful for
+  // cycle checks where "X under X" should be rejected as a self-cycle.
+  bool IsDescendantOfAny(int subject,
+                         const std::set<int>& maybe_ancestor_idxs,
+                         bool self_is_ancestor = true) const;
+  bool IsSubtreeCollapsed(nested_tabs::NestedTabId nested_id) const;
+  void SetSubtreeCollapsed(nested_tabs::NestedTabId nested_id,
+                           bool is_collapsed);
+  std::set<nested_tabs::NestedTabId> ListNesteds() const;
+  const nested_tabs::NestedTabData* GetNestingData(
+      nested_tabs::NestedTabId nested_id) const;
+  // Returns the deepest NESTED that contains the tab at `index` as a
+  // non-owner child. Self-owning NESTEDs are skipped. An owner is
+  // structurally in its own NESTED at slot 0, but isn't "enclosed" by it.
+  // Use this for "what nest am I a *child* of"-style questions.
+  std::optional<nested_tabs::NestedTabId> GetEnclosingNestedId(int index) const;
+  // Like GetEnclosingNestedId, but does NOT skip self-owning NESTEDs.
+  // Returns the deepest NESTED at or above the tab in the collection tree.
+  // Use this for the inclusive "innermost NESTED at or above me" semantic.
+  std::optional<nested_tabs::NestedTabId> GetInnermostNestedId(int index) const;
+  std::optional<nested_tabs::NestedTabId> GetOwnedNestedId(
+      const tabs::TabInterface* tab) const;
+  bool AllCanNestInto(const std::set<int>& subject_idxs, int target_idx) const;
+  void MoveNestedTo(
+      const nested_tabs::NestedTabId& nested_id,
+      int to_index,
+      std::optional<tab_groups::TabGroupId> target_group = std::nullopt);
+
   TabGroupModel* group_model() const { return group_model_.get(); }
 
   bool SupportsTabGroups() const { return group_model_.get() != nullptr; }
@@ -810,6 +896,14 @@ class TabStripModel {
   std::vector<tab_groups::TabGroupId> GetGroupsDestroyedFromRemovingIndices(
       const std::vector<int>& indices) const;
 
+  // Returns the NESTED ids that will be drained (and therefore destroyed) if
+  // the tabs at `indices` are removed. A NESTED is drained iff its owner is
+  // in the removal set; the owner brings its NESTED (and all descendants)
+  // along, so the whole NESTED is detached as a unit.
+  std::vector<nested_tabs::NestedTabId>
+  GetNestedsDestroyedFromRemovingIndices(
+      const std::vector<int>& indices) const;
+
   // Returns a list of the group ids that are going to be deleted if a given
   // list of tabs are removed.
   std::vector<tab_groups::TabGroupId> GetGroupsDestroyedFromRemovingTabs(
@@ -915,6 +1009,120 @@ class TabStripModel {
   // Temporary private API.
   FRIEND_TEST_ALL_PREFIXES(TabStripModelTest, FindGroupIdFor);
 
+  void MoveNestedToImpl(const nested_tabs::NestedTabId& nested_id,
+                        int to_index,
+                        std::optional<tab_groups::TabGroupId> target_group);
+
+  size_t CountEffectiveMovedTabsLeftOfDestination(
+      const std::vector<int>& indices,
+      int destination_index,
+      const tabs::TabCollection::TypeEnumSet& retain) const;
+
+  // If (global) `index` would place a new tab between a NESTED owner and its
+  // NESTED, returns an adjusted index that lands just past the NESTED.
+  // Otherwise returns `index` unchanged. Pure structural guard, does not
+  // consider add_types, opener, or policy. Safe to call from any insertion
+  // entry point.
+  int MaybeBumpIndexPastNested(int index) const;
+
+  std::optional<nested_tabs::NestedTabId> NestImpl(
+      std::optional<int> parent,
+      std::vector<int> children,
+      std::optional<nested_tabs::NestedTabVisualData> visual_data = {},
+      std::optional<size_t> target_child_idx = {},
+      std::optional<nested_tabs::NestedTabId> id_override = {});
+
+  void UnnestImpl(const nested_tabs::NestedTabId& nested_id);
+
+  void SetSubtreeCollapsedImpl(nested_tabs::NestedTabId nested_id,
+                               bool is_collapsed);
+
+  class CascadeCloseObserver : public content::WebContentsObserver {
+   public:
+    CascadeCloseObserver(content::WebContents* wc,
+                         base::WeakPtr<TabStripModel> tsm)
+        : content::WebContentsObserver(wc), tsm_(std::move(tsm)) {}
+
+    CascadeCloseObserver(const CascadeCloseObserver&) = delete;
+    CascadeCloseObserver& operator=(const CascadeCloseObserver&) = delete;
+
+    // content::WebContentsObserver:
+    void BeforeUnloadFired(bool proceed) override {
+      if (!proceed && tsm_) {
+        // User cancelled the beforeunload prompt, abort the cascade.
+        tsm_->TryCompletePendingCascade(web_contents(), /*cancelled=*/true);
+      }
+      // If proceed=true, the tab will continue closing; we'll hear about it
+      // via WebContentsDestroyed.
+    }
+
+    void WebContentsDestroyed() override {
+      if (tsm_) {
+        tsm_->TryCompletePendingCascade(web_contents(), /*cancelled=*/false);
+      }
+    }
+
+   private:
+    base::WeakPtr<TabStripModel> tsm_;
+  };
+
+  struct PendingCascade {
+    raw_ptr<content::WebContents> parent;
+    uint32_t close_types;
+    // Observers keyed by descendant. Destroying an observer unregisters it.
+    std::map<content::WebContents*, std::unique_ptr<CascadeCloseObserver>>
+        descendants;
+    SessionID parent_session_id = SessionID::InvalidValue();
+  };
+
+  // Callback for the observer: cancelled=true means the user dismissed a
+  // beforeunload prompt (abort the cascade); cancelled=false means the tab
+  // was destroyed (one less descendant pending).
+  void TryCompletePendingCascade(content::WebContents* descendant,
+                                 bool cancelled);
+
+  std::vector<std::unique_ptr<PendingCascade>> pending_cascades_;
+
+  // Observes the destruction of a NESTED subtree owner's WebContents so we
+  // can fire delegate_->NestedSubtreeClosed() at the right moment. The owner's
+  // CreateHistoricalTab fires synchronously inside DetachTabImpl, but the
+  // close itself may be deferred by an unload listener, so firing cleanup
+  // synchronously from the cascade-completion site would clear the
+  // suppression set before the owner actually closes. Tying cleanup to
+  // WebContentsDestroyed handles both sync and async paths uniformly.
+  class NestedSubtreeOwnerObserver : public content::WebContentsObserver {
+   public:
+    NestedSubtreeOwnerObserver(content::WebContents* wc,
+                               base::WeakPtr<TabStripModel> tsm,
+                               SessionID parent_session_id)
+        : content::WebContentsObserver(wc),
+          tsm_(std::move(tsm)),
+          parent_session_id_(parent_session_id) {}
+
+    NestedSubtreeOwnerObserver(const NestedSubtreeOwnerObserver&) = delete;
+    NestedSubtreeOwnerObserver& operator=(const NestedSubtreeOwnerObserver&) =
+        delete;
+
+    // content::WebContentsObserver:
+    void WebContentsDestroyed() override {
+      if (tsm_) {
+        tsm_->OnNestedSubtreeOwnerDestroyed(web_contents(), parent_session_id_);
+      }
+    }
+
+   private:
+    base::WeakPtr<TabStripModel> tsm_;
+    SessionID parent_session_id_;
+  };
+
+  // Fired by NestedSubtreeOwnerObserver. Drops the observer entry and
+  // notifies the delegate that the subtree close is fully complete.
+  void OnNestedSubtreeOwnerDestroyed(content::WebContents* contents,
+                                     SessionID parent_session_id);
+
+  std::map<content::WebContents*, std::unique_ptr<NestedSubtreeOwnerObserver>>
+      nested_subtree_owner_observers_;
+
   struct DetachNotifications;
   struct MoveNotification {
     int initial_index;
@@ -1006,6 +1214,15 @@ class TabStripModel {
   // This also sends any group related notification.
   void NotifySplitTabAttached(tabs::SplitTabCollection* split_collection);
 
+  // Notify observers that a NESTED was detached from this tabstrip model
+  // (e.g., dragged into another tabstrip). Fires kDestroyed so listeners that
+  // care about local presence can clean up.
+  void NotifyNestedDetached(tabs::NestedTabCollection* nested_collection);
+
+  // Notify observers that a NESTED was attached to this tabstrip model
+  // (e.g., dropped after a cross-tabstrip drag). Fires kCreated.
+  void NotifyNestedAttached(tabs::NestedTabCollection* nested_collection);
+
   // Detaches the tab at the specified `index` from this strip.
   // `web_contents_remove_reason` is used to indicate to observers what is going
   // to happen to the WebContents (i.e. deleted or reinserted into another tab
@@ -1282,9 +1499,11 @@ class TabStripModel {
   // Similar to `MoveTabToIndexImpl` but this is used for multiple tabs either
   // being moved or having their group updated. `tab_indices` should be sorted.
   // Tabs are inserted at `destination_index` after they are removed.
-  void MoveTabsToIndexImpl(const std::vector<int>& tab_indices,
-                           int destination_index,
-                           const std::optional<tab_groups::TabGroupId> group);
+  void MoveTabsToIndexImpl(
+      const std::vector<int>& tab_indices,
+      int destination_index,
+      const std::optional<tab_groups::TabGroupId> group,
+      std::optional<nested_tabs::NestedTabId> enclosing_nested = std::nullopt);
 
   // Sends group notifications for a tab at `index` based on its initial_group
   // and `final_group` and updates the `group_model_`.
--- a/chrome/browser/ui/tabs/tab_strip_model_delegate.h
+++ b/chrome/browser/ui/tabs/tab_strip_model_delegate.h
@@ -151,6 +151,12 @@ class TabStripModelDelegate {
   // Notifies the tab restore service that the group is no longer closing.
   virtual void GroupCloseStopped(const tab_groups::TabGroupId& group) = 0;
 
+  // Called when a parent tab with NESTED descendants is about to close.
+  // |parent_index| is the parent's tabstrip index at the moment of the call.
+  virtual void WillCloseNestedSubtree(int parent_index) = 0;
+  virtual void NestedSubtreeClosed(SessionID parent_session_id) = 0;
+  virtual void NestedSubtreeCloseStopped(SessionID parent_session_id) = 0;
+
   // Runs any unload listeners associated with the specified WebContents
   // before it is closed. If there are unload listeners that need to be run,
   // this function returns true and the TabStripModel will wait before closing
--- a/chrome/browser/ui/tabs/tab_strip_model_observer.cc
+++ b/chrome/browser/ui/tabs/tab_strip_model_observer.cc
@@ -330,6 +330,37 @@ const SplitTabChange::RemovedChange* Spl
   return static_cast<const RemovedChange*>(delta.get());
 }
 
+NestedTabChange::NestedTabChange(TabStripModel* model,
+                nested_tabs::NestedTabId nested_id,
+                Type type,
+                std::unique_ptr<Delta> deltap)
+    : nested_id(nested_id),
+      model(model),
+      type(type),
+      delta(std::move(deltap)) {}
+
+#define NESTED_TAB_CHANGE_CTOR_AND_GETTER_IMPL(cls, tt)                          \
+  NestedTabChange::NestedTabChange(                                         \
+      TabStripModel* model, nested_tabs::NestedTabId nested_id, cls deltap) \
+      : nested_id(nested_id),                                               \
+        model(model),                                                       \
+        type(tt),                                                           \
+        delta(std::make_unique<cls>(std::move(deltap))) {}                  \
+  const NestedTabChange::cls* NestedTabChange::Get##cls() const {           \
+    DCHECK_EQ(type, tt);                                                    \
+    return static_cast<const cls*>(delta.get());                            \
+  }
+
+NESTED_TAB_CHANGE_CTOR_AND_GETTER_IMPL(CreatedChange, Type::kCreated)
+NESTED_TAB_CHANGE_CTOR_AND_GETTER_IMPL(DestroyedChange, Type::kDestroyed)
+NESTED_TAB_CHANGE_CTOR_AND_GETTER_IMPL(ContentsChange, Type::kContentsChanged)
+NESTED_TAB_CHANGE_CTOR_AND_GETTER_IMPL(MovedChange, Type::kMoved)
+NESTED_TAB_CHANGE_CTOR_AND_GETTER_IMPL(VisualsChange, Type::kVisualsChanged)
+
+#undef NESTED_TAB_CHANGE_CTOR_AND_GETTER_IMPL
+
+NestedTabChange::~NestedTabChange() = default;
+
 ////////////////////////////////////////////////////////////////////////////////
 // TabStripModelObserver
 //
@@ -384,6 +415,8 @@ void TabStripModelObserver::OnTabGroupWi
 
 void TabStripModelObserver::OnSplitTabChanged(const SplitTabChange& change) {}
 
+void TabStripModelObserver::OnNestedTabChanged(const NestedTabChange& change) {}
+
 void TabStripModelObserver::TabStripEmpty() {}
 
 void TabStripModelObserver::WillCloseAllTabs(TabStripModel* tab_strip_model) {}
--- a/chrome/browser/ui/tabs/tab_strip_model_observer.h
+++ b/chrome/browser/ui/tabs/tab_strip_model_observer.h
@@ -14,6 +14,8 @@
 #include "base/memory/raw_ptr.h"
 #include "chrome/browser/tab_list/tab_removed_reason.h"
 #include "chrome/browser/ui/tabs/tab_change_type.h"
+#include "components/nested_tabs/nested_tab_id.h"
+#include "components/nested_tabs/nested_tab_visual_data.h"
 #include "components/sessions/core/session_id.h"
 #include "components/split_tabs/split_tab_id.h"
 #include "components/split_tabs/split_tab_visual_data.h"
@@ -253,13 +255,7 @@ struct TabGroupChange {
   // a kContentsChange event is fired. Whenever the group's visual data changes,
   // such as its title or color, a kVisualsChange event is fired. Whenever the
   // group is moved by interacting with its header, a kMoved event is fired.
-  enum Type {
-    kCreated,
-    kEditorOpened,
-    kVisualsChanged,
-    kMoved,
-    kClosed
-  };
+  enum Type { kCreated, kEditorOpened, kVisualsChanged, kMoved, kClosed };
 
   enum class TabGroupCreationReason {
     kNewGroupCreated,
@@ -469,6 +465,102 @@ struct SplitTabChange {
   std::unique_ptr<Delta> delta;
 };
 
+struct NestedTabChange {
+  enum class Type {
+    kCreated,
+    kDestroyed,
+    kMoved,
+    kContentsChanged,
+    kVisualsChanged,
+  };
+
+  // Base class for all changes. Similar to TabStripModelChange::Delta.
+  struct Delta {
+    virtual ~Delta() = default;
+  };
+
+  struct CreatedChange : public Delta {
+    CreatedChange(tabs::TabHandle owner,
+                  std::vector<tabs::TabHandle> initial_children,
+                  nested_tabs::NestedTabVisualData visual_data)
+        : owner(owner),
+          initial_children(std::move(initial_children)),
+          visual_data(std::move(visual_data)) {}
+
+    tabs::TabHandle owner;
+    std::vector<tabs::TabHandle> initial_children;
+    nested_tabs::NestedTabVisualData visual_data;
+  };
+
+  struct DestroyedChange : public Delta {
+    DestroyedChange(tabs::TabHandle former_owner,
+                    std::vector<tabs::TabHandle> orphaned_children)
+        : former_owner(former_owner),
+          orphaned_children(std::move(orphaned_children)) {}
+
+    tabs::TabHandle former_owner;
+    // Tabs that were in this NESTED at the moment they triggered its
+    // destruction. If the NESTED was emptied gradually, this list contains
+    // only the final tab(s) whose departure caused the empty state.
+    std::vector<tabs::TabHandle> orphaned_children;
+  };
+
+  struct ContentsChange : public Delta {
+    ContentsChange(std::vector<tabs::TabHandle> children_added,
+                   std::vector<tabs::TabHandle> children_removed)
+        : children_added(std::move(children_added)),
+          children_removed(std::move(children_removed)) {}
+
+    std::vector<tabs::TabHandle> children_added;
+    std::vector<tabs::TabHandle> children_removed;
+  };
+
+  struct MovedChange : public Delta {
+    MovedChange(int from_index, int to_index)
+        : from_index(from_index), to_index(to_index) {}
+
+    int from_index;
+    int to_index;
+  };
+
+  struct VisualsChange : public Delta {
+    VisualsChange(nested_tabs::NestedTabVisualData old_visual_data,
+                  nested_tabs::NestedTabVisualData new_visual_data)
+        : old_visual_data(std::move(old_visual_data)),
+          new_visual_data(std::move(new_visual_data)) {}
+
+    nested_tabs::NestedTabVisualData old_visual_data;
+    nested_tabs::NestedTabVisualData new_visual_data;
+  };
+
+  NestedTabChange(TabStripModel* model,
+                  nested_tabs::NestedTabId nested_id,
+                  Type type,
+                  std::unique_ptr<Delta> deltap);
+
+#define NESTED_TAB_CHANGE_CTOR_AND_GETTER(cls)                              \
+  NestedTabChange(TabStripModel* model, nested_tabs::NestedTabId nested_id, \
+                  cls deltap);                                              \
+  const cls* Get##cls() const;
+
+  NESTED_TAB_CHANGE_CTOR_AND_GETTER(CreatedChange)
+  NESTED_TAB_CHANGE_CTOR_AND_GETTER(DestroyedChange)
+  NESTED_TAB_CHANGE_CTOR_AND_GETTER(ContentsChange)
+  NESTED_TAB_CHANGE_CTOR_AND_GETTER(MovedChange)
+  NESTED_TAB_CHANGE_CTOR_AND_GETTER(VisualsChange)
+
+#undef NESTED_TAB_CHANGE_CTOR_AND_GETTER
+
+  ~NestedTabChange();
+
+  nested_tabs::NestedTabId nested_id;
+  raw_ptr<TabStripModel> model;
+  Type type;
+
+ private:
+  std::unique_ptr<Delta> delta;
+};
+
 ////////////////////////////////////////////////////////////////////////////////
 //
 // TabStripModelObserver
@@ -576,6 +668,9 @@ class TabStripModelObserver {
   // The |change| provides details of the change to split tab.
   virtual void OnSplitTabChanged(const SplitTabChange& change);
 
+  // Notifies us when there is a change to the nesting state.
+  virtual void OnNestedTabChanged(const NestedTabChange& change);
+
   // The TabStripModel now no longer has any tabs. The implementer may
   // use this as a trigger to try and close the window containing the
   // TabStripModel, for example...
--- a/chrome/browser/ui/tabs/tab_strip_model_unittest.cc
+++ b/chrome/browser/ui/tabs/tab_strip_model_unittest.cc
@@ -7274,3 +7274,26 @@ TEST_P(TabStripModelTest, ReinsertTabGro
   EXPECT_EQ("active=2 anchor=2 selection=2 4",
             tabstrip()->selection_model().GetListSelectionModel().ToString());
 }
+
+TEST_F(TabStripModelTest, NestCreatesNewNestedCollection) {
+  TestTabStripModelDelegate delegate;
+  TabStripModel model(&delegate, profile());
+
+  // Add three tabs: 0 = parent, 1 = child A, 2 = child B
+  model.AppendWebContents(CreateWebContents(), /*foreground=*/true);
+  model.AppendWebContents(CreateWebContents(), /*foreground=*/false);
+  model.AppendWebContents(CreateWebContents(), /*foreground=*/false);
+  ASSERT_EQ(3, model.count());
+
+  // Nest tabs 1 and 2 under tab 0.
+  auto nested_id = model.Nest(/*parent=*/0, /*children=*/{1, 2});
+  ASSERT_TRUE(nested_id.has_value());
+
+  // Verify the mapping was populated.
+  EXPECT_NE(nullptr, model.GetNestingData(*nested_id));
+
+  // Verify parent-child relationships.
+  EXPECT_EQ(std::nullopt, model.GetParentIndex(0));    // top-level
+  EXPECT_EQ(0, model.GetParentIndex(1).value_or(-1));  // child of 0
+  EXPECT_EQ(0, model.GetParentIndex(2).value_or(-1));  // child of 0
+}
--- a/chrome/browser/ui/tabs/tab_strip_prefs.cc
+++ b/chrome/browser/ui/tabs/tab_strip_prefs.cc
@@ -50,6 +50,7 @@ void RegisterProfilePrefs(user_prefs::Pr
   registry->RegisterIntegerPref(prefs::kHeliumLayout,
                                 std::to_underlying(HeliumLayoutType::kClassic));
   registry->RegisterBooleanPref(prefs::kHeliumVerticalRightAligned, false);
+  registry->RegisterBooleanPref(prefs::kHeliumVerticalNesting, false);
   registry->RegisterBooleanPref(prefs::kHeliumZenMode, false);
   registry->RegisterBooleanPref(prefs::kHeliumZenModeSidebarPinned, false);
   registry->RegisterBooleanPref(prefs::kHeliumZenModeTopChromePinned, false);
--- a/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.cc
+++ b/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.cc
@@ -61,7 +61,7 @@ VerticalTabStripStateController::Vertica
       std::to_underlying(HeliumLayoutType::kVertical);
 
   pref_change_registrar_.AddMultiple(
-      {prefs::kHeliumLayout, prefs::kHeliumVerticalRightAligned},
+      {prefs::kHeliumLayout, prefs::kHeliumVerticalRightAligned, prefs::kHeliumVerticalNesting},
       base::BindRepeating(&VerticalTabStripStateController::OnModeChanged,
                           base::Unretained(this)));
 
@@ -178,6 +178,14 @@ void VerticalTabStripStateController::Se
   pref_service_->SetBoolean(prefs::kHeliumVerticalRightAligned, right_aligned);
 }
 
+bool VerticalTabStripStateController::IsTabStripNestingEnabled() const {
+    return pref_service_->GetBoolean(prefs::kHeliumVerticalNesting);
+}
+
+void VerticalTabStripStateController::SetTabStripNestingEnabled(bool enable_nesting) {
+    pref_service_->SetBoolean(prefs::kHeliumVerticalNesting, enable_nesting);
+}
+
 bool VerticalTabStripStateController::IsCollapsed() const {
   return state_.collapsed;
 }
--- a/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h
+++ b/chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h
@@ -89,6 +89,8 @@ class VerticalTabStripStateController :
   void SetVerticalTabsEnabled(bool enabled);
   bool IsTabStripRightAligned() const;
   void SetTabStripRightAligned(bool right_aligned);
+  bool IsTabStripNestingEnabled() const;
+  void SetTabStripNestingEnabled(bool enable_nesting);
 
   std::unique_ptr<ScopedEnableStateLock> GetEnableStateLock();
 
--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -1542,6 +1542,11 @@ bool BrowserView::IsVerticalTabStripRigh
   return controller && controller->IsTabStripRightAligned();
 }
 
+bool BrowserView::IsVerticalTabStripNestingEnabled() const {
+    auto *controller = tabs::VerticalTabStripStateController::From(browser_);
+    return controller && controller->IsTabStripNestingEnabled();
+}
+
 bool BrowserView::ShouldDrawWebAppFrameToolbar() const {
   return !IsUnframedModeEnabled() &&
          GetFrameView()->ShouldShowWebAppFrameToolbar();
--- a/chrome/browser/ui/views/frame/browser_view.h
+++ b/chrome/browser/ui/views/frame/browser_view.h
@@ -371,6 +371,9 @@ class BrowserView : public BrowserWindow
   // Returns whether vertical tabs are aligned to the right edge.
   bool IsVerticalTabStripRightAligned() const;
 
+  // Returns whether vertical tabs allow nesting.
+  bool IsVerticalTabStripNestingEnabled() const;
+
   // Returns whether the webapp frame toolbar should be drawn.
   bool ShouldDrawWebAppFrameToolbar() const;
 
--- a/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate.h
+++ b/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate.h
@@ -32,6 +32,7 @@ class BrowserViewLayoutDelegate {
   virtual bool ShouldDrawToolbarTabStrip() const = 0;
   virtual bool IsVerticalTabStripCollapsed() const = 0;
   virtual bool IsVerticalTabStripRightAligned() const = 0;
+  virtual bool IsVerticalTabStripNestingEnabled() const = 0;
   virtual bool ShouldDrawWebAppFrameToolbar() const = 0;
   virtual bool GetBorderlessModeEnabled() const = 0;
   virtual BrowserLayoutParams GetBrowserLayoutParams(
--- a/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate_impl.cc
+++ b/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate_impl.cc
@@ -62,6 +62,10 @@ bool BrowserViewLayoutDelegateImpl::IsVe
   return browser_view_->IsVerticalTabStripRightAligned();
 }
 
+bool BrowserViewLayoutDelegateImpl::IsVerticalTabStripNestingEnabled() const {
+    return browser_view_->IsVerticalTabStripNestingEnabled();
+}
+
 bool BrowserViewLayoutDelegateImpl::ShouldDrawWebAppFrameToolbar() const {
   return browser_view_->ShouldDrawWebAppFrameToolbar();
 }
--- a/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate_impl.h
+++ b/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate_impl.h
@@ -27,6 +27,7 @@ class BrowserViewLayoutDelegateImpl : pu
   bool ShouldDrawToolbarTabStrip() const override;
   bool IsVerticalTabStripCollapsed() const override;
   bool IsVerticalTabStripRightAligned() const override;
+  bool IsVerticalTabStripNestingEnabled() const override;
   bool ShouldDrawWebAppFrameToolbar() const override;
   bool GetBorderlessModeEnabled() const override;
   BrowserLayoutParams GetBrowserLayoutParams(
--- a/chrome/browser/ui/views/tabs/dragging/drag_session_data.cc
+++ b/chrome/browser/ui/views/tabs/dragging/drag_session_data.cc
@@ -31,6 +31,10 @@ std::optional<int> ViewModelIndex(TabDra
 TabDragData::TabDragData(TabDragContext* source_context, TabSlotView* view)
     : source_model_index(ViewModelIndex(source_context, view)),
       view_type(view->GetTabSlotViewType()) {
+  // Pre-populate so the first GetViewsMatchingDraggedContents (which runs
+  // before AttachImpl) can resolve the NESTED's slot view; otherwise group
+  // drags containing a NESTED yield an empty views vector and cancel.
+  attached_view = view;
   if (source_model_index.has_value()) {
     contents = source_context->GetContentsForTab(view);
     pinned = source_context->GetTabStripModel()->IsTabPinned(
--- a/chrome/browser/ui/views/tabs/dragging/drag_session_data.h
+++ b/chrome/browser/ui/views/tabs/dragging/drag_session_data.h
@@ -7,10 +7,12 @@
 
 #include <limits>
 #include <memory>
+#include <optional>
 #include <vector>
 
 #include "base/memory/raw_ptr.h"
 #include "chrome/browser/ui/views/tabs/tab_slot_view.h"
+#include "components/nested_tabs/nested_tab_id.h"
 #include "components/tab_groups/tab_group_id.h"
 #include "components/tab_groups/tab_group_visual_data.h"
 #include "ui/gfx/geometry/vector2d_f.h"
@@ -38,6 +40,21 @@ struct GroupHeaderDragData final {
         active_tab_index_within_group(active_tab_index_within_group) {}
 };
 
+struct NestedDragData final {
+  // The nested that is being dragged.
+  nested_tabs::NestedTabId nested_id;
+
+  // The index of the tab within the nested, if any, that was active when the
+  // drag began. Defaults to 0 if the active tab was outside the nested, as we
+  // should fall back on activating the first tab during/after the drag.
+  int active_tab_index_within_nested;
+
+  NestedDragData(nested_tabs::NestedTabId nested_id,
+                 int active_tab_index_within_nested)
+      : nested_id(nested_id),
+        active_tab_index_within_nested(active_tab_index_within_nested) {}
+};
+
 // Stores the data associated with a single tab that is being dragged.
 struct TabDragData final {
   TabDragData(TabDragContext* source_context, TabSlotView* view);
@@ -90,6 +107,8 @@ struct DragSessionData final {
   // being dragged together.
   std::optional<GroupHeaderDragData> group_header_drag_data_ = std::nullopt;
 
+  std::optional<NestedDragData> nested_drag_data_ = std::nullopt;
+
   // Groups that are being dragged, including header-only drags (in which case
   // the size of this will be one) or by selecting all tabs in a group.
   std::set<tab_groups::TabGroupId> dragging_groups;
@@ -111,6 +130,10 @@ struct DragSessionData final {
                : std::nullopt;
   }
 
+  std::optional<nested_tabs::NestedTabId> nested_id() const {
+    return nested_drag_data_.transform([](auto dd) { return dd.nested_id; });
+  }
+
   // Convenience for getting the TabDragData corresponding to the source view
   // that the user started dragging.
   const TabDragData* source_view_drag_data() const {
--- a/chrome/browser/ui/views/tabs/dragging/tab_drag_controller.cc
+++ b/chrome/browser/ui/views/tabs/dragging/tab_drag_controller.cc
@@ -58,6 +58,8 @@
 #include "chrome/browser/web_applications/web_app_provider.h"
 #include "chrome/browser/web_applications/web_app_registrar.h"
 #include "components/tab_groups/tab_group_id.h"
+#include "components/tabs/public/nested_tab_collection.h"
+#include "components/tabs/public/nested_tab_data.h"
 #include "components/tabs/public/tab_group.h"
 #include "components/tabs/public/tab_interface.h"
 #include "components/viz/common/frame_sinks/copy_output_result.h"
@@ -475,8 +477,24 @@ TabDragController::Liveness TabDragContr
 
   const TabStripModel* tab_strip_model =
       ref->source_context_->GetTabStripModel();
-  if (source_view->GetTabSlotViewType() ==
-      TabSlotView::ViewType::kTabGroupHeader) {
+  // source_view->nested() may be a stale id (e.g., NESTED collapsed before
+  // this drag started); fall through to non-nested handling in that case.
+  const nested_tabs::NestedTabData* ntd =
+      source_view->nested() ? tab_strip_model->GetNestingData(
+                                  *source_view->nested())
+                            : nullptr;
+  if (ntd) {
+    const nested_tabs::NestedTabId nested = *source_view->nested();
+    gfx::Range nested_range = ntd->GetIndexRange();
+    const int active = tab_strip_model->active_index();
+    const int active_tab_index_within_nested =
+        nested_range.Contains(gfx::Range(active))
+            ? active - static_cast<int>(nested_range.GetMin())
+            : 0;
+    ref->drag_data_.nested_drag_data_ = std::make_optional<NestedDragData>(
+        nested, active_tab_index_within_nested);
+  } else if (source_view->GetTabSlotViewType() ==
+             TabSlotView::ViewType::kTabGroupHeader) {
     const tab_groups::TabGroupId group_id = source_view->group().value();
     const std::optional<tab_groups::TabGroupId> active_group_id =
         tab_strip_model->GetActiveTab()->GetGroup();
@@ -726,7 +744,7 @@ TabDragController::Liveness TabDragContr
     }
 
     current_state_ = DragState::kDraggingTabs;
-      StartDraggingTabsSession(true, point_in_screen);
+    StartDraggingTabsSession(true, point_in_screen);
   }
 
   return ContinueDragging(point_in_screen);
@@ -1399,6 +1417,12 @@ void TabDragController::AttachToNewConte
         collection_indices =
             attached_context_->GetTabStripModel()->InsertDetachedTabGroupAt(
                 std::move(*detached_tab_collection), index);
+      } else if (std::holds_alternative<
+                     std::unique_ptr<tabs::NestedTabCollection>>(
+                     detached_tab_collection->get()->collection_)) {
+        collection_indices =
+            attached_context_->GetTabStripModel()->InsertDetachedNestedAt(
+                std::move(*detached_tab_collection), index);
       } else {
         collection_indices =
             attached_context_->GetTabStripModel()->InsertDetachedSplitTabAt(
@@ -1793,7 +1817,8 @@ std::vector<TabSlotView*> TabDragControl
         return {};
       }
       views.push_back(tab_view);
-    } else {
+    } else if (tab_drag_datum.view_type ==
+               TabSlotView::ViewType::kTabGroupHeader) {
       // Return empty vector if the group is not present in the model.
       if (!model->group_model()->ContainsTabGroup(
               tab_drag_datum.tab_group_data->group_id)) {
@@ -1804,6 +1829,13 @@ std::vector<TabSlotView*> TabDragControl
           context->GetTabGroupHeader(tab_drag_datum.tab_group_data->group_id);
       CHECK(header_view);
       views.push_back(header_view);
+    } else {
+      // kNestedContainer: use the slot view captured at drag-init time;
+      // model-side handling is routed via NestedDragData, not tab_drag_data_.
+      if (!tab_drag_datum.attached_view) {
+        return {};
+      }
+      views.push_back(tab_drag_datum.attached_view);
     }
   }
   return views;
@@ -1919,6 +1951,11 @@ void TabDragController::RevertDrag() {
                ->GetTabGroup(tab_data.tab_group_data->group_id)
                ->tab_count() +
            1;
+    } else if (tab_data.view_type ==
+               TabSlotView::ViewType::kNestedContainer) {
+      // NESTED slot views have no per-tab model state to revert (the NESTED
+      // moves as a unit via NestedDragData).
+      i++;
     } else {
       CHECK(tab_data.contents);
 
--- a/chrome/browser/ui/views/tabs/tab/tab_icon.cc
+++ b/chrome/browser/ui/views/tabs/tab/tab_icon.cc
@@ -57,6 +57,7 @@ namespace {
 
 constexpr int kAttentionIndicatorRadius = 3;
 constexpr int kLoadingAnimationStrokeWidthDp = 2;
+constexpr uint8_t kDimmedAlpha = 0x5e;
 
 bool NetworkStateIsAnimated(tabs::TabNetworkState network_state) {
   return network_state != tabs::TabNetworkState::kNone &&
@@ -237,6 +238,10 @@ void TabIcon::OnPaint(gfx::Canvas* canva
     return;
   }
 
+  if (is_dimmed_) {
+    canvas->SaveLayerAlpha(kDimmedAlpha);
+  }
+
   gfx::Rect icon_bounds(
       GetMirroredXWithWidthInView(contents_bounds.x(), gfx::kFaviconSize),
       contents_bounds.y() +
@@ -257,6 +262,10 @@ void TabIcon::OnPaint(gfx::Canvas* canva
   if (GetShowingLoadingAnimation()) {
     PaintLoadingAnimation(canvas, icon_bounds);
   }
+
+  if (is_dimmed_) {
+    canvas->Restore();
+  }
 }
 
 views::PaintInfo::ScaleType TabIcon::GetPaintScaleType() const {
@@ -543,6 +552,15 @@ bool TabIcon::GetCrashed() const {
   return crashed_;
 }
 
+void TabIcon::SetDimmed(bool dimmed) {
+  if (dimmed == is_dimmed_) {
+    return;
+  }
+
+  is_dimmed_ = dimmed;
+  OnPropertyChanged(&is_dimmed_, views::PropertyEffects::kPaint);
+}
+
 void TabIcon::RefreshLayer() {
   // Since the loading animation can run for a long time, paint animation to a
   // separate layer when possible to reduce repaint overhead.
--- a/chrome/browser/ui/views/tabs/tab/tab_icon.h
+++ b/chrome/browser/ui/views/tabs/tab/tab_icon.h
@@ -92,6 +92,9 @@ class TabIcon : public views::View, publ
   void ResizeDiscardIndicatorRadiusForWidth(int width);
   void OnDiscardRingTreatmentEnabledChanged();
 
+  void SetDimmed(bool dimmed);
+  bool GetDimmed() const { return is_dimmed_; }
+
  protected:
   class CrashAnimation;
   friend CrashAnimation;
@@ -217,6 +220,8 @@ class TabIcon : public views::View, publ
 
   bool is_monochrome_favicon_ = false;
 
+  bool is_dimmed_ = false;
+
   // If there is enough space, increased the size of the discard ring beyond the
   // default favicon size, by this amount.
   static constexpr int kIncreasedDiscardIndicatorRadiusDp = 2;
--- a/chrome/browser/ui/views/tabs/tab_slot_view.h
+++ b/chrome/browser/ui/views/tabs/tab_slot_view.h
@@ -8,6 +8,7 @@
 #include <optional>
 
 #include "chrome/browser/ui/views/tabs/tab_strip_layout.h"
+#include "components/nested_tabs/nested_tab_id.h"
 #include "components/split_tabs/split_tab_id.h"
 #include "components/tab_groups/tab_group_id.h"
 #include "ui/base/metadata/metadata_header_macros.h"
@@ -21,6 +22,7 @@ class TabSlotView : public views::View {
   enum class ViewType {
     kTab,
     kTabGroupHeader,
+    kNestedContainer,
   };
 
   TabSlotView();
@@ -43,6 +45,12 @@ class TabSlotView : public views::View {
 
   std::optional<split_tabs::SplitTabId> split() const { return split_; }
 
+  virtual void SetNested(std::optional<nested_tabs::NestedTabId> nested) {
+    nested_ = nested;
+  }
+
+  std::optional<nested_tabs::NestedTabId> nested() const { return nested_; }
+
   // Used to mark the view as having been detached.  Once this has happened, the
   // view should be invisibly closed.  This is irreversible.
   void set_detached() { detached_ = true; }
@@ -71,6 +79,8 @@ class TabSlotView : public views::View {
   bool animating_ = false;
 
   std::optional<split_tabs::SplitTabId> split_;
+
+  std::optional<nested_tabs::NestedTabId> nested_;
 };
 
 #endif  // CHROME_BROWSER_UI_VIEWS_TABS_TAB_SLOT_VIEW_H_
--- a/chrome/browser/ui/views/tabs/vertical/BUILD.gn
+++ b/chrome/browser/ui/views/tabs/vertical/BUILD.gn
@@ -13,6 +13,7 @@ source_set("vertical") {
     "tab_drag_scroll_handler.h",
     "top_container_button.h",
     "vertical_dragged_tabs_container.h",
+    "vertical_nested_tab_view.h",
     "vertical_pinned_tab_container_view.h",
     "vertical_split_tab_view.h",
     "vertical_new_tab_button.h",
@@ -49,6 +50,7 @@ source_set("impl") {
     "tab_drag_scroll_handler.cc",
     "top_container_button.cc",
     "vertical_dragged_tabs_container.cc",
+    "vertical_nested_tab_view.cc",
     "vertical_new_tab_button.cc",
     "vertical_pinned_tab_container_view.cc",
     "vertical_split_tab_view.cc",
--- a/chrome/browser/ui/views/tabs/vertical/root_tab_collection_node.cc
+++ b/chrome/browser/ui/views/tabs/vertical/root_tab_collection_node.cc
@@ -10,9 +10,12 @@
 #include "base/types/pass_key.h"
 #include "chrome/browser/ui/tabs/tab_group_model.h"
 #include "chrome/browser/ui/tabs/tab_strip_model.h"
+#include "chrome/browser/ui/tabs/tab_strip_model_observer.h"
 #include "chrome/browser/ui/views/tabs/vertical/tab_collection_node.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_group_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_controller.h"
+#include "components/tabs/public/nested_tab_collection.h"
+#include "components/tabs/public/nested_tab_data.h"
 #include "components/tabs/public/split_tab_data.h"
 #include "components/tabs/public/tab_collection.h"
 #include "components/tabs/public/tab_collection_types.h"
@@ -261,6 +264,45 @@ void RootTabCollectionNode::OnTabChanged
   UpdateTabsData({tab});
 }
 
+void RootTabCollectionNode::OnNestedTabChanged(const NestedTabChange& change) {
+  if (tab_strip_model_->closing_all()) {
+    return;
+  }
+
+  TabCollectionNode* owner_node = nullptr;
+  TabCollectionNode* nested_node = nullptr;
+
+  switch (change.type) {
+    case NestedTabChange::Type::kCreated:
+    case NestedTabChange::Type::kVisualsChanged: {
+      const auto* data = change.model->GetNestingData(change.nested_id);
+      if (!data) {
+        return;
+      }
+      owner_node = GetNodeForHandle(data->owner_tab());
+      nested_node = GetNodeForHandle(data->GetCollectionHandle());
+      break;
+    }
+    case NestedTabChange::Type::kDestroyed: {
+      const auto* destroyed = change.GetDestroyedChange();
+      if (!destroyed) {
+        return;
+      }
+      owner_node = GetNodeForHandle(destroyed->former_owner);
+      break;
+    }
+    default:
+      return;  // kContentsChanged, kMoved: no chevron/dim impact today
+  }
+
+  if (nested_node) {
+    nested_node->NotifyDataChanged();
+  }
+  if (owner_node) {
+    owner_node->NotifyDataChanged();
+  }
+}
+
 void RootTabCollectionNode::UpdateTabsData(
     const std::set<tabs::TabInterface*>& changed_tabs) {
   std::set<TabCollectionNode*> nodes_to_notify;
--- a/chrome/browser/ui/views/tabs/vertical/root_tab_collection_node.h
+++ b/chrome/browser/ui/views/tabs/vertical/root_tab_collection_node.h
@@ -71,6 +71,7 @@ class RootTabCollectionNode : public Tab
   void OnTabChangedAt(tabs::TabInterface* tab,
                       int model_index,
                       TabChangeType change_type) override;
+  void OnNestedTabChanged(const NestedTabChange& change) override;
 
   void UpdateTabsData(const std::set<tabs::TabInterface*>& changed_tabs);
 
--- a/chrome/browser/ui/views/tabs/vertical/tab_collection_node.cc
+++ b/chrome/browser/ui/views/tabs/vertical/tab_collection_node.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/ui/views/tabs/vertical/tab_collection_node.h"
 
+#include <memory>
 #include <vector>
 
 #include "base/functional/bind.h"
@@ -11,6 +12,7 @@
 #include "base/no_destructor.h"
 #include "base/notreached.h"
 #include "base/strings/stringprintf.h"
+#include "chrome/browser/ui/views/tabs/vertical/vertical_nested_tab_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_pinned_tab_container_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_split_tab_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_group_view.h"
@@ -38,6 +40,8 @@ TabCollectionNode::Type GetTypeFromNode(
         return TabCollectionNode::Type::GROUP;
       case tabs::TabCollection::Type::SPLIT:
         return TabCollectionNode::Type::SPLIT;
+      case tabs::TabCollection::Type::NESTED:
+        return TabCollectionNode::Type::NESTED;
     }
   }
   CHECK(std::holds_alternative<const tabs::TabInterface*>(node_data_));
@@ -104,6 +108,8 @@ std::unique_ptr<views::View> TabCollecti
       return std::make_unique<VerticalSplitTabView>(node_for_view);
     case Type::GROUP:
       return std::make_unique<VerticalTabGroupView>(node_for_view);
+    case Type::NESTED:
+      return std::make_unique<VerticalNestedTabView>(node_for_view);
     case Type::TAB:
       return std::make_unique<VerticalTabView>(node_for_view);
   }
--- a/chrome/browser/ui/views/tabs/vertical/tab_collection_node.h
+++ b/chrome/browser/ui/views/tabs/vertical/tab_collection_node.h
@@ -30,7 +30,7 @@ class TabCollectionNode {
  public:
   // Keep the same as TabCollection::Type with a tab.
   // LINT.IfChange(TYPE)
-  enum class Type { TABSTRIP, PINNED, UNPINNED, GROUP, SPLIT, TAB };
+  enum class Type { TABSTRIP, PINNED, UNPINNED, GROUP, SPLIT, NESTED, TAB };
   // LINT.ThenChange(components/tabs/public/tab_collection.h:TYPE)
 
   // Helper type for creating CustomAddChildViewCallbacks with
--- a/chrome/browser/ui/views/tabs/vertical/vertical_dragged_tabs_container.cc
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_dragged_tabs_container.cc
@@ -4,6 +4,8 @@
 
 #include "chrome/browser/ui/views/tabs/vertical/vertical_dragged_tabs_container.h"
 
+#include <variant>
+
 #include "base/callback_list.h"
 #include "base/containers/flat_set.h"
 #include "base/i18n/rtl.h"
@@ -16,6 +18,8 @@
 #include "chrome/browser/ui/views/tabs/dragging/tab_drag_target.h"
 #include "chrome/browser/ui/views/tabs/tab_slot_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/tab_collection_node.h"
+#include "chrome/browser/ui/views/tabs/vertical/vertical_nested_tab_view.h"
+#include "chrome/browser/ui/views/tabs/vertical/vertical_tab_group_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_drag_handler.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_controller.h"
 #include "ui/compositor/layer.h"
@@ -29,6 +33,20 @@
 
 namespace {
 
+// Fraction of a tab's height treated as the "child into" zone.
+// Above and below this centered slice are insert-before/insert-after edges.
+// Example: 0.8 -> top 10% and bottom 10% are edge zones; middle 80% is child zone.
+constexpr float kChildIntoZoneFraction = 0.8f;
+
+const tabs::TabInterface* TabFromNode(const TabCollectionNode* node) {
+  if (!node) {
+    return nullptr;
+  }
+  auto child = node->GetNodeData();
+  const auto* tab_ptr = std::get_if<const tabs::TabInterface*>(&child);
+  return tab_ptr ? *tab_ptr : nullptr;
+}
+
 // Calculates the offset of the source dragged view (i.e. the main view being
 // dragged) from the mouse.
 gfx::Vector2d GetSourceViewOffsetFromMouse(
@@ -77,6 +95,27 @@ VerticalDraggedTabsContainer::~VerticalD
 
 VerticalDraggedTabsContainer& VerticalDraggedTabsContainer::GetTabDragTarget(
     const gfx::Point& point_in_screen) {
+  // Delegate to a child container (NESTED wrapper or group view) that
+  // contains the point. Skip a child that's itself the active drag target,
+  // so the dragged view and host stay in the same coord space (otherwise
+  // the SetTransform delta math leaves a persistent offset).
+  for (views::View* child : host_view_->children()) {
+    VerticalDraggedTabsContainer* child_container = nullptr;
+    if (auto* nested = views::AsViewClass<VerticalNestedTabView>(child)) {
+      child_container = nested;
+    } else if (auto* group = views::AsViewClass<VerticalTabGroupView>(child)) {
+      child_container = group;
+    }
+    if (!child_container) {
+      continue;
+    }
+    if (GetDragHandler().IsViewDragging(*child)) {
+      continue;
+    }
+    if (child->GetBoundsInScreen().Contains(point_in_screen)) {
+      return child_container->GetTabDragTarget(point_in_screen);
+    }
+  }
   return *this;
 }
 
@@ -84,6 +123,11 @@ TabDragContext* VerticalDraggedTabsConta
     TabDragTarget::DragController& drag_controller,
     const gfx::Point& point_in_screen) {
   last_drag_point_in_screen_ = point_in_screen;
+  // Backing collection may be a zombie (e.g., nested-in-nested collapse);
+  // GetDragTargetForPoint can still pick it before the view's torn down.
+  if (!collection_node_) {
+    return nullptr;
+  }
   if (drag_controller.GetAttachedContext() !=
       GetDragHandler().GetDragContext()) {
     // Do nothing until the drag attaches to this window's context.
@@ -99,6 +143,13 @@ TabDragContext* VerticalDraggedTabsConta
 
   ApplyUpdatesForDragPositionChange();
 
+  // Re-entrant model updates above may have destroyed our collection (e.g.,
+  // dragging the last non-owner child out collapses the NESTED). Bail; the
+  // drag controller retargets next frame.
+  if (!collection_node_) {
+    return nullptr;
+  }
+
   return GetDragHandler().GetDragContext();
 }
 
@@ -225,12 +276,17 @@ void VerticalDraggedTabsContainer::Initi
   gfx::Rect dragged_bounds_in_container =
       GetDraggingViewsBoundsAtPointClamped(point_in_container);
 
-  const auto* source_dragged_view = drag_handler.ViewFromTabSlot(
+  views::View* source_dragged_view = drag_handler.ViewFromTabSlot(
       controller.GetSessionData().source_view_drag_data()->attached_view);
 
+  // The source view may not participate at this level (BuildDragLayout
+  // filters out views not parented to `host_view_`); bail rather than CHECK.
+  auto source_it = dragging_views_.find(source_dragged_view);
+  if (source_it == dragging_views_.end()) {
+    return;
+  }
   const auto source_view_target_position = GetDraggingViewPositionForBounds(
-      source_dragged_view, dragged_bounds_in_container,
-      dragging_views_.at(source_dragged_view).offset);
+      source_dragged_view, dragged_bounds_in_container, source_it->second.offset);
 
   // Build the map of dragging views to their offset between their position at
   // the start of the drag, and the position they're expected to be at this
@@ -239,6 +295,12 @@ void VerticalDraggedTabsContainer::Initi
   for (const auto& [dragging_view, visual_data] : dragging_views_) {
     auto start_offset_from_source =
         drag_handler.GetOffsetFromSourceAtDragStart(dragging_view);
+    if (!start_offset_from_source) {
+      // View didn't go through StartedDragging's per-view setup (e.g., a
+      // NESTED wrapper rebased into dragging_views_ from a descendant drag);
+      // no enter-animation needed.
+      continue;
+    }
     auto target_offset_from_source =
         GetDraggingViewPositionForBounds(
             dragging_view, dragged_bounds_in_container, visual_data.offset) -
@@ -288,11 +350,11 @@ void VerticalDraggedTabsContainer::Build
     CHECK(dragging_view);
 
     if (dragging_view->parent() != base::to_address(host_view_)) {
-      continue;
+      continue;  // no host-direct ancestor; this view doesn't participate
     }
     if (dragging_views_.contains(dragging_view)) {
-      // It's possible that multiple dragged tabs map to the same dragged view
-      // (e.g., split tabs). Skip the duplicates.
+      // Multiple dragged tabs may map to the same dragged view (e.g., split
+      // tabs, or all NESTED descendants mapping to the wrapper).
       continue;
     }
 
@@ -417,6 +479,10 @@ gfx::Vector2d VerticalDraggedTabsContain
                            ? dragging_views_bounding_box.x() + target_offset.x()
                            : 0,
                        dragging_views_bounding_box.y() + target_offset.y());
+  // SetTransform's translation is a delta from layer bounds.origin, which
+  // equals the view's layout position. Subtract layout y so the rendered
+  // y == target. (x is handled separately by horizontal drag math.)
+  target.set_y(target.y() - dragging_view->bounds().y());
   double value = drag_start_animation_.GetCurrentValue();
   if (drag_start_animation_.is_animating()) {
     if (auto it = animating_views_start_offsets_.find(dragging_view);
@@ -495,6 +561,24 @@ VerticalDraggedTabsContainer::GetVisualD
   };
 }
 
+std::set<int> VerticalDraggedTabsContainer::GetDraggedIndices() const {
+  std::set<int> result;
+  TabStripModel* model = collection_node_->GetController()->model();
+  CHECK(GetDragHandler().GetDragController())
+      << "Somehow drag controller is null!";
+  for (const auto& data :
+       GetDragHandler().GetDragController()->GetSessionData().tab_drag_data_) {
+    if (!data.contents) {
+      continue;
+    }
+    const int idx = model->GetIndexOfWebContents(data.contents);
+    if (idx != TabStripModel::kNoTab) {
+      result.insert(idx);
+    }
+  }
+  return result;
+}
+
 bool VerticalDraggedTabsContainer::IsHorizontalDragSupported() const {
   return drag_axes_ != DragAxes::kVerticalOnly &&
          GetTabStripCollapseState() ==
@@ -555,6 +639,11 @@ VerticalDraggedTabsContainer::GetTabStri
 
 void VerticalDraggedTabsContainer::ResetCollectionNode() {
   collection_node_ = nullptr;
+  // Notify observers (e.g. TabDragController) that this container is no longer
+  // a valid drag target. Without this, a drag in progress can keep dispatching
+  // events into a zombie container whose backing collection is gone, hitting
+  // CHECKs on collection_node_.
+  on_will_destroy_callback_list_.Notify();
 }
 
 std::vector<const views::View*> VerticalDraggedTabsContainer::GetDraggingViews()
@@ -572,20 +661,57 @@ void VerticalDraggedTabsContainer::Handl
     const gfx::Rect& dragged_tab_bounds) {
   VerticalTabDragHandler& drag_handler = GetDragHandler();
   const views::ProposedLayout& drag_layout = GetLayoutForDrag();
-  const auto* target = GetTargetForTabDrag(drag_layout, dragged_tab_bounds);
+  const std::set<int> dragged_idxs = GetDraggedIndices();
+
+  const DragDropTarget target =
+      GetTargetForTabDrag(drag_layout, dragged_tab_bounds, dragged_idxs);
 
-  // A null target implies the drag is at the end of the container. We
-  // intentionally invalidate layout here to support resizing the container as
-  // the drag exceeds its bounds.
-  if (drag_handler.HandleDraggedTabsIntoPosition(*collection_node_, target) ||
-      !target) {
-    host_view_->InvalidateLayout();
+  switch (target.action) {
+    case DragDropTarget::Action::kChildInto: {
+      // Group header drags can't nest-into another tab; fall back to
+      // insert-before so they continue reordering normally.
+      const auto* dc = drag_handler.GetDragController();
+      if (dc && dc->GetSessionData().group_header_drag_data_.has_value()) {
+        drag_handler.SetPendingNestIntoTarget(std::nullopt);
+        if (drag_handler.HandleDraggedTabsIntoPosition(*collection_node_,
+                                                       target.node) ||
+            !target.node) {
+          host_view_->InvalidateLayout();
+        }
+        break;
+      }
+      // Record pending target; suppress the per-frame model move (actual
+      // Nest commits on drag end in StoppedDragging).
+      std::optional<tabs::TabHandle> target_handle;
+      if (target.node) {
+        const auto node_data = target.node->GetNodeData();
+        if (const auto* tab =
+                std::get_if<const tabs::TabInterface*>(&node_data)) {
+          if (*tab) {
+            target_handle = (*tab)->GetHandle();
+          }
+        }
+      }
+      drag_handler.SetPendingNestIntoTarget(target_handle);
+      break;
+    }
+    case DragDropTarget::Action::kInsertBefore:
+      // Left the kChildInto zone; clear any pending nest target.
+      drag_handler.SetPendingNestIntoTarget(std::nullopt);
+      if (drag_handler.HandleDraggedTabsIntoPosition(*collection_node_,
+                                                     target.node) ||
+          !target.node) {
+        host_view_->InvalidateLayout();
+      }
+      break;
   }
 }
 
-const TabCollectionNode* VerticalDraggedTabsContainer::GetTargetForTabDrag(
+VerticalDraggedTabsContainer::DragDropTarget
+VerticalDraggedTabsContainer::GetTargetForTabDrag(
     const views::ProposedLayout& layout,
-    const gfx::Rect& dragged_tab_bounds) const {
+    const gfx::Rect& dragged_tab_bounds,
+    const std::set<int>& dragged_idxs) const {
   // Whether the loop below saw the dragged view(s). Once the dragged view
   // was seen, the bounds of the later views must be adjusted to discount the
   // size of the dragged view.
@@ -610,6 +736,32 @@ const TabCollectionNode* VerticalDragged
       continue;
     }
 
+    // CENTER ZONE: does the dragged tab's center sit in the middle 80%?
+    // is_after_dragged_views adjustment is NOT applied to the center-zone test.
+    // The adjustment compensates for dragged views being removed from layout in
+    // the "insert position" sense. Center-zone is a containment test on the
+    // literal screen position of the candidate, so it uses raw
+    // child_layout.bounds.
+    const float edge_band = (1.0f - kChildIntoZoneFraction) / 2.0f;
+    const int center_top =
+        child_layout.bounds.y() + child_layout.bounds.height() * edge_band;
+    const int center_bottom = child_layout.bounds.y() +
+                              child_layout.bounds.height() * (1.0f - edge_band);
+    const int probe = dragged_tab_bounds.CenterPoint().y();
+
+    if (probe >= center_top && probe < center_bottom) {
+      if (const auto* tab = TabFromNode(child_node)) {
+        // Cursor is in the child-into zone for THIS candidate.
+        const int candidate_idx =
+            collection_node_->GetController()->model()->GetIndexOfTab(tab);
+        if (collection_node_->GetController()->model()->AllCanNestInto(
+                dragged_idxs, candidate_idx)) {
+          return {child_node, DragDropTarget::Action::kChildInto};
+        }
+      }
+      // CanNestInto rejected, fall through to edge logic for this candidate.
+    }
+
     // If horizontal dragging is supported, then only look for the row that the
     // drag falls on. Once the row is found, delegate to
     // `GetTargetForTabDragInRow` to find the exact node within the row.
@@ -619,8 +771,9 @@ const TabCollectionNode* VerticalDragged
       }
 
       const int row_y = child_layout.bounds.y();
-      return GetTargetForTabDragInRow(layout, dragged_tab_bounds, row_y, i,
-                                      is_after_dragged_views);
+      return {GetTargetForTabDragInRow(layout, dragged_tab_bounds, row_y, i,
+                                       is_after_dragged_views),
+              DragDropTarget::Action::kInsertBefore};
     }
 
     // If the dragged view was already seen, then discount its size from
@@ -633,10 +786,10 @@ const TabCollectionNode* VerticalDragged
     }
 
     if (dragged_tab_bounds.y() < adjusted_bounds.CenterPoint().y()) {
-      return child_node;
+      return {child_node, DragDropTarget::Action::kInsertBefore};
     }
   }
-  return nullptr;
+  return {};
 }
 
 const TabCollectionNode* VerticalDraggedTabsContainer::GetTargetForTabDragInRow(
--- a/chrome/browser/ui/views/tabs/vertical/vertical_dragged_tabs_container.h
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_dragged_tabs_container.h
@@ -5,10 +5,13 @@
 #ifndef CHROME_BROWSER_UI_VIEWS_TABS_VERTICAL_VERTICAL_DRAGGED_TABS_CONTAINER_H_
 #define CHROME_BROWSER_UI_VIEWS_TABS_VERTICAL_VERTICAL_DRAGGED_TABS_CONTAINER_H_
 
+#include <set>
+
 #include "base/callback_list.h"
 #include "base/containers/flat_map.h"
 #include "base/memory/raw_ref.h"
 #include "base/scoped_observation.h"
+#include "chrome/browser/ui/views/tabs/dragging/tab_drag_context.h"
 #include "chrome/browser/ui/views/tabs/dragging/tab_drag_target.h"
 #include "chrome/browser/ui/views/tabs/vertical/tab_drag_scroll_handler.h"
 #include "ui/gfx/animation/animation_delegate.h"
@@ -105,6 +108,8 @@ class VerticalDraggedTabsContainer : pub
   std::optional<DraggedViewVisualData> GetVisualDataForDraggedView(
       const views::View& view) const;
 
+  std::set<int> GetDraggedIndices() const;
+
   // Returns whether the two rects overlap by at least the provided minimums for
   // each axis.
   // E.g, if only `min_x_overlap` is provided, then this will return true
@@ -196,12 +201,21 @@ class VerticalDraggedTabsContainer : pub
 
   std::vector<const views::View*> GetDraggingViews() const;
 
+  struct DragDropTarget {
+    enum class Action {
+      kInsertBefore,
+      kChildInto,
+    };
+    raw_ptr<const TabCollectionNode> node;  // nullptr = append at end
+    Action action = Action::kInsertBefore;
+  };
+
   // Finds the node that the tab drag occurs *before*.
   // A null value indicates that the drag is not before any node (i.e., at
   // the end of the this container).
-  const TabCollectionNode* GetTargetForTabDrag(
-      const views::ProposedLayout& layout,
-      const gfx::Rect& dragged_tab_bounds) const;
+  DragDropTarget GetTargetForTabDrag(const views::ProposedLayout& layout,
+                                     const gfx::Rect& dragged_tab_bounds,
+                                     const std::set<int>& dragged_idxs) const;
 
   // This is a helper method for grid-like layout (i.e. when horizontal
   // dragging in the vertical tab strip is supported), which finds the node that
--- /dev/null
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_nested_tab_view.cc
@@ -0,0 +1,226 @@
+#include "chrome/browser/ui/views/tabs/vertical/vertical_nested_tab_view.h"
+
+#include "base/functional/bind.h"
+#include "chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h"
+#include "chrome/browser/ui/views/tabs/vertical/tab_collection_node.h"
+#include "chrome/browser/ui/views/tabs/vertical/vertical_split_tab_view.h"
+#include "chrome/browser/ui/views/tabs/vertical/vertical_tab_drag_handler.h"
+#include "chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h"
+#include "components/tabs/public/nested_tab_collection.h"
+#include "components/tabs/public/nested_tab_data.h"
+#include "ui/base/metadata/metadata_impl_macros.h"
+#include "ui/compositor/layer.h"
+#include "ui/gfx/geometry/rect.h"
+#include "ui/views/controls/scroll_view.h"
+#include "ui/views/layout/delegating_layout_manager.h"
+#include "ui/views/layout/proposed_layout.h"
+#include "ui/views/view_utils.h"
+
+namespace {
+constexpr int kTabVerticalPadding = 2;
+
+const tabs::NestedTabCollection* GetNestedTabCollectionFromNode(
+    TabCollectionNode* node) {
+  CHECK(node);
+  return static_cast<const tabs::NestedTabCollection*>(
+      std::get<const tabs::TabCollection*>(node->GetNodeData()));
+}
+}  // namespace
+
+VerticalNestedTabView::VerticalNestedTabView(TabCollectionNode* collection_node)
+    : VerticalDraggedTabsContainer(static_cast<views::View&>(*this),
+                                   collection_node,
+                                   DragAxes::kVerticalOnly,
+                                   DragLayout::kVertical),
+      collection_node_(collection_node),
+      layout_manager_(*SetLayoutManager(
+          std::make_unique<TabCollectionAnimatingLayoutManager>(
+              std::make_unique<views::DelegatingLayoutManager>(this),
+              /*delegate=*/*this,
+              /*animation_axis=*/
+              TabCollectionAnimatingLayoutManager::AnimationAxis::kVertical,
+              /*animate_host_size=*/true))) {
+  collection_node->set_remove_child_from_node(base::BindRepeating(
+      &TabCollectionAnimatingLayoutManager::AnimateAndDestroyChildView,
+      base::Unretained(&layout_manager_.get())));
+  collection_node->set_attach_child_to_node(base::BindRepeating(
+      &VerticalNestedTabView::AttachChildView, base::Unretained(this)));
+  collection_node->set_detach_child_from_node(base::BindRepeating(
+      &VerticalNestedTabView::DetachChildView, base::Unretained(this)));
+
+  node_destroyed_subscription_ =
+      collection_node_->RegisterWillDestroyCallback(base::BindOnce(
+          &VerticalNestedTabView::ResetCollectionNode, base::Unretained(this)));
+  data_changed_subscription_ =
+      collection_node_->RegisterDataChangedCallback(base::BindRepeating(
+          &VerticalNestedTabView::OnDataChanged, base::Unretained(this)));
+}
+
+VerticalNestedTabView::~VerticalNestedTabView() = default;
+
+views::ProposedLayout VerticalNestedTabView::CalculateProposedLayout(
+    const views::SizeBounds& size_bounds) const {
+  views::ProposedLayout layouts;
+  int width = 0;
+  int height = 0;
+
+  const std::vector<views::View*> children =
+      collection_node_ ? collection_node_->GetDirectChildren()
+                       : std::vector<views::View*>();
+
+  // Owner (first child) renders flush at the top; subsequent children get the
+  // NESTED indent. When collapsed, the owner stays at full bounds (it's the
+  // visible "header"); non-owner children animate to zero height.
+  for (size_t i = 0; i < children.size(); ++i) {
+    auto* child = children[i];
+    const bool is_owner = (i == 0);
+
+    const int strip_indent =
+        GetTabStripCollapseState() ==
+                tabs::VerticalTabStripCollapseState::kExpanded
+            ? kNestedIndent
+            : 0;
+    const int x = is_owner ? 0 : strip_indent;
+    const int avail_w =
+        size_bounds.width().is_bounded() ? size_bounds.width().value() - x : 0;
+
+    gfx::Rect bounds;
+    if (is_collapsed_ && !is_owner) {
+      // Target: zero-height row stacked under owner. Bounds interpolate from
+      // each child's current rect, producing a slide-up-and-shrink animation.
+      bounds = gfx::Rect(x, height, avail_w, 0);
+    } else {
+      views::SizeBounds child_size_bounds(
+          size_bounds.width().is_bounded() ? avail_w : size_bounds.width(), {});
+      bounds = gfx::Rect(child->GetPreferredSize(child_size_bounds));
+      bounds.set_x(x);
+      bounds.set_y(height);
+      if (size_bounds.width().is_bounded()) {
+        bounds.set_width(avail_w);
+      }
+      height += bounds.height() + kTabVerticalPadding;
+    }
+
+    layouts.child_layouts.emplace_back(child, child->GetVisible(), bounds);
+    width = std::max(width, bounds.width() + bounds.x());
+  }
+  if (!children.empty()) {
+    height -= kTabVerticalPadding;
+  }
+
+  layouts.host_size = gfx::Size(width, height);
+  return layouts;
+}
+
+bool VerticalNestedTabView::IsDragging() const {
+  if (!collection_node_ || !collection_node_->GetController()) {
+    return false;
+  }
+  return GetDragHandler().IsDragging();
+}
+
+bool VerticalNestedTabView::IsViewDragging(
+    const views::View& child_view) const {
+  if (!collection_node_ || !collection_node_->GetController()) {
+    return false;
+  }
+  return GetDragHandler().IsViewDragging(child_view);
+}
+
+bool VerticalNestedTabView::ShouldAnimateOpacityForAddAndRemove(
+    const views::View& child_view) const {
+  // Only animate opacity for tab views.
+  return views::IsViewClass<VerticalTabView>(&child_view);
+}
+
+bool VerticalNestedTabView::ShouldSnapToTarget(
+    const views::View& child_view) const {
+  return views::IsViewClass<VerticalSplitTabView>(&child_view) ||
+         views::IsViewClass<VerticalNestedTabView>(&child_view);
+}
+
+views::ScrollView* VerticalNestedTabView::GetScrollViewForContainer() const {
+  // GetScrollViewForContents inspects pointers only; const-incorrect API
+  views::View* ancestor =
+      const_cast<views::View*>(static_cast<const views::View*>(this));
+  while (ancestor) {
+    if (auto* scroll_view =
+            views::ScrollView::GetScrollViewForContents(ancestor)) {
+      return scroll_view;
+    }
+    ancestor = ancestor->parent();
+  }
+  return nullptr;
+}
+
+void VerticalNestedTabView::UpdateTargetLayoutForDrag(
+    const std::vector<const views::View*>& views_to_snap) {
+  layout_manager_->ResetViewsToTargetLayout(views_to_snap);
+}
+
+const views::ProposedLayout& VerticalNestedTabView::GetLayoutForDrag() const {
+  return layout_manager_->target_layout();
+}
+
+const TabCollectionNode* VerticalNestedTabView::GetCollectionNodeFromView(
+    const views::View& view) const {
+  if (auto* tab_view = views::AsViewClass<VerticalTabView>(&view)) {
+    return tab_view->collection_node();
+  } else if (auto* split_tab_view =
+                 views::AsViewClass<VerticalSplitTabView>(&view)) {
+    return split_tab_view->collection_node();
+  } else if (auto* nested_tab_view =
+                 views::AsViewClass<VerticalNestedTabView>(&view)) {
+    return nested_tab_view->collection_node();
+  }
+  return nullptr;
+}
+
+void VerticalNestedTabView::AttachChildView(
+    std::unique_ptr<views::View> child_view,
+    const gfx::Rect& previous_bounds_in_screen) {
+  layout_manager_->AnimateAndReparentView(std::move(child_view),
+                                          previous_bounds_in_screen);
+}
+
+std::unique_ptr<views::View> VerticalNestedTabView::DetachChildView(
+    views::View* child_view) {
+  return RemoveChildViewT(child_view);
+}
+
+void VerticalNestedTabView::ResetCollectionNode() {
+  collection_node_ = nullptr;
+}
+
+void VerticalNestedTabView::OnDataChanged() {
+  auto coll = GetNestedTabCollectionFromNode(collection_node_);
+  bool new_is_collapsed = coll->data()->visual_data()->IsCollapsed();
+  if (new_is_collapsed != is_collapsed_) {
+    is_collapsed_ = new_is_collapsed;
+    if (!is_collapsed_) {
+      UpdateChildVisibilityForCollapseState(false);
+    }
+    InvalidateLayout();
+  }
+}
+
+void VerticalNestedTabView::UpdateChildVisibilityForCollapseState(
+    bool collapsed) {
+  if (!collection_node_) {
+    return;
+  }
+  const auto children = collection_node_->GetDirectChildren();
+  // Owner (index 0) is the NESTED's visible "header" and stays shown.
+  for (size_t i = 1; i < children.size(); ++i) {
+    children[i]->SetVisible(!collapsed);
+  }
+}
+
+void VerticalNestedTabView::OnAnimationEnded() {
+  if (is_collapsed_) {
+    UpdateChildVisibilityForCollapseState(true);
+  }
+}
+
+BEGIN_METADATA(VerticalNestedTabView)
+END_METADATA
--- /dev/null
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_nested_tab_view.h
@@ -0,0 +1,75 @@
+#ifndef CHROME_BROWSER_UI_VIEWS_TABS_VERTICAL_VERTICAL_NESTED_TAB_VIEW_H_
+#define CHROME_BROWSER_UI_VIEWS_TABS_VERTICAL_VERTICAL_NESTED_TAB_VIEW_H_
+
+#include <memory>
+
+#include "base/callback_list.h"
+#include "base/memory/raw_ptr.h"
+#include "base/memory/raw_ref.h"
+#include "chrome/browser/ui/views/tabs/vertical/tab_collection_animating_layout_manager.h"
+#include "chrome/browser/ui/views/tabs/vertical/vertical_dragged_tabs_container.h"
+#include "ui/base/metadata/metadata_header_macros.h"
+#include "ui/views/controls/scroll_view.h"
+#include "ui/views/layout/delegating_layout_manager.h"
+#include "ui/views/layout/proposed_layout.h"
+#include "ui/views/view.h"
+
+class TabCollectionNode;
+
+class VerticalNestedTabView
+    : public views::View,
+      public views::LayoutDelegate,
+      public VerticalDraggedTabsContainer,
+      public TabCollectionAnimatingLayoutManager::Delegate {
+  METADATA_HEADER(VerticalNestedTabView, views::View)
+
+ public:
+  // Horizontal indent applied to direct children. Sub-NESTED views stack their
+  // own indent on top of this one via the view hierarchy, so total depth N
+  // produces N * kNestedIndent of inset.
+  static constexpr int kNestedIndent = 16;
+
+  explicit VerticalNestedTabView(TabCollectionNode* collection_node);
+  ~VerticalNestedTabView() override;
+  VerticalNestedTabView(const VerticalNestedTabView&) = delete;
+  VerticalNestedTabView& operator=(const VerticalNestedTabView&) = delete;
+
+  // views::LayoutDelegate:
+  views::ProposedLayout CalculateProposedLayout(
+      const views::SizeBounds& size_bounds) const override;
+
+  const TabCollectionNode* collection_node() const { return collection_node_; }
+
+ private:
+  bool IsDragging() const override;
+  bool IsViewDragging(const views::View& child_view) const override;
+  bool ShouldAnimateOpacityForAddAndRemove(
+      const views::View& child_view) const override;
+  bool ShouldSnapToTarget(const views::View& child_view) const override;
+
+  views::ScrollView* GetScrollViewForContainer() const override;
+  void UpdateTargetLayoutForDrag(
+      const std::vector<const views::View*>& views_to_snap) override;
+  const views::ProposedLayout& GetLayoutForDrag() const override;
+  const TabCollectionNode* GetCollectionNodeFromView(
+      const views::View& view) const override;
+
+  void AttachChildView(std::unique_ptr<views::View> child_view,
+                       const gfx::Rect& previous_bounds_in_screen);
+  std::unique_ptr<views::View> DetachChildView(views::View* child_view);
+  void ResetCollectionNode();
+
+  void OnDataChanged();
+
+  void UpdateChildVisibilityForCollapseState(bool collapsed);
+
+  void OnAnimationEnded() override;
+
+  bool is_collapsed_ = false;
+  raw_ptr<TabCollectionNode> collection_node_;
+  raw_ref<TabCollectionAnimatingLayoutManager> layout_manager_;
+  base::CallbackListSubscription node_destroyed_subscription_;
+  base::CallbackListSubscription data_changed_subscription_;
+};
+
+#endif  // CHROME_BROWSER_UI_VIEWS_TABS_VERTICAL_VERTICAL_NESTED_TAB_VIEW_H_
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_drag_handler.cc
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_drag_handler.cc
@@ -7,8 +7,10 @@
 #include <algorithm>
 #include <memory>
 #include <optional>
+#include <ranges>
 
 #include "base/check_deref.h"
+#include "base/check_op.h"
 #include "base/functional/bind.h"
 #include "base/notreached.h"
 #include "base/types/to_address.h"
@@ -21,9 +23,13 @@
 #include "chrome/browser/ui/views/tabs/tab_slot_view.h"
 #include "chrome/browser/ui/views/tabs/tab_strip_types.h"
 #include "chrome/browser/ui/views/tabs/vertical/tab_collection_node.h"
+#include "chrome/browser/ui/views/tabs/vertical/vertical_nested_tab_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_link_drop_handler.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_controller.h"
+#include "chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h"
 #include "components/tab_groups/tab_group_id.h"
+#include "components/tabs/public/nested_tab_collection.h"
+#include "components/tabs/public/nested_tab_data.h"
 #include "components/tabs/public/split_tab_collection.h"
 #include "components/tabs/public/tab_collection.h"
 #include "components/tabs/public/tab_group.h"
@@ -70,6 +76,8 @@ class VerticalTabSlotView : public TabSl
         return ViewType::kTab;
       case TabCollectionNode::Type::GROUP:
         return ViewType::kTabGroupHeader;
+      case TabCollectionNode::Type::NESTED:
+        return ViewType::kNestedContainer;
       default:
         NOTREACHED();
     }
@@ -107,6 +115,11 @@ gfx::Range GetContainerRange(const TabCo
   switch (container_node.type()) {
     case TabCollectionNode::Type::GROUP:
       return TabGroupDataFromNode(container_node).ListTabs();
+    case TabCollectionNode::Type::NESTED: {
+      const auto* nested = static_cast<const tabs::NestedTabCollection*>(
+          std::get<const tabs::TabCollection*>(container_node.GetNodeData()));
+      return nested->data()->GetIndexRange();
+    }
     case TabCollectionNode::Type::PINNED:
       return gfx::Range(0, tab_strip_model.IndexOfFirstNonPinnedTab());
     case TabCollectionNode::Type::UNPINNED:
@@ -128,6 +141,66 @@ std::optional<tab_groups::TabGroupId> Ge
   return std::nullopt;
 }
 
+// Count of moving tabs sitting strictly before `target_index` when a whole
+// NESTED is being dragged. `tab_drag_data_` only holds the owner slot for
+// NESTED owner drags, so the naive count undercounts; this returns the
+// correct value or nullopt if the dragged_nested has no nesting data.
+std::optional<int> NumNestedTabsBeforeTarget(
+    const TabStripModel& tab_strip_model,
+    const nested_tabs::NestedTabId& dragged_nested,
+    int target_index) {
+  const auto* ntd = tab_strip_model.GetNestingData(dragged_nested);
+  if (!ntd) {
+    return std::nullopt;
+  }
+  const gfx::Range r = ntd->GetIndexRange();
+  const int lo = static_cast<int>(r.GetMin());
+  const int hi = static_cast<int>(r.GetMax());
+  if (hi <= target_index) {
+    return hi - lo;
+  }
+  if (lo < target_index) {
+    return target_index - lo;
+  }
+  return 0;
+}
+
+// When dragging a NESTED currently inside a group, the cursor passing over
+// the NESTED can drop target_group_id and kick the NESTED out. Re-anchor to
+// the NESTED's current group when the destination is still inside that
+// group's flat range; otherwise the user is intentionally dragging out.
+std::optional<tab_groups::TabGroupId> StickyAnchorGroupForNestedDrag(
+    const TabStripModel& tab_strip_model,
+    const nested_tabs::NestedTabId& dragged_nested,
+    std::optional<tab_groups::TabGroupId> explicit_target_group,
+    int target_index) {
+  if (explicit_target_group.has_value()) {
+    return explicit_target_group;
+  }
+  const auto* ntd = tab_strip_model.GetNestingData(dragged_nested);
+  if (!ntd) {
+    return std::nullopt;
+  }
+  const tabs::TabInterface* owner = ntd->owner_tab().Get();
+  if (!owner) {
+    return std::nullopt;
+  }
+  auto current_group = owner->GetGroup();
+  if (!current_group) {
+    return std::nullopt;
+  }
+  const auto* tg = tab_strip_model.group_model()->GetTabGroup(*current_group);
+  if (!tg) {
+    return std::nullopt;
+  }
+  const gfx::Range group_range = tg->ListTabs();
+  if (target_index >= static_cast<int>(group_range.GetMin()) &&
+      target_index <= static_cast<int>(group_range.GetMax())) {
+    return current_group;
+  }
+  return std::nullopt;
+}
+
 // Returns the index for insertion before the given collection node.
 int GetInsertionIndexForNode(const TabCollectionNode& node,
                              const TabStripModel& tab_strip_model) {
@@ -148,6 +221,12 @@ int GetInsertionIndexForNode(const TabCo
     const auto* split_data = split_collection->data();
     gfx::Range range = split_data->GetIndexRange();
     return range.start();
+  } else if (node.type() == TabCollectionNode::Type::NESTED) {
+    const auto* nested_collection =
+        static_cast<const tabs::NestedTabCollection*>(
+            std::get<const tabs::TabCollection*>(node.GetNodeData()));
+    return tab_strip_model.GetIndexOfTab(
+        nested_collection->data()->owner_tab().Get());
   }
   NOTREACHED() << "Unsupported Tab Collection Node";
 }
@@ -173,9 +252,16 @@ void VerticalTabDragHandlerImpl::Initial
 
   DragInitData drag_init_data;
   switch (node.type()) {
-    case TabCollectionNode::Type::TAB:
-      drag_init_data = GetDragInitDataForTabDrag(node);
+    case TabCollectionNode::Type::TAB: {
+      bool tab_has_nested =
+          tab_strip_model_
+              ->GetOwnedNestedId(
+                  std::get<const tabs::TabInterface*>(node.GetNodeData()))
+              .has_value();
+      drag_init_data = tab_has_nested ? GetDragInitDataForNestedOwnerDrag(node)
+                                      : GetDragInitDataForTabDrag(node);
       break;
+    }
     case TabCollectionNode::Type::GROUP:
       drag_init_data = GetDragInitDataForGroupHeaderDrag(node);
       break;
@@ -282,6 +368,32 @@ VerticalTabDragHandlerImpl::GetDragInitD
   return drag_init_data;
 }
 
+VerticalTabDragHandlerImpl::DragInitData
+VerticalTabDragHandlerImpl::GetDragInitDataForNestedOwnerDrag(
+    TabCollectionNode& source_node) {
+  DragInitData drag_init_data;
+  drag_init_data.source_dragged_view = &GetOrCreateSlotViewForNode(source_node);
+  drag_init_data.dragged_views.push_back(drag_init_data.source_dragged_view);
+
+  const auto* owner_tab =
+      std::get<const tabs::TabInterface*>(source_node.GetNodeData());
+  auto nested_id = tab_strip_model_->GetOwnedNestedId(owner_tab);
+  CHECK(nested_id);
+
+  // Tag the owner's slot view with the NESTED id so TabDragController::Init
+  // routes commit-time dispatch through MoveNestedTo. Descendants are
+  // deliberately NOT added to `tab_drag_data_` (mainline TabDragController
+  // doesn't tolerate non-tab/non-group-header entries); visual lockstep
+  // comes from BuildDragLayout rebasing the owner's view onto the enclosing
+  // VerticalNestedTabView.
+  size_t owner_index = tab_strip_model_->GetIndexOfTab(owner_tab);
+  drag_init_data.list_selection_model.AddIndexToSelection(owner_index);
+  drag_init_data.list_selection_model.set_active(owner_index);
+  drag_init_data.source_dragged_view->SetNested(nested_id);
+
+  return drag_init_data;
+}
+
 std::map<tab_groups::TabGroupId, TabSlotView*>
 VerticalTabDragHandlerImpl::GetFullySelectedGroups(
     const std::vector<tabs::TabInterface*>& selected_tabs) {
@@ -355,6 +467,15 @@ void VerticalTabDragHandlerImpl::HandleD
 
   int target_index;
   std::optional<tab_groups::TabGroupId> target_group_id = std::nullopt;
+  std::optional<nested_tabs::NestedTabId> target_nested_id = std::nullopt;
+  if (node.type() == TabCollectionNode::Type::NESTED) {
+    // Hint required, else MoveSelectedTabsTo would resolve dst to unpinned
+    // and the walk would overshoot while still inside the NESTED.
+    const auto* nested_collection =
+        static_cast<const tabs::NestedTabCollection*>(
+            std::get<const tabs::TabCollection*>(node.GetNodeData()));
+    target_nested_id = nested_collection->GetNestedTabId();
+  }
   if (node.type() == TabCollectionNode::Type::GROUP) {
     // If dragging into a group, then either put the dragged tabs into the
     // start/end if the source dragged tab is before/after the group, or move
@@ -392,10 +513,31 @@ void VerticalTabDragHandlerImpl::HandleD
       ++num_dragged_tabs_before_target;
     }
   }
+
+  // For NESTED drags `tab_drag_data_` only holds the owner slot; replace
+  // the owner-only count with the actual count of NESTED tabs before
+  // `target_index`.
+  if (auto dragged_nested = GetDraggingNestedId()) {
+    if (auto n = NumNestedTabsBeforeTarget(*tab_strip_model_,
+                                           *dragged_nested, target_index)) {
+      num_dragged_tabs_before_target = *n;
+    }
+  }
+
   target_index = target_index - num_dragged_tabs_before_target;
   target_index = std::clamp(target_index, 0, tab_strip_model_->count() - 1);
 
-  tab_strip_model_->MoveSelectedTabsTo(target_index, target_group_id);
+  if (auto dragged_nested = GetDraggingNestedId()) {
+    tab_strip_model_->MoveNestedTo(
+        *dragged_nested, target_index,
+        StickyAnchorGroupForNestedDrag(*tab_strip_model_, *dragged_nested,
+                                       target_group_id, target_index));
+  } else if (auto dragged_group = GetDraggingGroupHeaderId()) {
+    tab_strip_model_->MoveGroupTo(*dragged_group, target_index);
+  } else {
+    tab_strip_model_->MoveSelectedTabsTo(target_index, target_group_id,
+                                         target_nested_id);
+  }
 }
 
 TabDragContext* VerticalTabDragHandlerImpl::GetDragContext() {
@@ -459,6 +601,16 @@ views::View* VerticalTabDragHandlerImpl:
     const auto* tab = std::get<const tabs::TabInterface*>(node.GetNodeData());
     CHECK(tab);
 
+    // Owner of the actively-dragged NESTED redirects to the wrapper view
+    // (parent of the owner's VerticalTabView) so layer transforms apply to
+    // the whole unit. Mirrors the group redirect below.
+    if (auto nested_id = tab_strip_model_->GetOwnedNestedId(tab);
+        nested_id && GetDraggingNestedId() == nested_id) {
+      views::View* wrapper = node.view()->parent();
+      CHECK(wrapper);
+      return wrapper;
+    }
+
     if (auto group_id = tab->GetGroup();
         group_id && drag_controller_->GetSessionData().dragging_groups.contains(
                         *group_id)) {
@@ -716,6 +868,47 @@ void VerticalTabDragHandlerImpl::Stopped
     dragged_view->ClearProperty(kOffsetAtTabDragStart);
   }
 
+  // Commit a pending kChildInto: per-frame model moves were suppressed
+  // while a target was pending, so Nest the dragged tabs into the target
+  // here. For NESTED owner drags pass only the owner index. MoveTabToNested
+  // (called inside Nest) moves the whole NESTED as a unit when the moved tab
+  // owns one, preserving nest-in-nest structure instead of flattening.
+  if (drag_controller_ && pending_nest_into_target_.has_value()) {
+    const tabs::TabInterface* target_tab = pending_nest_into_target_->Get();
+    if (target_tab) {
+      const int parent_idx = tab_strip_model_->GetIndexOfTab(target_tab);
+      std::vector<int> children;
+      if (auto nested = GetDraggingNestedId()) {
+        if (const auto* ntd = tab_strip_model_->GetNestingData(*nested)) {
+          const tabs::TabInterface* owner = ntd->owner_tab().Get();
+          const int owner_idx =
+              owner ? tab_strip_model_->GetIndexOfTab(owner) : TabStripModel::kNoTab;
+          if (owner_idx != TabStripModel::kNoTab && owner_idx != parent_idx) {
+            children.push_back(owner_idx);
+          }
+        }
+      } else {
+        for (const TabDragData& tdd :
+             drag_controller_->GetSessionData().tab_drag_data_) {
+          if (!tdd.contents) {
+            continue;
+          }
+          const int idx =
+              tab_strip_model_->GetIndexOfWebContents(tdd.contents);
+          if (idx != TabStripModel::kNoTab && idx != parent_idx) {
+            children.push_back(idx);
+          }
+        }
+      }
+      if (!children.empty() && parent_idx != TabStripModel::kNoTab) {
+        tab_strip_model_->Nest(parent_idx, std::move(children));
+      }
+    }
+  }
+  // Clear via the setter so the drop-target highlight on the previously
+  // hovered tab gets removed.
+  SetPendingNestIntoTarget(std::nullopt);
+
   if (!drag_controller_) {
     return;
   }
@@ -793,6 +986,9 @@ TabSlotView& VerticalTabDragHandlerImpl:
       case TabCollectionNode::Type::GROUP:
         slot_view.SetGroup(TabGroupDataFromNode(node).id());
         break;
+      case TabCollectionNode::Type::NESTED:
+        // No group/split to propagate; just set bounds (done below).
+        break;
       default:
         NOTREACHED();
     }
@@ -828,6 +1024,38 @@ void VerticalTabDragHandlerImpl::OnNodeW
 
 void VerticalTabDragHandlerImpl::ResetDragState() {
   drag_controller_.reset();
+  SetPendingNestIntoTarget(std::nullopt);
+}
+
+void VerticalTabDragHandlerImpl::SetPendingNestIntoTarget(
+    std::optional<tabs::TabHandle> target) {
+  if (target == pending_nest_into_target_) {
+    return;
+  }
+  // Toggle the drop-target highlight on the affected tab views.
+  auto set_highlight = [&](std::optional<tabs::TabHandle> handle, bool on) {
+    if (!handle.has_value()) {
+      return;
+    }
+    tabs::TabInterface* tab = handle->Get();
+    if (!tab) {
+      return;
+    }
+    content::WebContents* contents = tab->GetContents();
+    if (!contents) {
+      return;
+    }
+    TabCollectionNode* node = GetNodeForContents(contents);
+    if (!node || !node->view()) {
+      return;
+    }
+    if (auto* tab_view = views::AsViewClass<VerticalTabView>(node->view())) {
+      tab_view->SetIsDropTarget(on);
+    }
+  };
+  set_highlight(pending_nest_into_target_, false);
+  pending_nest_into_target_ = target;
+  set_highlight(pending_nest_into_target_, true);
 }
 
 bool VerticalTabDragHandlerImpl::HandleDraggedTabsIntoPosition(
@@ -872,6 +1100,17 @@ bool VerticalTabDragHandlerImpl::HandleD
       ++num_dragged_tabs_before_target;
     }
   }
+
+  // For NESTED owner drags `tab_drag_data_` only holds the owner slot; the
+  // adjustment above undercounts by `sz - 1` so fix it before computing
+  // `adjusted_target_index`.
+  if (auto dragged_nested = GetDraggingNestedId()) {
+    if (auto n = NumNestedTabsBeforeTarget(*tab_strip_model_,
+                                           *dragged_nested, target_index)) {
+      num_dragged_tabs_before_target = *n;
+    }
+  }
+
   int adjusted_target_index =
       std::max(0, target_index - num_dragged_tabs_before_target);
 
@@ -886,11 +1125,32 @@ bool VerticalTabDragHandlerImpl::HandleD
     return false;
   }
 
-  if (auto dragged_group = GetDraggingGroupHeaderId()) {
+  std::optional<nested_tabs::NestedTabId> target_nested_id = std::nullopt;
+  if (container_node.type() == TabCollectionNode::Type::NESTED) {
+    const auto* nested = static_cast<const tabs::NestedTabCollection*>(
+        std::get<const tabs::TabCollection*>(container_node.GetNodeData()));
+    // Target on the NESTED's owner means "drop at the parent-level boundary
+    // before the NESTED" rather than "land inside at the owner's slot";
+    // leave target_nested_id unset so dst resolves at the parent.
+    const bool target_is_nested_owner =
+        target && target->type() == TabCollectionNode::Type::TAB &&
+        std::get<const tabs::TabInterface*>(target->GetNodeData()) ==
+            nested->data()->owner_tab().Get();
+    if (!target_is_nested_owner) {
+      target_nested_id = nested->GetNestedTabId();
+    }
+  }
+
+  if (auto dragged_nested = GetDraggingNestedId()) {
+    tab_strip_model_->MoveNestedTo(
+        *dragged_nested, adjusted_target_index,
+        StickyAnchorGroupForNestedDrag(*tab_strip_model_, *dragged_nested,
+                                       target_group_id, target_index));
+  } else if (auto dragged_group = GetDraggingGroupHeaderId()) {
     tab_strip_model_->MoveGroupTo(*dragged_group, adjusted_target_index);
   } else {
-    tab_strip_model_->MoveSelectedTabsTo(adjusted_target_index,
-                                         target_group_id);
+    tab_strip_model_->MoveSelectedTabsTo(adjusted_target_index, target_group_id,
+                                         target_nested_id);
   }
   return true;
 }
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_drag_handler.h
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_drag_handler.h
@@ -6,6 +6,7 @@
 #define CHROME_BROWSER_UI_VIEWS_TABS_VERTICAL_VERTICAL_TAB_DRAG_HANDLER_H_
 
 #include <memory>
+#include <optional>
 #include <set>
 #include <vector>
 
@@ -19,6 +20,7 @@
 #include "chrome/browser/ui/views/tabs/dragging/tab_drag_controller.h"
 #include "chrome/browser/ui/views/tabs/tab_strip_types.h"
 #include "components/tab_groups/tab_group_id.h"
+#include "components/tabs/public/tab_interface.h"
 #include "content/public/browser/web_contents.h"
 #include "ui/base/metadata/metadata_header_macros.h"
 #include "ui/events/event.h"
@@ -101,6 +103,14 @@ class VerticalTabDragHandler {
   // Called before a tab is removed from the tabstrip. Cancels the current drag
   // if the tab is one of the dragged tabs.
   virtual void OnTabWillBeRemoved(content::WebContents* contents) = 0;
+
+  virtual const TabDragController* GetDragController() const = 0;
+
+  // Records the tab the drag is currently hovering inside (kChildInto zone)
+  // so per-frame insert-before commits are suppressed; on drop the impl
+  // calls `Nest`. Pass nullopt to clear.
+  virtual void SetPendingNestIntoTarget(
+      std::optional<tabs::TabHandle> target) = 0;
 };
 
 // Implements a minimal drag context to interact with the central
@@ -155,6 +165,9 @@ class VerticalTabDragHandlerImpl : publi
       const tab_groups::TabGroupId& group_id) override;
   TabStripModel* GetTabStripModel() override;
   TabDragController* GetDragController() override;
+  const TabDragController* GetDragController() const override {
+      return drag_controller_ ? drag_controller_.get() : nullptr;
+  }
   void OwnDragController(
       std::unique_ptr<TabDragController> controller) override;
   std::unique_ptr<TabDragController> ReleaseDragController() override;
@@ -195,6 +208,7 @@ class VerticalTabDragHandlerImpl : publi
   DragInitData GetDragInitDataForTabDrag(TabCollectionNode& source_node);
   DragInitData GetDragInitDataForGroupHeaderDrag(
       TabCollectionNode& source_node);
+  VerticalTabDragHandlerImpl::DragInitData GetDragInitDataForNestedOwnerDrag(TabCollectionNode& source_node);
   std::map<tab_groups::TabGroupId, TabSlotView*> GetFullySelectedGroups(
       const std::vector<tabs::TabInterface*>& selected_tabs);
 
@@ -217,6 +231,13 @@ class VerticalTabDragHandlerImpl : publi
   // was not initiated by a group header.
   std::optional<tab_groups::TabGroupId> GetDraggingGroupHeaderId() const;
 
+  std::optional<nested_tabs::NestedTabId> GetDraggingNestedId() const {
+      return drag_controller_ ? drag_controller_->GetSessionData().nested_id() : std::nullopt;
+  }
+
+  void SetPendingNestIntoTarget(
+      std::optional<tabs::TabHandle> target) override;
+
   const raw_ref<TabStripModel> tab_strip_model_;
   const raw_ref<TabCollectionNode> root_node_;
 
@@ -230,6 +251,9 @@ class VerticalTabDragHandlerImpl : publi
   std::map<raw_ptr<const TabCollectionNode>, raw_ptr<TabSlotView>> slot_views_;
   std::vector<base::CallbackListSubscription> node_destroyed_callbacks_;
 
+  // See SetPendingNestIntoTarget.
+  std::optional<tabs::TabHandle> pending_nest_into_target_;
+
   base::WeakPtrFactory<VerticalTabDragHandlerImpl> weak_factory_{this};
 };
 
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_group_view.cc
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_group_view.cc
@@ -20,6 +20,7 @@
 #include "chrome/browser/ui/views/tabs/vertical/tab_collection_animating_layout_manager.h"
 #include "chrome/browser/ui/views/tabs/vertical/tab_collection_node.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_dragged_tabs_container.h"
+#include "chrome/browser/ui/views/tabs/vertical/vertical_nested_tab_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_split_tab_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_group_header_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_controller.h"
@@ -425,6 +426,8 @@ const TabCollectionNode* VerticalTabGrou
   } else if (auto* split_tab_view =
                  views::AsViewClass<VerticalSplitTabView>(&view)) {
     return split_tab_view->collection_node();
+  } else if (auto* nested_tab_view = views::AsViewClass<VerticalNestedTabView>(&view)) {
+      return nested_tab_view->collection_node();
   }
   return nullptr;
 }
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_controller.cc
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_controller.cc
@@ -21,6 +21,7 @@
 #include "chrome/browser/ui/tabs/tab_menu_model_factory.h"
 #include "chrome/browser/ui/tabs/tab_strip_model.h"
 #include "chrome/browser/ui/tabs/tab_strip_model_delegate.h"
+#include "chrome/browser/ui/tabs/tab_strip_user_gesture_details.h"
 #include "chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h"
 #include "chrome/browser/ui/ui_features.h"
 #include "chrome/browser/ui/views/event_utils.h"
@@ -35,6 +36,7 @@
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_group_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h"
 #include "chrome/grit/generated_resources.h"
+#include "components/tabs/public/nested_tab_data.h"
 #include "components/tabs/public/tab_collection_types.h"
 #include "components/tabs/public/tab_group.h"
 #include "components/tabs/public/tab_interface.h"
@@ -357,6 +359,32 @@ void VerticalTabStripController::ToggleT
   }
 }
 
+void VerticalTabStripController::SetSubtreeCollapsed(
+    const nested_tabs::NestedTabId& nested_id,
+    bool collapsed) {
+  const nested_tabs::NestedTabData* data = model_->GetNestingData(nested_id);
+
+  bool is_currently_collapsed = model_->IsSubtreeCollapsed(nested_id);
+
+  if (is_currently_collapsed == collapsed) {
+    return;
+  }
+
+  tabs::TabInterface* active_tab = model_->GetActiveTab();
+  if (active_tab && collapsed) {
+    if (data->GetIndexRange().Contains(
+            gfx::Range(model_->GetIndexOfTab(active_tab)))) {
+      // Active tab will be collapsed. Select the parent of the collapsing
+      // NESTED.
+      SelectTab(data->owner_tab().Get(),
+                TabStripUserGestureDetails(
+                    TabStripUserGestureDetails::GestureType::kOther));
+    }
+  }
+
+  model_->SetSubtreeCollapsed(nested_id, collapsed);
+}
+
 void VerticalTabStripController::ShowGroupEditorBubble(
     const TabCollectionNode* group_node) {
   auto* group_header_view =
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_controller.h
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_controller.h
@@ -13,6 +13,7 @@
 #include "chrome/browser/ui/views/tabs/tab/tab_context_menu_controller.h"
 #include "chrome/browser/ui/views/tabs/tab_strip_types.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_drag_handler.h"
+#include "components/nested_tabs/nested_tab_id.h"
 #include "components/tab_groups/tab_group_id.h"
 #include "ui/base/models/list_selection_model.h"
 
@@ -25,7 +26,7 @@ class TabHoverCardController;
 namespace tabs {
 class TabInterface;
 class VerticalTabStripStateController;
-}
+}  // namespace tabs
 
 namespace tab_groups {
 class TabGroupSyncService;
@@ -70,6 +71,8 @@ class VerticalTabStripController : publi
   const ui::ListSelectionModel& GetSelectionModel() const;
   void ToggleTabGroupCollapsedState(const TabGroup* group,
                                     ToggleTabGroupCollapsedStateOrigin origin);
+  void SetSubtreeCollapsed(const nested_tabs::NestedTabId& nested_id,
+                           bool collapsed);
   void ShowGroupEditorBubble(const TabCollectionNode* group_node);
   views::Widget* ShowGroupEditorBubble(const tab_groups::TabGroupId& group_id,
                                        views::View* anchor_view,
@@ -105,6 +108,9 @@ class VerticalTabStripController : publi
       std::optional<tab_groups::TabGroupId> new_focused_group_id,
       std::optional<tab_groups::TabGroupId> old_focused_group_id);
 
+  TabStripModel* model() { return model_; }
+  const TabStripModel* model() const { return model_; }
+
  private:
   // TabContextMenuController::Delegate:
   bool IsContextMenuCommandChecked(
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.cc
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h"
 
+#include <memory>
 #include <optional>
 #include <string>
 
@@ -14,6 +15,8 @@
 #include "base/metrics/user_metrics_action.h"
 #include "base/notimplemented.h"
 #include "base/strings/utf_string_conversions.h"
+#include "cc/paint/paint_flags.h"
+#include "chrome/app/vector_icons/vector_icons.h"
 #include "chrome/browser/glic/browser_ui/tab_underline_controller.h"
 #include "chrome/browser/glic/browser_ui/tab_underline_view.h"
 #include "chrome/browser/glic/public/glic_enabling.h"
@@ -67,11 +70,14 @@
 #include "ui/gfx/geometry/rect.h"
 #include "ui/gfx/geometry/size.h"
 #include "ui/gfx/geometry/skia_conversions.h"
+#include "ui/gfx/image/image_skia_operations.h"
+#include "ui/gfx/paint_vector_icon.h"
 #include "ui/gfx/scoped_canvas.h"
 #include "ui/views/accessibility/view_accessibility.h"
 #include "ui/views/background.h"
 #include "ui/views/border.h"
 #include "ui/views/controls/button/button.h"
+#include "ui/views/controls/button/image_button.h"
 #include "ui/views/controls/focus_ring.h"
 #include "ui/views/controls/highlight_path_generator.h"
 #include "ui/views/controls/label.h"
@@ -88,6 +94,8 @@ constexpr int kTitleMinWidth = 10;
 constexpr int kHorizontalInset = 7;
 constexpr int kDefaultPadding = 4;
 constexpr int kFocusRingInset = 0.0f;
+constexpr int kIconSize = 16;
+constexpr int kChevronIconSize = 20;
 
 class VerticalTabHighlightPathGenerator : public views::HighlightPathGenerator {
  public:
@@ -139,6 +147,7 @@ VerticalTabView::VerticalTabView(TabColl
                               base::Unretained(this)),
           // TODO(crbug.com/467733947): Hook up metrics logging callback.
           base::DoNothingAs<void(views::View*, const ui::MouseEvent&)>()))),
+      chevron_(AddChildView(std::make_unique<views::ImageButton>())),
       hover_controller_(gfx::Animation::ShouldRenderRichAnimation()
                             ? std::make_unique<GlowHoverController>(this)
                             : nullptr) {
@@ -162,6 +171,18 @@ VerticalTabView::VerticalTabView(TabColl
                   GetLayoutConstant(LayoutConstant::kVerticalTabHeight)));
   }
 
+  chevron_->SetProperty(views::kViewIgnoredByLayoutKey, true);
+  chevron_->SetImageHorizontalAlignment(views::ImageButton::ALIGN_CENTER);
+  chevron_->SetImageVerticalAlignment(views::ImageButton::ALIGN_MIDDLE);
+  chevron_->SetCallback(base::BindRepeating(
+      [](VerticalTabView* self) {
+        if (self->owned_nested_id_) {
+          self->collection_node_->GetController()->SetSubtreeCollapsed(
+              *self->owned_nested_id_, !self->nested_collapsed_);
+        }
+      },
+      base::Unretained(this)));
+
   // Ordered vector of children to be rendered in the tab.
   tab_children_configs_ = {
       TabChildConfig(close_button_, kIconDesignWidth, kDefaultPadding,
@@ -258,6 +279,8 @@ void VerticalTabView::UpdateHovered(bool
     }
   }
 
+  icon_->SetDimmed(ChevronState().has_value());
+
   UpdateColors();
   InvalidateLayout();
 }
@@ -533,6 +556,31 @@ void VerticalTabView::OnPaint(gfx::Canva
   views::View::OnPaint(canvas);
 }
 
+void VerticalTabView::OnPaintBorder(gfx::Canvas* canvas) {
+  views::View::OnPaintBorder(canvas);
+  if (!is_drop_target_) {
+    return;
+  }
+
+  const SkScalar corner_radius = SkIntToScalar(
+      GetLayoutConstant(LayoutConstant::kVerticalTabCornerRadius) +
+      (split_ ? GetInsets().height() : 0));
+
+  cc::PaintFlags flags;
+  flags.setStyle(cc::PaintFlags::kStroke_Style);
+  flags.setStrokeWidth(2);
+  flags.setAntiAlias(true);
+  flags.setColor(
+      GetColorProvider()->GetColor(ui::kColorFocusableBorderFocused));
+
+  // Inset by half stroke width so the stroke lives fully inside bounds.
+  gfx::Rect bounds = GetLocalBounds();
+  bounds.Inset(1);  // inset by 1 on all sides
+  SkRRect rrect = SkRRect::MakeRectXY(gfx::RectToSkRect(bounds),
+                                      corner_radius - 1, corner_radius - 1);
+  canvas->sk_canvas()->drawRRect(rrect, flags);
+}
+
 void VerticalTabView::PaintTabBackgroundWithImages(
     gfx::Canvas* canvas,
     std::optional<int> active_tab_fill_id,
@@ -727,7 +775,8 @@ bool VerticalTabView::IsChildVisible(con
 
   if (child_view == icon_) {
     const bool should_hide_icon_for_alert = pinned_ || collapsed_;
-    return !should_hide_icon_for_alert || !IsChildVisible(alert_indicator_, width);
+    return !should_hide_icon_for_alert ||
+           !IsChildVisible(alert_indicator_, width);
   }
 
   if (child_view == close_button_) {
@@ -773,6 +822,7 @@ views::ProposedLayout VerticalTabView::C
   const bool is_centered = (pinned_ || collapsed_) && !IsInExpandOnHover(width);
 
   int placed_children = 0;
+  gfx::Rect icon_bounds = {};
   for (const auto& child : tab_children_configs_) {
     const bool can_render_child =
         is_centered
@@ -781,9 +831,13 @@ views::ProposedLayout VerticalTabView::C
                placed_children < 2);
     const bool is_child_visible = IsChildVisible(child.view, width);
     if (is_child_visible && can_render_child) {
-      layouts.child_layouts.emplace_back(
-          child.view.get(), is_child_visible,
-          GetChildBounds(bounds_remaining, child, is_centered));
+      const gfx::Rect computed =
+          GetChildBounds(bounds_remaining, child, is_centered);
+      layouts.child_layouts.emplace_back(child.view.get(), is_child_visible,
+                                         computed);
+      if (child.view == icon_) {
+        icon_bounds = computed;
+      }
 
       if (!is_centered) {
         bounds_remaining.Inset(
@@ -804,6 +858,11 @@ views::ProposedLayout VerticalTabView::C
     }
   }
 
+  if (!icon_bounds.IsEmpty()) {
+    layouts.child_layouts.emplace_back(chevron_.get(),
+                                       ChevronState().has_value(), icon_bounds);
+  }
+
   return layouts;
 }
 
@@ -953,6 +1012,13 @@ void VerticalTabView::OnDataChanged() {
   active_ = tab_strip_model->IsTabInForeground(index);
   split_ = tab->IsSplit();
   pinned_ = tab->IsPinned();
+  owned_nested_id_ = tab_strip_model->GetOwnedNestedId(tab);
+  nested_collapsed_ =
+      owned_nested_id_
+          .transform([&](auto nested_id) {
+            return tab_strip_model->IsSubtreeCollapsed(nested_id);
+          })
+          .value_or(false);
 
   SetSelection(tab->IsSelected());
   UpdateTabData(tab);
@@ -985,6 +1051,7 @@ void VerticalTabView::UpdateTabData(tabs
                       !tab->IsActivated() && tab->IsBlocked());
   icon_->SetAttention(TabIcon::AttentionType::kTabWantsAttentionStatus,
                       tab_data_.needs_attention);
+  icon_->SetDimmed(ChevronState().has_value());
 
   UpdateTitle();
 
@@ -1063,6 +1130,29 @@ void VerticalTabView::UpdateColors() {
   close_button_->SetColors(colors);
   alert_indicator_->OnParentTabButtonColorChanged();
 
+  gfx::ImageSkia icon =
+      gfx::CreateVectorIcon(kKeyboardArrowDownChromeRefreshIcon,
+                            kChevronIconSize, colors.foreground_color);
+  if (ChevronState() == NestedChevronState::kCollapsed) {
+    icon = gfx::ImageSkiaOperations::CreateRotatedImage(
+        icon, SkBitmapOperations::ROTATION_270_CW);
+  }
+  chevron_->SetImageModel(views::Button::STATE_NORMAL,
+                          ui::ImageModel::FromImageSkia(icon));
+  std::string chevron_accessibility;
+  if (auto s = ChevronState()) {
+    switch (*s) {
+      case VerticalTabView::NestedChevronState::kCollapsed:
+        chevron_accessibility = "Expand nested tabs";
+        break;
+      case VerticalTabView::NestedChevronState::kHovered:
+        chevron_accessibility = "Collapse nested tabs";
+    }
+  } else {
+    chevron_accessibility = "No nested";
+  }
+  chevron_->GetViewAccessibility().SetName(chevron_accessibility);
+
   UpdateBorder();
 
   // TODO(crbug.com/465159185): Update focus ring colors.
@@ -1186,7 +1276,15 @@ const tabs::TabInterface* VerticalTabVie
 
 void VerticalTabView::UpdateHoverCard(HoverCardAnchorTarget* target,
                                       int hover_card_update_type) {
-  CHECK(collection_node_);
+  // Tolerate calls after ResetCollectionNode has run. Focus/blur events can
+  // fire on a tab view while the strip is tearing down (e.g. cascade close
+  // of a NESTED subtree on quit), and OnFocus/OnBlur both call into here
+  // without re-checking collection_node_. There is nothing to update if the
+  // node is gone, the hover card controller already received a
+  // kTabRemoved update from ResetCollectionNode.
+  if (!collection_node_) {
+    return;
+  }
 
   if (TabHoverCardController* hover_card_controller =
           collection_node_->GetController()->GetHoverCardController()) {
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.h
@@ -23,6 +23,7 @@
 #include "ui/base/metadata/metadata_header_macros.h"
 #include "ui/gfx/canvas.h"
 #include "ui/views/context_menu_controller.h"
+#include "ui/views/controls/button/image_button.h"
 #include "ui/views/layout/delegating_layout_manager.h"
 #include "ui/views/layout/flex_layout.h"
 #include "ui/views/masked_targeter_delegate.h"
@@ -83,6 +84,14 @@ class VerticalTabView : public views::Vi
   views::BubbleBorder::Arrow GetAnchorPosition() const override;
   const views::View* GetAnchorView() const override;
 
+  void SetIsDropTarget(bool is_drop_target) {
+    if (is_drop_target == is_drop_target_) {
+      return;
+    }
+    is_drop_target_ = is_drop_target;
+    SchedulePaint();
+  }
+
  private:
   // views::View
   void Layout(PassKey) override;
@@ -96,6 +105,7 @@ class VerticalTabView : public views::Vi
   bool OnMouseDragged(const ui::MouseEvent& event) override;
   void OnGestureEvent(ui::GestureEvent* event) override;
   void OnPaint(gfx::Canvas* canvas) override;
+  void OnPaintBorder(gfx::Canvas* canvas) override;
   void AddedToWidget() override;
   void RemovedFromWidget() override;
   void OnFocus() override;
@@ -204,6 +214,7 @@ class VerticalTabView : public views::Vi
   const raw_ptr<TabTitle> title_;
   const raw_ptr<AlertIndicatorButton> alert_indicator_;
   const raw_ptr<TabCloseButton> close_button_;
+  const raw_ptr<views::ImageButton> chevron_;
   raw_ptr<glic::TabUnderlineView> glic_tab_underline_view_ = nullptr;
 
   base::CallbackListSubscription node_destroyed_subscription_;
@@ -220,6 +231,30 @@ class VerticalTabView : public views::Vi
   bool collapsed_ = false;
   bool pinned_ = false;
   bool shift_pressed_on_mouse_down_ = false;
+  bool is_drop_target_ = false;
+
+  enum class NestedChevronState {
+    // Show v
+    kHovered,
+    // Always show in > position
+    kCollapsed,
+  };
+
+  std::optional<nested_tabs::NestedTabId> owned_nested_id_;
+  bool nested_collapsed_ = false;
+
+  std::optional<NestedChevronState> ChevronState() const {
+    if (!owned_nested_id_) {
+      return std::nullopt;  // not an owner
+    }
+    if (nested_collapsed_) {
+      return NestedChevronState::kCollapsed;  // always ›
+    }
+    if (hovered_) {
+      return NestedChevronState::kHovered;  // ⌄
+    }
+    return std::nullopt;  // owner, expanded, idle
+  }
 
   std::unique_ptr<GlowHoverController> hover_controller_;
   float hover_opacity_min_;
--- a/chrome/browser/ui/views/tabs/vertical/vertical_unpinned_tab_container_view.cc
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_unpinned_tab_container_view.cc
@@ -9,6 +9,7 @@
 #include "chrome/browser/ui/views/tabs/vertical/tab_collection_animating_layout_manager.h"
 #include "chrome/browser/ui/views/tabs/vertical/tab_collection_node.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_dragged_tabs_container.h"
+#include "chrome/browser/ui/views/tabs/vertical/vertical_nested_tab_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_split_tab_view.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_drag_handler.h"
 #include "chrome/browser/ui/views/tabs/vertical/vertical_tab_group_view.h"
@@ -312,6 +313,24 @@ VerticalUnpinnedTabContainerView::GetTab
                              GetDraggingViewsBoundsAtPoint(point_in_container))
                        : std::nullopt;
   const views::ProposedLayout& target_layout = layout_manager_->target_layout();
+
+  // NESTEDs are full drag containers like groups; delegate if the cursor
+  // falls inside one so child reordering happens in the right host_view_.
+  for (const views::ChildLayout& layout : target_layout.child_layouts) {
+    if (!layout.visible || IsViewDragging(*layout.child_view)) {
+      continue;
+    }
+    auto* nested_view =
+        views::AsViewClass<VerticalNestedTabView>(layout.child_view);
+    if (!nested_view) {
+      continue;
+    }
+    if (layout.bounds.y() <= point_in_container.y() &&
+        layout.bounds.bottom() >= point_in_container.y()) {
+      return nested_view->GetTabDragTarget(point_in_screen);
+    }
+  }
+
   for (const views::ChildLayout& layout : target_layout.child_layouts) {
     if (!layout.visible || IsViewDragging(*layout.child_view)) {
       continue;
@@ -325,7 +344,7 @@ VerticalUnpinnedTabContainerView::GetTab
     if (group_view->IsHandlingDrag()) {
       if (ShouldDragRemainInGroup(*group_view, layout.bounds,
                                   point_in_screen)) {
-        return *group_view;
+        return group_view->GetTabDragTarget(point_in_screen);
       } else {
         return *this;
       }
@@ -337,14 +356,14 @@ VerticalUnpinnedTabContainerView::GetTab
           kMinHeaderHeightPctForGroupEntry;
       if (HasMinimumOverlap(*dragging_view_bounds, layout.bounds, std::nullopt,
                             required_overlap_amount)) {
-        return *group_view;
+        return group_view->GetTabDragTarget(point_in_screen);
       }
     } else if (layout.bounds.y() <= point_in_container.y() &&
                layout.bounds.bottom() >= point_in_container.y()) {
       // If neither the group or this container are handling a drag and the drag
       // point falls in the group (e.g. when starting the drag), then use the
       // group.
-      return *group_view;
+      return group_view->GetTabDragTarget(point_in_screen);
     }
   }
   return *this;
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -525,6 +525,7 @@ void AddAppearanceStrings(content::WebUI
       {"browserLayoutCompact", IDS_SETTINGS_BROWSER_LAYOUT_COMPACT},
       {"browserLayoutVertical", IDS_SETTINGS_BROWSER_LAYOUT_VERTICAL},
       {"tabStripRightAlign", IDS_SETTINGS_TAB_STRIP_RIGHT_ALIGN},
+      {"tabStripNesting", IDS_SETTINGS_TAB_STRIP_NESTING},
       {"browserZenMode", IDS_SETTINGS_BROWSER_ZEN_MODE},
       {"browserZenModeDescription", IDS_SETTINGS_BROWSER_ZEN_MODE_DESCRIPTION},
       {"zenModePinSidebar", IDS_SETTINGS_ZEN_PIN_SIDEBAR},
--- a/chrome/browser/ui/webui/tab_strip_internals/tab_strip_internals_util.cc
+++ b/chrome/browser/ui/webui/tab_strip_internals/tab_strip_internals_util.cc
@@ -55,7 +55,9 @@ mojom::DataPtr BuildMojoCollection(const
       pinned_tabs->id = std::move(node_id);
       return mojom::Data::NewPinnedTabCollection(std::move(pinned_tabs));
     }
-    case tabs::TabCollection::Type::UNPINNED: {
+    case tabs::TabCollection::Type::UNPINNED:
+    // I am NOT dealing with Mojo for right now. Ignore nested tabs.
+    case tabs::TabCollection::Type::NESTED: {
       auto unpinned_tabs = mojom::UnpinnedCollection::New();
       unpinned_tabs->id = std::move(node_id);
       return mojom::Data::NewUnpinnedTabCollection(std::move(unpinned_tabs));
--- a/chrome/common/pref_names.h
+++ b/chrome/common/pref_names.h
@@ -840,6 +840,10 @@ inline constexpr char kHeliumLayout[] =
 inline constexpr char kHeliumVerticalRightAligned[] =
     "helium.browser.vertical_right_aligned";
 
+// A boolean pref set to true if vertical tabs should allow nesting.
+inline constexpr char kHeliumVerticalNesting[] =
+    "helium.browser.vertical_nesting";
+
 // A boolean pref set to true if zen mode is enabled.
 inline constexpr char kHeliumZenMode[] = "helium.browser.zen_mode";
 
--- /dev/null
+++ b/components/nested_tabs/BUILD.gn
@@ -0,0 +1,15 @@
+# Copyright 2026 The Chromium Authors
+# Use of this source code is governed by a BSD-style license that can be
+# found in the LICENSE file.
+
+source_set("nested_tabs") {
+  sources = [
+    "nested_tab_id.h",
+    "nested_tab_visual_data.h",
+  ]
+
+  public_deps = [
+    "//base",
+    "//components/tab_groups",
+  ]
+}
--- /dev/null
+++ b/components/nested_tabs/DEPS
@@ -0,0 +1,3 @@
+include_rules = [
+    "+components/tab_groups",
+]
\ No newline at end of file
--- /dev/null
+++ b/components/nested_tabs/DIR_METADATA
@@ -0,0 +1,6 @@
+monorail: {
+  component: "UI>Browser>TopChrome>TabStrip"
+}
+buganizer_public: {
+  component_id: 1456118
+}
\ No newline at end of file
--- /dev/null
+++ b/components/nested_tabs/OWNERS
@@ -0,0 +1,3 @@
+# Use the above COMPONENT in crbugs to to file bugs against issues in this folder
+file://chrome/browser/ui/tabs/OWNERS
+file://components/tabs/OWNERS
\ No newline at end of file
--- /dev/null
+++ b/components/nested_tabs/nested_tab_id.h
@@ -0,0 +1,21 @@
+// Copyright 2025 The Chromium Authors
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+#ifndef COMPONENTS_NESTED_TABS_NESTED_TAB_ID_H_
+#define COMPONENTS_NESTED_TABS_NESTED_TAB_ID_H_
+
+#include <ostream>
+
+#include "components/tab_groups/token_id.h"
+
+namespace nested_tabs {
+
+class NestedTabId : public tab_groups::TokenId<NestedTabId> {};
+
+// For use in std::unordered_map.
+using NestedTabIdHash = tab_groups::TokenIdHash<NestedTabId>;
+
+}  // namespace nested_tabs
+
+#endif  // COMPONENTS_NESTED_TABS_NESTED_TAB_ID_H_
--- /dev/null
+++ b/components/nested_tabs/nested_tab_visual_data.h
@@ -0,0 +1,46 @@
+// Copyright 2025 The Chromium Authors
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+#ifndef COMPONENTS_NESTED_TABS_NESTED_TAB_VISUAL_DATA_H_
+#define COMPONENTS_NESTED_TABS_NESTED_TAB_VISUAL_DATA_H_
+
+namespace tabs {
+    class NestedTabCollection;
+}
+
+namespace nested_tabs {
+
+// Represents the visual state of a nested tab, including its layout type and the
+// proportional size of the webcontents. Also used for persistence
+class NestedTabVisualData {
+ public:
+  NestedTabVisualData() = default;
+  explicit NestedTabVisualData(bool is_collapsed) : is_collapsed_(is_collapsed) {}
+  ~NestedTabVisualData() = default;
+
+  NestedTabVisualData(const NestedTabVisualData& other) = default;
+  NestedTabVisualData(NestedTabVisualData&& other) = default;
+
+  NestedTabVisualData& operator=(const NestedTabVisualData& other) = default;
+  NestedTabVisualData& operator=(NestedTabVisualData&& other) = default;
+
+
+  // Checks whether two instances are visually equivalent.
+  friend bool operator==(const NestedTabVisualData&,
+                         const NestedTabVisualData&) = default;
+
+  bool IsCollapsed() const { return is_collapsed_; }
+  void SetIsCollapsed(bool is_collapsed) {
+    is_collapsed_ = is_collapsed;
+  }
+ private:
+
+
+
+ bool is_collapsed_ = false;
+};
+
+}  // namespace nested_tabs
+
+#endif  // COMPONENTS_NESTED_TABS_NESTED_TAB_VISUAL_DATA_H_
--- a/components/sessions/core/live_tab_context.h
+++ b/components/sessions/core/live_tab_context.h
@@ -66,6 +66,31 @@ class SESSIONS_EXPORT LiveTabContext {
   virtual void SetVisualDataForGroup(
       const tab_groups::TabGroupId& group,
       const tab_groups::TabGroupVisualData& visual_data) = 0;
+
+
+  virtual std::optional<tab_restore::NestedTabChildRestorationInfo>
+  GetParentSessionIdAndChildIndexForTab(int index) const = 0;
+  virtual std::vector<int> GetDirectChildIndices(int index) const = 0;
+  virtual std::optional<int> GetParentIndex(int index) const = 0;
+  virtual SessionID GetSessionIdAtIndex(int index) const = 0;
+
+  // Returns the live SessionIDs of all tabs currently in |group|, in
+  // tabstrip order.
+  virtual std::vector<SessionID> GetSessionIdsInGroup(
+      const tab_groups::TabGroupId& group) const = 0;
+
+  // Applies the nested structure encoded in |entries| (top-level Tab
+  // entries in DFS pre-order, with children populated) onto the live tabs
+  // identified by |live_tab_ids| (also DFS pre-order). Each entry consumes
+  // (1 + recursive descendant count) ids; for each entry with non-empty
+  // children, the corresponding live tabs are nested under the entry's
+  // live tab via the embedder's NESTED API. Used by Group and Window
+  // restore to rebuild nesting on tabs the SavedTabGroup service or
+  // session restore brought back flat.
+  virtual void NestTabsByStructure(
+      const std::vector<SessionID>& live_tab_ids,
+      const std::vector<std::unique_ptr<tab_restore::Tab>>& entries) = 0;
+
   virtual const gfx::Rect GetRestoredBounds() const = 0;
   virtual ui::mojom::WindowShowState GetRestoredState() const = 0;
   virtual std::string GetWorkspace() const = 0;
@@ -80,11 +105,18 @@ class SESSIONS_EXPORT LiveTabContext {
   // |is_restoring_group_or_window| when true indicates if the tab we
   // are restoring is part of a window or group which is trying to restore all
   // of its tabs.
-  virtual LiveTab* AddRestoredTab(const tab_restore::Tab& tab,
-                                  int tab_index,
-                                  bool select,
-                                  bool is_restoring_group_or_window,
-                                  tab_restore::Type original_session_type) = 0;
+  // |parent_override|, when set, forces the restored tab to nest under the
+  // tab with this live SessionID instead of looking up
+  // |tab.parent_session_id| (whose stored id is stale across a subtree
+  // restore). Used by recursive subtree restore. Callers that don't need
+  // to override should pass std::nullopt.
+  virtual LiveTab* AddRestoredTab(
+      const tab_restore::Tab& tab,
+      int tab_index,
+      bool select,
+      bool is_restoring_group_or_window,
+      tab_restore::Type original_session_type,
+      std::optional<SessionID> parent_override) = 0;
 
   // Note: |tab.platform_data| may be null (e.g., if restoring from last session
   // as this data is not persisted, or if the platform does not provide
--- a/components/sessions/core/session_service_commands.cc
+++ b/components/sessions/core/session_service_commands.cc
@@ -8,11 +8,14 @@
 #include <string.h>
 
 #include <map>
+#include <memory>
+#include <optional>
 #include <set>
 #include <tuple>
 #include <utility>
 #include <vector>
 
+#include "base/check.h"
 #include "base/compiler_specific.h"
 #include "base/containers/flat_set.h"
 #include "base/logging.h"
@@ -21,7 +24,12 @@
 #include "base/token.h"
 #include "base/uuid.h"
 #include "base/values.h"
+#include "components/nested_tabs/nested_tab_id.h"
+#include "components/nested_tabs/nested_tab_visual_data.h"
 #include "components/sessions/core/base_session_service_commands.h"
+#include "components/sessions/core/session_command.h"
+#include "components/sessions/core/session_id.h"
+#include "components/sessions/core/session_types.h"
 #include "components/split_tabs/split_tab_id.h"
 #include "components/split_tabs/split_tab_visual_data.h"
 #include "components/tab_groups/tab_group_color.h"
@@ -61,8 +69,8 @@ static const SessionCommand::id_type kCo
 
 // OBSOLETE: Preserved for backward compatibility. Using
 // kCommandTabNavigationPathPruned instead
-static const SessionCommand::id_type
-    kCommandTabNavigationPathPrunedFromBack = 5;
+static const SessionCommand::id_type kCommandTabNavigationPathPrunedFromBack =
+    5;
 
 static const SessionCommand::id_type kCommandUpdateTabNavigation = 6;
 static const SessionCommand::id_type kCommandSetSelectedNavigationIndex = 7;
@@ -73,8 +81,8 @@ static const SessionCommand::id_type kCo
 
 // OBSOLETE: Preserved for backward compatibility. Using
 // kCommandTabNavigationPathPruned instead
-static const SessionCommand::id_type
-    kCommandTabNavigationPathPrunedFromFront = 11;
+static const SessionCommand::id_type kCommandTabNavigationPathPrunedFromFront =
+    11;
 
 static const SessionCommand::id_type kCommandSetPinnedState = 12;
 static const SessionCommand::id_type kCommandSetExtensionAppID = 13;
@@ -108,6 +116,9 @@ static const SessionCommand::id_type kCo
 static const SessionCommand::id_type kCommandSetSplitTab = 36;
 static const SessionCommand::id_type kCommandSetSplitTabData = 37;
 
+static const SessionCommand::id_type kCommandSetTabNested = 38;
+static const SessionCommand::id_type kCommandSetNestedTabData = 39;
+
 // ID 255 is used by CommandStorageBackend.
 
 namespace {
@@ -181,6 +192,12 @@ struct SplitTabPayload {
   bool has_split;
 };
 
+struct NestedTabPayload {
+  SessionID::id_type tab_id;
+  SerializedToken maybe_nested;
+  bool has_nested;
+};
+
 struct PinnedStatePayload {
   SessionID::id_type tab_id;
   bool pinned_state;
@@ -282,6 +299,8 @@ using GroupIdToSessionTabGroup =
     std::map<tab_groups::TabGroupId, std::unique_ptr<SessionTabGroup>>;
 using SplitIdToSessionSplitTab =
     std::map<split_tabs::SplitTabId, std::unique_ptr<SessionSplitTab>>;
+using NestedIdToSessionNestedTab =
+    std::map<nested_tabs::NestedTabId, std::unique_ptr<SessionNestedTab>>;
 
 // Returns the window in windows with the specified id. If a window does
 // not exist, one is created.
@@ -316,8 +335,9 @@ SessionTabGroup* GetTabGroup(tab_groups:
   // For |group_id|, insert a corresponding group entry or get the existing one.
   auto result = groups->emplace(group_id, nullptr);
   GroupIdToSessionTabGroup::iterator it = result.first;
-  if (result.second)
+  if (result.second) {
     it->second = std::make_unique<SessionTabGroup>(group_id);
+  }
   return it->second.get();
 }
 
@@ -333,19 +353,33 @@ SessionSplitTab* GetSplitTab(split_tabs:
   return it->second.get();
 }
 
+SessionNestedTab* GetNestedTab(nested_tabs::NestedTabId nested_id,
+                               NestedIdToSessionNestedTab* nesteds) {
+  DCHECK(nesteds);
+  // For `nested_id`, insert a corresponding nested entry or get the existing
+  // one.
+  auto result = nesteds->emplace(nested_id, nullptr);
+  NestedIdToSessionNestedTab::iterator it = result.first;
+  if (result.second) {
+    it->second = std::make_unique<SessionNestedTab>(nested_id);
+  }
+  return it->second.get();
+}
+
 // Returns an iterator into navigations pointing to the navigation whose
 // index matches |index|. If no navigation index matches |index|, the first
 // navigation with an index > |index| is returned.
 //
 // This assumes the navigations are ordered by index in ascending order.
 std::vector<sessions::SerializedNavigationEntry>::iterator
-  FindClosestNavigationWithIndex(
+FindClosestNavigationWithIndex(
     std::vector<sessions::SerializedNavigationEntry>* navigations,
     int index) {
   DCHECK(navigations);
   for (auto i = navigations->begin(); i != navigations->end(); ++i) {
-    if (i->index() >= index)
+    if (i->index() >= index) {
       return i;
+    }
   }
   return navigations->end();
 }
@@ -407,6 +441,7 @@ void SortTabsBasedOnVisualOrderAndClear(
 void AddTabsToWindows(IdToSessionTab* tabs,
                       GroupIdToSessionTabGroup* tab_groups,
                       SplitIdToSessionSplitTab* split_tabs,
+                      NestedIdToSessionNestedTab* nested_tabs,
                       IdToSessionWindow* windows) {
   DVLOG(1) << "AddTabsToWindows";
   DVLOG(1) << "Tabs " << tabs->size() << ", groups " << tab_groups->size()
@@ -414,8 +449,9 @@ void AddTabsToWindows(IdToSessionTab* ta
 
   for (auto& tab_pair : *tabs) {
     std::unique_ptr<SessionTab> tab = std::move(tab_pair.second);
-    if (!tab->window_id.id() || tab->navigations.empty())
+    if (!tab->window_id.id() || tab->navigations.empty()) {
       continue;
+    }
 
     SessionTab* tab_ptr = tab.get();
     SessionWindow* window = GetWindow(tab_ptr->window_id, windows);
@@ -445,6 +481,7 @@ void AddTabsToWindows(IdToSessionTab* ta
 
     base::flat_set<tab_groups::TabGroupId> groups_in_current_window;
     base::flat_set<split_tabs::SplitTabId> splits_in_current_window;
+    base::flat_set<nested_tabs::NestedTabId> nesteds_in_current_window;
 
     for (const auto& tab : window->tabs) {
       if (tab->group.has_value()) {
@@ -453,6 +490,9 @@ void AddTabsToWindows(IdToSessionTab* ta
       if (tab->split_id.has_value()) {
         splits_in_current_window.insert(tab->split_id.value());
       }
+      if (tab->nested_id) {
+        nesteds_in_current_window.insert(*tab->nested_id);
+      }
     }
 
     // Move corresponding SessionTabGroup entries into SessionWindow.
@@ -478,6 +518,17 @@ void AddTabsToWindows(IdToSessionTab* ta
       window->split_tabs.push_back(std::move(it->second));
       split_tabs->erase(it);
     }
+
+    // Same for SessionNestedTab
+    for (const nested_tabs::NestedTabId& nested_id: nesteds_in_current_window) {
+        auto it = nested_tabs->find(nested_id);
+        if (it == nested_tabs->end()) {
+            window->nested_tabs.push_back(std::make_unique<SessionNestedTab>(nested_id));
+            continue;
+        }
+        window->nested_tabs.push_back(std::move(it->second));
+        nested_tabs->erase(it);
+    }
   }
 
   // We may have extraneous tab group entries. Since we don't have explicit
@@ -487,6 +538,9 @@ void AddTabsToWindows(IdToSessionTab* ta
 
   // Clear any extra split entries.
   split_tabs->clear();
+
+  // Clear extra nesteds
+  nested_tabs->clear();
 }
 
 void ProcessTabNavigationPathPrunedCommand(
@@ -508,8 +562,9 @@ void ProcessTabNavigationPathPrunedComma
 
   // And update the index of existing navigations.
   for (auto& entry : tab->navigations) {
-    if (entry.index() < payload.index)
+    if (entry.index() < payload.index) {
       continue;
+    }
     entry.set_index(entry.index() - payload.count);
   }
 }
@@ -526,6 +581,7 @@ void CreateTabsAndWindows(
     IdToSessionTab* tabs,
     GroupIdToSessionTabGroup* tab_groups,
     SplitIdToSessionSplitTab* split_tabs,
+    NestedIdToSessionNestedTab* nested_tabs,
     IdToSessionWindow* windows,
     SessionID* active_window_id,
     std::string* platform_session_id,
@@ -661,8 +717,7 @@ void CreateTabsAndWindows(
       case kCommandUpdateTabNavigation: {
         sessions::SerializedNavigationEntry navigation;
         SessionID tab_id = SessionID::InvalidValue();
-        if (!RestoreUpdateTabNavigationCommand(*command,
-                                               &navigation,
+        if (!RestoreUpdateTabNavigationCommand(*command, &navigation,
                                                &tab_id)) {
           DVLOG(1) << "Failed reading command " << command->id();
           return;
@@ -670,10 +725,11 @@ void CreateTabsAndWindows(
         SessionTab* tab = GetTab(tab_id, tabs);
         auto i = FindClosestNavigationWithIndex(&(tab->navigations),
                                                 navigation.index());
-        if (i != tab->navigations.end() && i->index() == navigation.index())
+        if (i != tab->navigations.end() && i->index() == navigation.index()) {
           *i = navigation;
-        else
+        } else {
           tab->navigations.insert(i, navigation);
+        }
         break;
       }
 
@@ -746,24 +802,45 @@ void CreateTabsAndWindows(
         break;
       }
 
+      case kCommandSetTabNested: {
+        NestedTabPayload payload;
+        if (!command->GetContents(&payload, sizeof(payload))) {
+          DVLOG(1) << "Failed reading command " << command->id();
+          return;
+        }
+        SessionTab* session_tab =
+            GetTab(SessionID::FromSerializedValue(payload.tab_id), tabs);
+        const base::Token token(payload.maybe_nested.id_high,
+                                payload.maybe_nested.id_low);
+        session_tab->nested_id =
+            payload.has_nested
+                ? std::make_optional(
+                      nested_tabs::NestedTabId::FromRawToken(token))
+                : std::nullopt;
+        break;
+      }
+
       case kCommandSetTabGroupMetadata2: {
         base::PickleIterator iter = command->ContentsAsPickle();
 
         std::optional<base::Token> group_token = ReadTokenFromPickle(&iter);
-        if (!group_token.has_value())
+        if (!group_token.has_value()) {
           return;
+        }
 
         SessionTabGroup* group = GetTabGroup(
             tab_groups::TabGroupId::FromRawToken(group_token.value()),
             tab_groups);
 
         std::u16string title;
-        if (!iter.ReadString16(&title))
+        if (!iter.ReadString16(&title)) {
           return;
+        }
 
         uint32_t color_int;
-        if (!iter.ReadUInt32(&color_int))
+        if (!iter.ReadUInt32(&color_int)) {
           return;
+        }
 
         // The |is_collapsed| boolean was added in M88 to save the collapsed
         // state, so previous versions may not have this stored.
@@ -821,6 +898,33 @@ void CreateTabsAndWindows(
         break;
       }
 
+      case kCommandSetNestedTabData: {
+        base::PickleIterator iter = command->ContentsAsPickle();
+        std::optional<base::Token> nested_token = ReadTokenFromPickle(&iter);
+        if (!nested_token.has_value()) {
+          return;
+        }
+
+        SessionNestedTab* nested = GetNestedTab(
+            nested_tabs::NestedTabId::FromRawToken(nested_token.value()),
+            nested_tabs);
+
+        bool is_collapsed;
+        if (!iter.ReadBool(&is_collapsed)) {
+          return;
+        }
+
+        int owner_id;
+        if (!iter.ReadInt(&owner_id)) {
+          return;
+        }
+
+        nested->nested_visual_data_ =
+            nested_tabs::NestedTabVisualData(is_collapsed);
+        nested->owner_tab_ = SessionID::FromSerializedValue(owner_id);
+        break;
+      }
+
       case kCommandSetPinnedState: {
         PinnedStatePayload payload;
         if (!command->GetContents(&payload, sizeof(payload))) {
@@ -835,8 +939,9 @@ void CreateTabsAndWindows(
       case kCommandSetWindowAppName: {
         SessionID window_id = SessionID::InvalidValue();
         std::string app_name;
-        if (!RestoreSetWindowAppNameCommand(*command, &window_id, &app_name))
+        if (!RestoreSetWindowAppNameCommand(*command, &window_id, &app_name)) {
           return;
+        }
 
         GetWindow(window_id, windows)->app_name.swap(app_name);
         break;
@@ -845,8 +950,7 @@ void CreateTabsAndWindows(
       case kCommandSetExtensionAppID: {
         SessionID tab_id = SessionID::InvalidValue();
         std::string extension_app_id;
-        if (!RestoreSetTabExtensionAppIDCommand(*command,
-                                                &tab_id,
+        if (!RestoreSetTabExtensionAppIDCommand(*command, &tab_id,
                                                 &extension_app_id)) {
           DVLOG(1) << "Failed reading command " << command->id();
           return;
@@ -859,10 +963,8 @@ void CreateTabsAndWindows(
       case kCommandSetTabUserAgentOverride: {
         SessionID tab_id = SessionID::InvalidValue();
         std::string user_agent_override;
-        if (!RestoreSetTabUserAgentOverrideCommand(
-                *command,
-                &tab_id,
-                &user_agent_override)) {
+        if (!RestoreSetTabUserAgentOverrideCommand(*command, &tab_id,
+                                                   &user_agent_override)) {
           return;
         }
 
@@ -894,8 +996,9 @@ void CreateTabsAndWindows(
         SessionID::id_type command_tab_id;
         std::string session_storage_persistent_id;
         if (!iter.ReadInt(&command_tab_id) ||
-            !iter.ReadString(&session_storage_persistent_id))
+            !iter.ReadString(&session_storage_persistent_id)) {
           return;
+        }
         // Associate the session storage back.
         GetTab(SessionID::FromSerializedValue(command_tab_id), tabs)
             ->session_storage_persistent_id = session_storage_persistent_id;
@@ -929,7 +1032,7 @@ void CreateTabsAndWindows(
         base::PickleIterator it = command->ContentsAsPickle();
         SessionID::id_type window_id = -1;
         std::string workspace;
-         if (!it.ReadInt(&window_id) || !it.ReadString(&workspace)) {
+        if (!it.ReadInt(&window_id) || !it.ReadString(&workspace)) {
           DVLOG(1) << "Failed reading command " << command->id();
           return;
         }
@@ -1016,8 +1119,9 @@ void CreateTabsAndWindows(
       case kCommandSetWindowUserTitle: {
         SessionID window_id = SessionID::InvalidValue();
         std::string title;
-        if (!RestoreSetWindowUserTitleCommand(*command, &window_id, &title))
+        if (!RestoreSetWindowUserTitleCommand(*command, &window_id, &title)) {
           return;
+        }
         GetWindow(window_id, windows)->user_title = title;
         break;
       }
@@ -1059,7 +1163,7 @@ std::unique_ptr<SessionCommand> CreateSe
 std::unique_ptr<SessionCommand> CreateSetSelectedTabInWindowCommand(
     SessionID window_id,
     int index) {
-  SelectedTabInIndexPayload payload = { 0 };
+  SelectedTabInIndexPayload payload = {0};
   payload.id = window_id.id();
   payload.index = index;
   return CreateSessionCommandForPayload(kCommandSetSelectedTabInIndex, payload);
@@ -1067,7 +1171,7 @@ std::unique_ptr<SessionCommand> CreateSe
 
 std::unique_ptr<SessionCommand> CreateSetTabWindowCommand(SessionID window_id,
                                                           SessionID tab_id) {
-  SessionID::id_type payload[] = { window_id.id(), tab_id.id() };
+  SessionID::id_type payload[] = {window_id.id(), tab_id.id()};
   return CreateSessionCommandForPayload(kCommandSetTabWindow, payload);
 }
 
@@ -1075,7 +1179,7 @@ std::unique_ptr<SessionCommand> CreateSe
     SessionID window_id,
     const gfx::Rect& bounds,
     ui::mojom::WindowShowState show_state) {
-  WindowBoundsPayload3 payload = { 0 };
+  WindowBoundsPayload3 payload = {0};
   payload.window_id = window_id.id();
   payload.x = bounds.x();
   payload.y = bounds.y();
@@ -1088,7 +1192,7 @@ std::unique_ptr<SessionCommand> CreateSe
 std::unique_ptr<SessionCommand> CreateSetTabIndexInWindowCommand(
     SessionID tab_id,
     int new_index) {
-  TabIndexInWindowPayload payload = { 0 };
+  TabIndexInWindowPayload payload = {0};
   payload.id = tab_id.id();
   payload.index = new_index;
   return CreateSessionCommandForPayload(kCommandSetTabIndexInWindow, payload);
@@ -1118,7 +1222,7 @@ std::unique_ptr<SessionCommand> CreateWi
 std::unique_ptr<SessionCommand> CreateSetSelectedNavigationIndexCommand(
     SessionID tab_id,
     int index) {
-  SelectedNavigationIndexPayload payload = { 0 };
+  SelectedNavigationIndexPayload payload = {0};
   payload.id = tab_id.id();
   payload.index = index;
   return CreateSessionCommandForPayload(kCommandSetSelectedNavigationIndex,
@@ -1128,7 +1232,7 @@ std::unique_ptr<SessionCommand> CreateSe
 std::unique_ptr<SessionCommand> CreateSetWindowTypeCommand(
     SessionID window_id,
     SessionWindow::WindowType type) {
-  WindowTypePayload payload = { 0 };
+  WindowTypePayload payload = {0};
   payload.id = window_id.id();
   payload.index = static_cast<int32_t>(type);
   return CreateSessionCommandForPayload(kCommandSetWindowType, payload);
@@ -1197,9 +1301,36 @@ std::unique_ptr<SessionCommand> CreateSp
   return std::make_unique<SessionCommand>(kCommandSetSplitTabData, pickle);
 }
 
+std::unique_ptr<SessionCommand> CreateNestedTabCommand(
+    SessionID tab_id,
+    std::optional<nested_tabs::NestedTabId> nested_id) {
+  NestedTabPayload payload = {0};
+  payload.tab_id = tab_id.id();
+  if (nested_id.has_value()) {
+    DCHECK(!nested_id.value().token().is_zero());
+    payload.maybe_nested = {.id_high = nested_id.value().token().high(),
+                            .id_low = nested_id.value().token().low()};
+    payload.has_nested = true;
+  }
+  return CreateSessionCommandForPayload(kCommandSetTabNested, payload);
+}
+
+std::unique_ptr<SessionCommand> CreateNestedTabDataUpdateCommand(
+    const nested_tabs::NestedTabId& nested_id,
+    const nested_tabs::NestedTabVisualData* nested_tab_visual_data,
+    const SessionID owner_session_id) {
+  base::Pickle pickle;
+  base::WriteTokenToPickle(&pickle, nested_id.token());
+
+  pickle.WriteBool(nested_tab_visual_data->IsCollapsed());
+  pickle.WriteInt(owner_session_id.id());
+
+  return std::make_unique<SessionCommand>(kCommandSetNestedTabData, pickle);
+}
+
 std::unique_ptr<SessionCommand> CreatePinnedStateCommand(SessionID tab_id,
                                                          bool is_pinned) {
-  PinnedStatePayload payload = { 0 };
+  PinnedStatePayload payload = {0};
   payload.tab_id = tab_id.id();
   payload.pinned_state = is_pinned;
   return CreateSessionCommandForPayload(kCommandSetPinnedState, payload);
@@ -1407,13 +1538,14 @@ void RestoreSessionFromCommands(
   IdToSessionTab tabs;
   GroupIdToSessionTabGroup tab_groups;
   SplitIdToSessionSplitTab split_tabs;
+  NestedIdToSessionNestedTab nested_tabs;
   IdToSessionWindow windows;
 
   DVLOG(1) << "RestoreSessionFromCommands " << commands.size();
-  CreateTabsAndWindows(commands, &tabs, &tab_groups, &split_tabs, &windows,
-                       active_window_id, platform_session_id,
+  CreateTabsAndWindows(commands, &tabs, &tab_groups, &split_tabs, &nested_tabs,
+                       &windows, active_window_id, platform_session_id,
                        discarded_window_ids);
-  AddTabsToWindows(&tabs, &tab_groups, &split_tabs, &windows);
+  AddTabsToWindows(&tabs, &tab_groups, &split_tabs, &nested_tabs, &windows);
   SortTabsBasedOnVisualOrderAndClear(&windows, valid_windows,
                                      discarded_window_ids);
   UpdateSelectedTabIndex(valid_windows);
@@ -1422,8 +1554,9 @@ void RestoreSessionFromCommands(
 #if DCHECK_IS_ON()
   for (const auto& window : *valid_windows) {
     DCHECK(!window->tabs.empty());
-    for (const auto& tab : window->tabs)
+    for (const auto& tab : window->tabs) {
       DCHECK(!tab->navigations.empty());
+    }
   }
 #endif
   // AddTabsToWindows should have processed all the tabs and groups.
--- a/components/sessions/core/session_service_commands.h
+++ b/components/sessions/core/session_service_commands.h
@@ -60,6 +60,14 @@ SESSIONS_EXPORT std::unique_ptr<SessionC
 SESSIONS_EXPORT std::unique_ptr<SessionCommand> CreateSplitTabDataUpdateCommand(
     const split_tabs::SplitTabId split_id,
     const split_tabs::SplitTabVisualData* split_tab_visual_data);
+SESSIONS_EXPORT std::unique_ptr<SessionCommand> CreateNestedTabCommand(
+    SessionID tab_id,
+    std::optional<nested_tabs::NestedTabId> nested_id);
+SESSIONS_EXPORT std::unique_ptr<SessionCommand>
+CreateNestedTabDataUpdateCommand(
+    const nested_tabs::NestedTabId& nested_id,
+    const nested_tabs::NestedTabVisualData* nested_tab_visual_data,
+    const SessionID owner_session_id);
 SESSIONS_EXPORT std::unique_ptr<SessionCommand> CreatePinnedStateCommand(
     SessionID tab_id,
     bool is_pinned);
--- a/components/sessions/core/session_types.h
+++ b/components/sessions/core/session_types.h
@@ -14,6 +14,8 @@
 
 #include "base/time/time.h"
 #include "base/token.h"
+#include "components/nested_tabs/nested_tab_id.h"
+#include "components/nested_tabs/nested_tab_visual_data.h"
 #include "components/sessions/core/serialized_navigation_entry.h"
 #include "components/sessions/core/serialized_user_agent_override.h"
 #include "components/sessions/core/session_id.h"
@@ -82,6 +84,9 @@ struct SESSIONS_EXPORT SessionTab {
   // The tab's split ID, if any.
   std::optional<split_tabs::SplitTabId> split_id;
 
+  // The tab's nested ID, if any.
+  std::optional<nested_tabs::NestedTabId> nested_id;
+
   // True if the tab is pinned.
   bool pinned;
 
@@ -160,6 +165,29 @@ struct SESSIONS_EXPORT SessionSplitTab {
   split_tabs::SplitTabVisualData split_visual_data_;
 };
 
+// SessionNestedTab -----------------------------------------------------------
+
+// Describes a Nested referenced by some SessionTab entry in its nested_id
+// field. By default, this is initialized with placeholder values that are
+// visually obvious.
+struct SESSIONS_EXPORT SessionNestedTab {
+  explicit SessionNestedTab(const nested_tabs::NestedTabId& id) : id_(id) {}
+
+  SessionNestedTab(const SessionNestedTab&) = delete;
+  SessionNestedTab& operator=(const SessionNestedTab&) = delete;
+
+  ~SessionNestedTab() = default;
+
+  // Uniquely identifies this Nested. Initialized to zero and must be set by
+  // user. Unlike SessionID this should be globally unique, even across
+  // different sessions.
+  nested_tabs::NestedTabId id_;
+
+  nested_tabs::NestedTabVisualData nested_visual_data_;
+
+  SessionID owner_tab_ = SessionID::InvalidValue();
+};
+
 // SessionWindow -------------------------------------------------------------
 
 // Describes a saved window.
@@ -226,6 +254,9 @@ struct SESSIONS_EXPORT SessionWindow {
   // should be at least one tab in `tabs` in the split.
   std::vector<std::unique_ptr<SessionSplitTab>> split_tabs;
 
+  // Nested tabs in no particular order
+  std::vector<std::unique_ptr<SessionNestedTab>> nested_tabs;
+
   // Is the window maximized, minimized, or normal?
   ui::mojom::WindowShowState show_state;
 
--- a/components/sessions/core/tab_restore_service.h
+++ b/components/sessions/core/tab_restore_service.h
@@ -77,6 +77,15 @@ class SESSIONS_EXPORT TabRestoreService
   // had a beforeunload handler.
   virtual void GroupCloseStopped(const tab_groups::TabGroupId& group) = 0;
 
+  virtual void CreateHistoricalNestedSubtree(LiveTabContext* context, int parent_index) = 0;
+  // Removes suppression state for a subtree close. |parent_id| is the live
+  // SessionID of the closed parent tab that was passed to
+  // CreateHistoricalNestedSubtree (i.e. context->GetLiveTabAt(parent_index)'s
+  // SessionID at close time).
+  virtual void NestedSubtreeClosed(SessionID parent_id) = 0;
+  // See `NestedSubtreeClosed`
+  virtual void NestedSubtreeCloseStopped(SessionID parent_id) = 0;
+
   // TODO(blundell): Rename and fix comment.
   // Invoked when a browser is closing. If |context| is a tabbed browser with
   // at least one tab, a Window is created, added to entries and observers are
--- a/components/sessions/core/tab_restore_service_helper.cc
+++ b/components/sessions/core/tab_restore_service_helper.cc
@@ -12,6 +12,8 @@
 #include <map>
 #include <memory>
 #include <optional>
+#include <ranges>
+#include <stack>
 #include <string>
 #include <utility>
 #include <vector>
@@ -30,6 +32,7 @@
 #include "base/trace_event/process_memory_dump.h"
 #include "base/values.h"
 #include "components/dom_distiller/core/url_constants.h"
+#include "components/sessions/content/session_tab_helper.h"
 #include "components/sessions/core/live_tab.h"
 #include "components/sessions/core/live_tab_context.h"
 #include "components/sessions/core/serialized_navigation_entry.h"
@@ -185,6 +188,15 @@ std::optional<SessionID> TabRestoreServi
     return std::nullopt;
   }
 
+  if (context) {
+    SessionID live_id = context->GetSessionIdAtIndex(index);
+    for (const auto& [root, set] : closing_subtrees_) {
+      if (set.contains(live_id)) {
+        return std::nullopt;
+      }
+    }
+  }
+
   // Save the Window as well as the Tab if this is the last tab of an app
   // browser to ensure the tab will reopen in the correct app window.
   if (context && context->GetTabCount() == 1 &&
@@ -219,21 +231,23 @@ void TabRestoreServiceHelper::BrowserClo
   window->extra_data = context->GetExtraDataForWindow();
 
   for (int tab_index = 0; tab_index < context->GetTabCount(); ++tab_index) {
-    auto tab = std::make_unique<Tab>();
-    PopulateTab(tab.get(), tab_index, context,
-                context->GetLiveTabAt(tab_index));
-    if (tab->navigations.empty()) {
+    // Skip non-root tabs, they'll be gathered as children of their root.
+    if (context->GetParentIndex(tab_index).has_value()) {
+      continue;
+    }
+
+    auto tab =
+        NestedDfsHelper(context, tab_index, /*do_suppression_tracking=*/false);
+    if (!tab || tab->navigations.empty()) {
       continue;
     }
     tab->browser_id = context->GetSessionID().id();
 
     if (tab->group.has_value() &&
         !window->tab_groups.contains(tab->group.value())) {
-      // Add new groups to the mapping if we haven't already.
       window->tab_groups.emplace(tab->group.value(),
                                  Group::FromTab(*tab.get()));
     }
-
     window->tabs.push_back(std::move(tab));
   }
 
@@ -263,14 +277,21 @@ TabRestoreServiceHelper::CreateHistorica
   group->timestamp = TimeNow();
 
   for (int tab_index = 0; tab_index < context->GetTabCount(); ++tab_index) {
-    if (context->GetTabGroupForTab(tab_index) == id) {
-      auto tab = std::make_unique<Tab>();
-      PopulateTab(tab.get(), tab_index, context,
-                  context->GetLiveTabAt(tab_index));
-      if (!tab->navigations.empty()) {
-        tab->browser_id = context->GetSessionID().id();
-        group->tabs.push_back(std::move(tab));
-      }
+    if (context->GetTabGroupForTab(tab_index) != id) {
+      continue;
+    }
+    // Skip non-root tabs of in-group subtrees; they'll be gathered as
+    // children of their group-mate root below.
+    std::optional<int> parent_idx_opt = context->GetParentIndex(tab_index);
+    if (parent_idx_opt.has_value() &&
+        context->GetTabGroupForTab(*parent_idx_opt) == id) {
+      continue;
+    }
+    // Gather this tab + its in-group subtree into a single Tab entry.
+    auto tab = NestedDfsHelper(context, tab_index, false);
+    if (tab && !tab->navigations.empty()) {
+      tab->browser_id = context->GetSessionID().id();
+      group->tabs.push_back(std::move(tab));
     }
   }
 
@@ -300,6 +321,92 @@ void TabRestoreServiceHelper::GroupClose
   closing_groups_.erase(group);
 }
 
+void TabRestoreServiceHelper::CreateHistoricalNestedSubtree(
+    LiveTabContext* context,
+    int parent_index) {
+  // Window version of the group thing below
+  if (closing_contexts_.contains(context)) {
+    return;
+  }
+  // If this subtree's parent is in a group that's being closed as a unit,
+  // skip emission. The group's historical entry (built by
+  // CreateHistoricalGroupImpl) already captures the full nested structure
+  // inside the group; emitting per-subtree entries on top would put
+  // duplicate entries on the restore stack and require multiple
+  // cmd-shift-t presses to recover the same group close.
+  if (auto group = context->GetTabGroupForTab(parent_index);
+      group && closing_groups_.contains(*group)) {
+    return;
+  }
+
+  auto root = NestedDfsHelper(context, parent_index, true);
+
+  if (root && !root->navigations.empty()) {
+    AddEntry(std::move(root), true, true);
+  }
+}
+
+std::unique_ptr<tab_restore::Tab> TabRestoreServiceHelper::NestedDfsHelper(
+    LiveTabContext* context,
+    int parent_index,
+    bool do_suppression_tracking) {
+  constexpr size_t kSubtreeCap = 24;
+
+  std::unique_ptr<Tab> root;
+  size_t emitted = 0;
+  std::stack<std::pair<Tab*, int>> s;  // (parent-in-tree, tabstrip-index)
+  s.push({nullptr, parent_index});
+
+  SessionID root_live_id = context->GetSessionIdAtIndex(parent_index);
+  std::set<SessionID>& descendants = closing_subtrees_[root_live_id];
+
+  while (!s.empty()) {
+    auto [parent, subject] = s.top();
+    s.pop();
+
+    LiveTab* live = context->GetLiveTabAt(subject);
+
+    if (do_suppression_tracking) {
+      // Mark for suppression regardless of cap.
+      SessionID live_id = context->GetSessionIdAtIndex(subject);
+      descendants.insert(live_id);
+    }
+
+    // Only build entry struct if under cap; still recurse for suppression.
+    Tab* this_in_tree = nullptr;
+    if (emitted < kSubtreeCap) {
+      auto t = std::make_unique<Tab>();
+      PopulateTab(t.get(), subject, context, live);
+      this_in_tree = t.get();
+      if (parent) {
+        parent->children.push_back(std::move(t));
+      } else {
+        root = std::move(t);
+      }
+      emitted++;
+    }
+
+    // Push children in reverse for pre-order DFS pop order.
+    for (int idx :
+         std::views::reverse(context->GetDirectChildIndices(subject))) {
+      // If we're past cap, pass nullptr as parent so children also get
+      // suppression-tracked but aren't attached to a tree node.
+      s.push({this_in_tree, idx});
+    }
+  }
+
+  return root;
+}
+
+void TabRestoreServiceHelper::NestedSubtreeClosed(SessionID parent_id) {
+  closing_subtrees_.erase(parent_id);
+}
+
+void TabRestoreServiceHelper::NestedSubtreeCloseStopped(SessionID parent_id) {
+  // TODO: Make this similar to `GroupClosedStopped` when that function is fixed
+  closing_subtrees_.erase(parent_id);
+}
+
 void TabRestoreServiceHelper::ClearEntries() {
   if (observer_) {
     observer_->OnClearEntries();
@@ -647,6 +754,19 @@ std::vector<LiveTab*> TabRestoreServiceH
       context = RestoreTab(tab, context, disposition, entry.type, &restored_tab,
                            /*is_restoring_group_or_window=*/false);
       live_tabs.push_back(restored_tab);
+
+      // Restore the rest of the subtree, if any, under the just-restored root.
+      if (restored_tab && !tab.children.empty()) {
+        for (int i = 0; i < context->GetTabCount(); ++i) {
+          if (context->GetLiveTabAt(i) == restored_tab) {
+            RestoreSubtreeChildren(tab.children,
+                                   context->GetSessionIdAtIndex(i), context,
+                                   disposition, &live_tabs, false);
+            break;
+          }
+        }
+      }
+
       context->ShowBrowserWindow();
       break;
     }
@@ -698,7 +818,8 @@ std::vector<LiveTab*> TabRestoreServiceH
           const bool select_tab = tab->id == selected_tab_id;
           LiveTab* restored_tab = context->AddRestoredTab(
               *tab.get(), context->GetTabCount(), select_tab,
-              /*restored_from_group_or_window_context=*/true, entry.type);
+              /*restored_from_group_or_window_context=*/true, entry.type,
+              /*parent_override=*/std::nullopt);
 
           if (restored_tab) {
             client_->OnTabRestored(
@@ -706,12 +827,34 @@ std::vector<LiveTab*> TabRestoreServiceH
                     .virtual_url());
             live_tabs.push_back(restored_tab);
           }
+          if (restored_tab && !tab->children.empty()) {
+            LiveTabContext* tab_ctx =
+                client_->FindLiveTabContextForTab(restored_tab);
+            if (!tab_ctx) {
+              continue;
+            }
+            for (int i = 0; i < tab_ctx->GetTabCount(); ++i) {
+              if (tab_ctx->GetLiveTabAt(i) == restored_tab) {
+                RestoreSubtreeChildren(tab->children,
+                                       tab_ctx->GetSessionIdAtIndex(i), context,
+                                       disposition, &live_tabs, true);
+                break;
+              }
+            }
+          }
         }
 
         // Update all tabs to point to the correct context.
         if (auto browser_id = window.tabs[0]->browser_id) {
           UpdateTabBrowserIDs(browser_id, context->GetSessionID());
         }
+
+        std::vector<SessionID> all_ids;
+        all_ids.reserve(context->GetTabCount());
+        for (int i = 0; i < context->GetTabCount(); ++i) {
+          all_ids.push_back(context->GetSessionIdAtIndex(i));
+        }
+        context->NestTabsByStructure(all_ids, window.tabs);
       } else {
         // Restore a single entry from the window. It could be a single tab, or
         // an entire group.
@@ -768,9 +911,50 @@ std::vector<LiveTab*> TabRestoreServiceH
         for (const auto& tab : group.tabs) {
           LiveTab* restored_tab = context->AddRestoredTab(
               *tab.get(), context->GetTabCount(), group.tabs[0]->id == tab->id,
-              /*restored_from_group_or_window_context=*/true, entry.type);
+              /*restored_from_group_or_window_context=*/true, entry.type,
+              /*parent_override=*/std::nullopt);
           live_tabs.push_back(restored_tab);
+
+          // If this tab is a subtree root (unsaved-group case where
+          // AddRestoredTab returned a live tab and inserted normally),
+          // restore its children inline. The post-restore fixup below
+          // handles the saved-group case where AddRestoredTab returned
+          // nullptr and the SavedTabGroup service brought tabs back flat.
+          if (restored_tab && !tab->children.empty()) {
+            LiveTabContext* tab_ctx =
+                client_->FindLiveTabContextForTab(restored_tab);
+            if (!tab_ctx) {
+              continue;
+            }
+            for (int i = 0; i < tab_ctx->GetTabCount(); ++i) {
+              if (tab_ctx->GetLiveTabAt(i) == restored_tab) {
+                RestoreSubtreeChildren(tab->children,
+                                       tab_ctx->GetSessionIdAtIndex(i), context,
+                                       disposition, &live_tabs, true);
+                break;
+              }
+            }
+          }
+        }
+
+        // Apply nested structure post-hoc. Idempotent for the unsaved
+        // case (children are already nested by the inline recursion
+        // above); the real work happens for saved groups, where the
+        // SavedTabGroup service brought tabs back flat and the inline
+        // recursion didn't run.
+        //
+        // For saved groups the SavedTabGroupService opens the group under
+        // a fresh local TabGroupId, so |group.group_id| is stale. Resolve
+        // the current local id from the saved id.
+        tab_groups::TabGroupId resolved_group_id = group.group_id;
+        if (group.saved_group_id) {
+          if (auto current =
+                  context->GetGroupIdForSavedGroup(*group.saved_group_id)) {
+            resolved_group_id = *current;
+          }
         }
+        context->NestTabsByStructure(
+            context->GetSessionIdsInGroup(resolved_group_id), group.tabs);
       } else {
         // Restore a single tab from the group. Find the tab that matches the
         // ID in the group and restore it.
@@ -1026,6 +1210,52 @@ void TabRestoreServiceHelper::PopulateTa
     }
 
     tab->extra_data = context->GetExtraDataForTab(tab->tabstrip_index);
+
+    tab->parent_session_id =
+        context->GetParentSessionIdAndChildIndexForTab(index);
+  }
+}
+
+void TabRestoreServiceHelper::RestoreSubtreeChildren(
+    const std::vector<std::unique_ptr<Tab>>& children,
+    SessionID live_parent_id,
+    LiveTabContext* context,
+    WindowOpenDisposition disposition,
+    std::vector<LiveTab*>* live_tabs,
+    bool is_restoring_group_or_window) {
+  for (const auto& child : children) {
+    LiveTab* restored_child = nullptr;
+    // is_restoring_group_or_window=false: this recursion restores children of
+    // a Tab entry, not tabs from a Window/Group entry. The root parent's
+    // AddRestoredTab already opened the parent's group via the normal path,
+    // so children should join via the same path. Passing true would trip
+    // the saved-group early-return in BrowserLiveTabContext::AddRestoredTab
+    // and skip insertion entirely.
+    RestoreTab(*child, context, disposition, sessions::tab_restore::TAB,
+               &restored_child, is_restoring_group_or_window,
+               /*parent_override=*/live_parent_id);
+    live_tabs->push_back(restored_child);
+
+    if (!child->children.empty()) {
+      // The just-restored child becomes the live parent for its own subtree.
+      // Resolve its current live SessionID via the client's lookup so the
+      // recursion stays free of LiveTab->WebContents casts.
+      LiveTabContext* child_ctx =
+          client_->FindLiveTabContextForTab(restored_child);
+      if (!child_ctx) {
+        continue;
+      }
+      // Locate the just-restored child within its context by pointer identity
+      // to read out its live session id.
+      for (int i = 0; i < child_ctx->GetTabCount(); ++i) {
+        if (child_ctx->GetLiveTabAt(i) == restored_child) {
+          RestoreSubtreeChildren(
+              child->children, child_ctx->GetSessionIdAtIndex(i), context,
+              disposition, live_tabs, is_restoring_group_or_window);
+          break;
+        }
+      }
+    }
   }
 }
 
@@ -1035,26 +1265,36 @@ LiveTabContext* TabRestoreServiceHelper:
     WindowOpenDisposition disposition,
     sessions::tab_restore::Type session_restore_type,
     LiveTab** live_tab,
-    bool is_restoring_group_or_window) {
+    bool is_restoring_group_or_window,
+    std::optional<SessionID> parent_override) {
   LiveTab* restored_tab;
   if (disposition == WindowOpenDisposition::CURRENT_TAB && context) {
     restored_tab = context->ReplaceRestoredTab(tab);
   } else {
-    // We only respect the tab's original browser if there's no disposition.
-    if (disposition == WindowOpenDisposition::UNKNOWN && tab.browser_id) {
-      context = client_->FindLiveTabContextWithID(
-          SessionID::FromSerializedValue(tab.browser_id));
-    }
-
-    // Restore a grouped tab into its original group, even if the group has
-    // since been moved to a different context. If the original group doesn't
-    // exist any more, fall back to using the tab's original browser.
-    if (tab.group.has_value()) {
-      // TODO: This needs to look at the saved id.
-      LiveTabContext* group_context =
-          client_->FindLiveTabContextWithGroup(tab.group.value());
-      if (group_context) {
-        context = group_context;
+    // Context-resolution by tab.browser_id / tab.group is only correct for
+    // top-level restores. When parent_override is set we're inside a subtree
+    // restore: the caller has already picked the right context (the parent's),
+    // and the entry's browser_id/group still refer to the pre-close state,
+    // which would route us to the wrong (or stale) context and cause Nest()
+    // to receive indices for an unrelated tabstrip.
+    if (!parent_override.has_value()) {
+      // We only respect the tab's original browser if there's no disposition.
+      if (disposition == WindowOpenDisposition::UNKNOWN && tab.browser_id) {
+        context = client_->FindLiveTabContextWithID(
+            SessionID::FromSerializedValue(tab.browser_id));
+      }
+
+      // Restore a grouped tab into its original group, even if the group has
+      // since been moved to a different context. If the original group
+      // doesn't exist any more, fall back to using the tab's original
+      // browser.
+      if (tab.group.has_value()) {
+        // TODO: This needs to look at the saved id.
+        LiveTabContext* group_context =
+            client_->FindLiveTabContextWithGroup(tab.group.value());
+        if (group_context) {
+          context = group_context;
+        }
       }
     }
 
@@ -1085,7 +1325,7 @@ LiveTabContext* TabRestoreServiceHelper:
     restored_tab = context->AddRestoredTab(
         tab, tab_index,
         /*select=*/disposition != WindowOpenDisposition::NEW_BACKGROUND_TAB,
-        is_restoring_group_or_window, session_restore_type);
+        is_restoring_group_or_window, session_restore_type, parent_override);
   }
 
   client_->OnTabRestored(
--- a/components/sessions/core/tab_restore_service_helper.h
+++ b/components/sessions/core/tab_restore_service_helper.h
@@ -103,6 +103,14 @@ class SESSIONS_EXPORT TabRestoreServiceH
                              const tab_groups::TabGroupId& id);
   void GroupClosed(const tab_groups::TabGroupId& group);
   void GroupCloseStopped(const tab_groups::TabGroupId& group);
+  void CreateHistoricalNestedSubtree(LiveTabContext* context, int parent_index);
+  // Removes suppression state for a subtree close. |parent_id| is the live
+  // SessionID of the closed parent tab that was passed to
+  // CreateHistoricalNestedSubtree (i.e. context->GetLiveTabAt(parent_index)'s
+  // SessionID at close time).
+  void NestedSubtreeClosed(SessionID parent_id);
+  // See `NestedSubtreeClosed`
+  void NestedSubtreeCloseStopped(SessionID parent_id);
   void ClearEntries();
   void DeleteNavigationEntries(const DeletionPredicate& predicate);
 
@@ -154,6 +162,22 @@ class SESSIONS_EXPORT TabRestoreServiceH
                    LiveTabContext* context,
                    LiveTab* live_tab);
 
+  // Recursively restores |children| under the live tab identified by
+  // |live_parent_id|. The caller is responsible for resolving the
+  // just-restored ancestor's SessionID (e.g. via LiveTabContext::
+  // GetSessionIdAtIndex) before calling.
+  void RestoreSubtreeChildren(const std::vector<std::unique_ptr<Tab>>& children,
+                              SessionID live_parent_id,
+                              LiveTabContext* context,
+                              WindowOpenDisposition disposition,
+                              std::vector<LiveTab*>* live_tabs,
+                              bool is_restoring_group_or_window);
+
+  std::unique_ptr<tab_restore::Tab> NestedDfsHelper(
+      LiveTabContext* context,
+      int parent_index,
+      bool do_suppression_tracking);
+
   // This is a helper function for RestoreEntryById() for restoring a single
   // tab. If |context| is NULL, this creates a new window for the entry. This
   // returns the LiveTabContext into which the tab was restored. |disposition|
@@ -165,12 +189,14 @@ class SESSIONS_EXPORT TabRestoreServiceH
   // belongs to.
   // |is_restoring_group_or_window| indicates if the tab is being restored
   // alongside other tabs inside the same group or window.
-  LiveTabContext* RestoreTab(const Tab& tab,
-                             LiveTabContext* context,
-                             WindowOpenDisposition disposition,
-                             sessions::tab_restore::Type session_restore_type,
-                             LiveTab** live_tab,
-                             bool is_restoring_group_or_window);
+  LiveTabContext* RestoreTab(
+      const Tab& tab,
+      LiveTabContext* context,
+      WindowOpenDisposition disposition,
+      sessions::tab_restore::Type session_restore_type,
+      LiveTab** live_tab,
+      bool is_restoring_group_or_window,
+      std::optional<SessionID> parent_override = std::nullopt);
 
   // This is a helper function for RestoreEntryById(). Restores a single entry
   // from the `window`. The entry to restore is denoted by `id` and can either
@@ -260,6 +286,8 @@ class SESSIONS_EXPORT TabRestoreServiceH
   // creating historical tabs for them.
   std::set<tab_groups::TabGroupId> closing_groups_;
 
+  std::map<SessionID, std::set<SessionID>> closing_subtrees_;
+
   const raw_ptr<TimeFactory> time_factory_;
 };
 
--- a/components/sessions/core/tab_restore_service_impl.h
+++ b/components/sessions/core/tab_restore_service_impl.h
@@ -48,6 +48,15 @@ class SESSIONS_EXPORT TabRestoreServiceI
                              const tab_groups::TabGroupId& id) override;
   void GroupClosed(const tab_groups::TabGroupId& group) override;
   void GroupCloseStopped(const tab_groups::TabGroupId& group) override;
+  void CreateHistoricalNestedSubtree(LiveTabContext* context, int parent_index) override {
+      helper_.CreateHistoricalNestedSubtree(context, parent_index);
+  }
+  void NestedSubtreeClosed(SessionID parent_id) override {
+      helper_.NestedSubtreeClosed(parent_id);
+  }
+  void NestedSubtreeCloseStopped(SessionID parent_id) override {
+      helper_.NestedSubtreeCloseStopped(parent_id);
+  }
   void ClearEntries() override;
   void DeleteNavigationEntries(const DeletionPredicate& predicate) override;
   const Entries& entries() const override;
--- a/components/sessions/core/tab_restore_types.h
+++ b/components/sessions/core/tab_restore_types.h
@@ -79,6 +79,11 @@ struct SESSIONS_EXPORT Entry {
   explicit Entry(Type type);
 };
 
+struct NestedTabChildRestorationInfo {
+  SessionID parent_session_id;
+  size_t child_index;
+};
+
 // Represents a previously open tab.
 // If you add a new field that can allocate memory also add
 // it to the EstimatedMemoryUsage() implementation.
@@ -131,6 +136,12 @@ struct SESSIONS_EXPORT Tab : public Entr
 
   // The group metadata for the tab, if any.
   std::optional<tab_groups::TabGroupVisualData> group_visual_data;
+
+  // The children of the nested, if any.
+  std::vector<std::unique_ptr<Tab>> children;
+
+  // The parent of the nested, if any.
+  std::optional<NestedTabChildRestorationInfo> parent_session_id;
 };
 
 // Represents a previously open group.
--- a/components/tabs/BUILD.gn
+++ b/components/tabs/BUILD.gn
@@ -22,6 +22,8 @@ source_set("public") {
     "public/pinned_tab_collection.h",
     "public/split_tab_collection.h",
     "public/split_tab_data.h",
+    "public/nested_tab_collection.h",
+    "public/nested_tab_data.h",
     "public/supports_handles.h",
     "public/tab_collection.h",
     "public/tab_collection_observer.h",
@@ -54,6 +56,8 @@ source_set("tabs") {
     "impl/pinned_tab_collection.cc",
     "impl/split_tab_collection.cc",
     "impl/split_tab_data.cc",
+    "impl/nested_tab_collection.cc",
+    "impl/nested_tab_data.cc",
     "impl/supports_handles.cc",
     "impl/tab_collection.cc",
     "impl/tab_collection_storage.cc",
--- /dev/null
+++ b/components/tabs/impl/nested_tab_collection.cc
@@ -0,0 +1,56 @@
+// Copyright 2025 The Chromium Authors
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+#include "components/tabs/public/nested_tab_collection.h"
+
+#include <cstddef>
+#include <memory>
+#include <optional>
+
+#include "components/nested_tabs/nested_tab_visual_data.h"
+#include "components/tabs/public/nested_tab_data.h"
+#include "components/tabs/public/tab_collection.h"
+#include "components/tabs/public/tab_collection_storage.h"
+
+namespace tabs {
+
+// POTENTIAL FOOTGUN ALERT: Do not allow GROUP tabs in this collection. The
+// semantics of what happens is too complicated to deal with right now.
+NestedTabCollection::NestedTabCollection(
+    nested_tabs::NestedTabId nested_id,
+    nested_tabs::NestedTabVisualData visual_data, tabs::TabHandle owner_tab)
+    : TabCollection(TabCollection::Type::NESTED,
+                    {TabCollection::Type::NESTED, TabCollection::Type::SPLIT},
+                    true),
+      nested_tab_data_(
+          std::make_unique<nested_tabs::NestedTabData>(this,
+                                                       nested_id,
+                                                       visual_data, owner_tab)) {}
+
+nested_tabs::NestedTabId NestedTabCollection::GetNestedTabId() const {
+  return nested_tab_data_->id();
+}
+
+NestedTabCollection::~NestedTabCollection() = default;
+
+size_t NestedTabCollection::Depth() const {
+    size_t depth = 1; // Depth can never be 0 (nested to who?)
+    for (TabCollection *p = GetParentCollection(); p; p = p->GetParentCollection()) {
+        if (p->type() == TabCollection::Type::NESTED) {
+            depth++;
+        }
+    }
+
+    return depth;
+}
+
+bool NestedTabCollection::IsCollapsed() const {
+    return nested_tab_data_->visual_data()->IsCollapsed();
+}
+
+void NestedTabCollection::SetIsCollapsed(bool is_collapsed) {
+    nested_tab_data_->visual_data()->SetIsCollapsed(is_collapsed);
+}
+
+}  // namespace tabs
--- /dev/null
+++ b/components/tabs/impl/nested_tab_data.cc
@@ -0,0 +1,52 @@
+// Copyright 2025 The Chromium Authors
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+#include "components/tabs/public/nested_tab_data.h"
+
+#include <memory>
+
+#include "components/nested_tabs/nested_tab_visual_data.h"
+#include "components/tabs/public/nested_tab_collection.h"
+#include "components/tabs/public/tab_collection.h"
+#include "components/tabs/public/tab_interface.h"
+
+namespace nested_tabs {
+
+NestedTabData::NestedTabData(tabs::NestedTabCollection* collection,
+                           const nested_tabs::NestedTabId& id,
+                           const NestedTabVisualData& visual_data, tabs::TabHandle owner_tab)
+    : collection_(collection), owner_tab_(owner_tab), visual_data_(visual_data), id_(id) {}
+
+std::vector<tabs::TabInterface*> NestedTabData::ListTabs() const {
+  return collection_->GetTabsRecursive();
+}
+
+tabs::TabCollection::Handle NestedTabData::GetCollectionHandle() const {
+  return collection_->GetHandle();
+}
+
+gfx::Range NestedTabData::GetIndexRange() const {
+  std::vector<tabs::TabInterface*> nested_tabs = ListTabs();
+
+  if (nested_tabs.empty()) {
+    return gfx::Range();
+  }
+
+  // Find the TabStripCollection (for which indexes are based off of.)
+  tabs::TabCollection* root_collection = collection_;
+  while (root_collection &&
+         root_collection->type() != tabs::TabCollection::Type::TABSTRIP) {
+    root_collection = root_collection->GetParentCollection();
+  }
+
+  if (!root_collection) {
+    return gfx::Range();
+  }
+
+  int start_index =
+      root_collection->GetIndexOfTabRecursive(nested_tabs[0]).value();
+  return gfx::Range(start_index, start_index + nested_tabs.size());
+}
+
+}  // namespace nested_tabs
--- a/components/tabs/impl/tab_collection.cc
+++ b/components/tabs/impl/tab_collection.cc
@@ -310,7 +310,6 @@ std::optional<TabCollection::Position> T
       tabs::TabCollection* collection =
           std::get<std::unique_ptr<tabs::TabCollection>>(child).get();
       if (!collections_moved.contains(collection)) {
-        // Recursively call into the collection.
         std::optional<TabCollection::Position> move_position =
             collection->FindMovePositionRecursive(
                 destination_index, dst_collection, curr_insertion_index,
--- a/components/tabs/impl/tab_group_tab_collection.cc
+++ b/components/tabs/impl/tab_group_tab_collection.cc
@@ -19,7 +19,7 @@ TabGroupTabCollection::TabGroupTabCollec
     tab_groups::TabGroupVisualData visual_data)
     : TabCollection(TabCollection::Type::GROUP,
 
-                    {TabCollection::Type::SPLIT},
+                    {TabCollection::Type::SPLIT, TabCollection::Type::NESTED},
                     /*supports_tabs=*/true),
       group_(group_factory.Create(this, group_id, visual_data)) {}
 
--- a/components/tabs/impl/tab_strip_collection.cc
+++ b/components/tabs/impl/tab_strip_collection.cc
@@ -4,14 +4,22 @@
 
 #include "components/tabs/public/tab_strip_collection.h"
 
+#include <algorithm>
 #include <memory>
 #include <optional>
+#include <ranges>
 #include <set>
+#include <variant>
 
 #include "base/containers/adapters.h"
 #include "base/memory/ptr_util.h"
+#include "base/strings/stringprintf.h"
+#include "base/strings/utf_string_conversions.h"
+#include "components/nested_tabs/nested_tab_id.h"
 #include "components/split_tabs/split_tab_id.h"
 #include "components/split_tabs/split_tab_visual_data.h"
+#include "components/tabs/public/nested_tab_collection.h"
+#include "components/tabs/public/nested_tab_data.h"
 #include "components/tabs/public/pinned_tab_collection.h"
 #include "components/tabs/public/split_tab_collection.h"
 #include "components/tabs/public/tab_collection.h"
@@ -51,10 +59,56 @@ tabs::TabCollection* GetCommonAncestor(t
   return nullptr;
 }
 
+using tabs::TabCollection;
+using namespace base;
+std::string DumpCollection(const TabCollection* c, int depth) {
+  std::string out;
+  std::string indent(depth * 2, ' ');
+  const char* type_name = [&] {
+    switch (c->type()) {
+      case TabCollection::Type::TABSTRIP:
+        return "TABSTRIP";
+      case TabCollection::Type::PINNED:
+        return "PINNED";
+      case TabCollection::Type::UNPINNED:
+        return "UNPINNED";
+      case TabCollection::Type::GROUP:
+        return "GROUP";
+      case TabCollection::Type::SPLIT:
+        return "SPLIT";
+      case TabCollection::Type::NESTED:
+        return "NESTED";
+    }
+  }();
+  out += base::StringPrintf("%s[%s handle=%d", indent.c_str(), type_name,
+                            c->GetHandle().raw_value());
+  if (c->type() == TabCollection::Type::NESTED) {
+    auto* n = static_cast<const tabs::NestedTabCollection*>(c);
+    out += base::StringPrintf(" owner=%d", n->data()->owner_tab().raw_value());
+  }
+  out += "]\n";
+  for (const auto& child : c->GetChildren()) {
+    if (auto* tab = std::get_if<std::unique_ptr<tabs::TabInterface>>(&child)) {
+      out += base::StringPrintf(
+          "%s  tab handle=%d title=\"%s\"\n", indent.c_str(),
+          (*tab)->GetHandle().raw_value(),
+          base::UTF16ToUTF8((*tab)->GetContents()->GetTitle()).c_str());
+    } else {
+      out += DumpCollection(
+          std::get<std::unique_ptr<TabCollection>>(child).get(), depth + 1);
+    }
+  }
+  return out;
+}
+
 }  // namespace
 
 namespace tabs {
 
+std::string TabStripCollection::DebugDump() const {
+  return DumpCollection(this, 0);
+}
+
 TabStripCollection::TabStripCollection(bool send_notifications_immediately)
     : TabCollection(
           TabCollection::Type::TABSTRIP,
@@ -119,14 +173,15 @@ void TabStripCollection::MoveTabRecursiv
     }
   }
 
-  MoveTabsRecursive({tab_indices}, final_index, new_group_id, new_pinned_state,
-                    retain_collection_types);
+  MoveTabsRecursive({tab_indices}, final_index, new_group_id, {},
+                    new_pinned_state, retain_collection_types);
 }
 
 void TabStripCollection::MoveTabsRecursive(
     const std::vector<int>& tab_indices,
     size_t destination_index,
     std::optional<tab_groups::TabGroupId> new_group_id,
+    std::optional<nested_tabs::NestedTabId> enclosing_nested,
     bool new_pinned_state,
     const TabCollection::TypeEnumSet retain_collection_types) {
   CHECK(destination_index >= 0);
@@ -136,8 +191,9 @@ void TabStripCollection::MoveTabsRecursi
   // If new group collection is created, we can find the first position of the
   // tab as though it was moving to the unpinned container and then update the
   // positions.
-  TabCollection::Position move_position = GetMovePosition(
-      tab_or_collections, destination_index, new_group_id, new_pinned_state);
+  TabCollection::Position move_position =
+      GetMovePosition(tab_or_collections, destination_index, new_group_id,
+                      enclosing_nested, new_pinned_state);
   if (new_group_id.has_value() &&
       !GetTabGroupCollection(new_group_id.value())) {
     TabCollection::Position group_insertion_details = move_position;
@@ -153,7 +209,14 @@ void TabStripCollection::MoveTabsRecursi
     if (std::holds_alternative<TabInterface*>(tab_or_collection)) {
       TabInterface* tab_ptr = std::get<TabInterface*>(tab_or_collection);
       src_parent_collection = tab_ptr->GetParentCollection(GetPassKey());
+
       MoveTabImpl(tab_ptr, move_position);
+
+      DCHECK_EQ(*move_position.parent_handle.Get()->GetIndexOfTab(tab_ptr),
+                move_position.index)
+          << "MoveTabImpl didn't place tab at requested position; "
+             "nested-position "
+             "math below will be wrong.";
     } else {
       TabCollection* collection_ptr =
           std::get<TabCollection*>(tab_or_collection);
@@ -164,9 +227,13 @@ void TabStripCollection::MoveTabsRecursi
     // Remove previous collection if needed. This also notifies the
     // collection is removed.
     if (src_parent_collection != unpinned_collection_ &&
-        src_parent_collection != pinned_collection_ &&
-        src_parent_collection->TabCountRecursive() == 0) {
-      RemoveTabCollectionImpl(src_parent_collection);
+        src_parent_collection != pinned_collection_) {
+      if (src_parent_collection->type() == Type::NESTED) {
+        MaybeDestroyNestedWithOnlyOwner(
+            static_cast<NestedTabCollection*>(src_parent_collection));
+      } else if (src_parent_collection->TabCountRecursive() == 0) {
+        RemoveTabCollectionImpl(src_parent_collection);
+      }
     }
 
     // Update the move position to right after where the previous node is.
@@ -179,10 +246,13 @@ TabCollection::Position TabStripCollecti
     ChildrenPtrs tab_or_collections,
     size_t destination_index,
     std::optional<tab_groups::TabGroupId> new_group_id,
+    std::optional<nested_tabs::NestedTabId> enclosing_nested,
     bool new_pinned_state) {
   // Calculate destination collection.
-  TabCollection* dst_collection = nullptr;
-  if (new_pinned_state) {
+  TabCollection* dst_collection;
+  if (enclosing_nested) {
+    dst_collection = GetNestedTabCollection(*enclosing_nested);
+  } else if (new_pinned_state) {
     dst_collection = pinned_collection_;
   } else if (new_group_id.has_value() &&
              group_mapping_.contains(new_group_id.value())) {
@@ -211,7 +281,14 @@ TabCollection::Position TabStripCollecti
                                 curr_insertion_index, tabs_moved,
                                 collections_moved);
 
-  return move_position.value();
+  // If the walk couldn't resolve `destination_index` (typically because it
+  // falls inside the moving set or past dst_collection's reachable range),
+  // append at the end of dst_collection rather than crashing.
+  if (!move_position) {
+    return TabCollection::Position{dst_collection->GetHandle(),
+                                   dst_collection->ChildCount()};
+  }
+  return *move_position;
 }
 
 ChildrenPtrs TabStripCollection::GetTabsAndCollectionsForMove(
@@ -262,14 +339,58 @@ ChildrenPtrs TabStripCollection::GetTabs
 
   ChildrenPtrs move_datas;
 
-  // Iterates through `tab_indices`. If the tab is a direct child of
-  // `pinned_collection_` or `unpinned_collection_` return it directly as it
-  // needs to be removed directly. Otherwise potentially return the biggest
-  // subcollection that contains the tab to be removed.
+  // Iterate `tab_indices`. Direct children of pinned/unpinned are pushed as
+  // tabs; otherwise push the biggest enclosing subcollection.
   int array_index = 0;
+
+  // If `tab` owns a retained NESTED, push the whole NESTED as a unit. Just
+  // advance past this owner's own index. Any of `nested`'s children that
+  // also appear in `tab_indices` are deduplicated by the per-element
+  // `needs_skip` check below (their NESTED's owner is selected, so they're
+  // skipped). Advancing by the full recursive tab count would over-advance
+  // when `tab_indices` contains other top-level selections (e.g. a second
+  // nest owner) after this one.
+  auto try_pack_owned_nested = [&](TabInterface* tab) -> bool {
+    NestedTabCollection* nested = GetNestedTabCollection(tab);
+    if (!nested || !retain_collection_types.Has(TabCollection::Type::NESTED)) {
+      return false;
+    }
+    move_datas.push_back(nested);
+    array_index++;
+    return true;
+  };
+
   while (array_index < static_cast<int>(tab_indices.size())) {
     TabInterface* tab = GetTabAtIndexRecursive(tab_indices[array_index]);
     TabCollection* collection = tab->GetParentCollection(GetPassKey());
+
+    // Skip if any ancestor NESTED's owner is also selected (tab will move
+    // along with that owner's NESTED). Tab's own NESTED doesn't count.
+    bool needs_skip = false;
+    for (TabCollection* c_iter = collection; c_iter;
+         c_iter = c_iter->GetParentCollection()) {
+      if (c_iter->type() != TabCollection::Type::NESTED) {
+        continue;
+      }
+      NestedTabCollection* nstd = static_cast<NestedTabCollection*>(c_iter);
+      TabInterface* nested_owner = nstd->data()->owner_tab().Get();
+      if (nested_owner == tab) {
+        continue;
+      }
+      if (selected_tabs.contains(nested_owner)) {
+        array_index++;
+        needs_skip = true;
+        break;
+      }
+    }
+    if (needs_skip) {
+      continue;
+    }
+
+    if (try_pack_owned_nested(tab)) {
+      continue;
+    }
+
     if (collection == pinned_collection_ ||
         collection == unpinned_collection_) {
       TabInterface* move_data =
@@ -315,15 +436,26 @@ std::unique_ptr<TabInterface> TabStripCo
       tab_to_be_removed->GetParentCollection(GetPassKey());
   CHECK(parent_collection);
 
-  // If it is the only tab in the group remove the collection.
+  // Remove an empty wrapping group/split when this was its only tab. NESTED
+  // is handled below via MaybeDestroyNestedWithOnlyOwner.
   if (parent_collection != unpinned_collection_ &&
       parent_collection != pinned_collection_ &&
+      parent_collection->type() != Type::NESTED &&
       parent_collection->TabCountRecursive() == 1) {
-    RemoveTabCollectionImpl(parent_collection);
+    // Keep the collection alive across MaybeRemoveTab; the unique_ptr returned
+    // from RemoveTabCollectionImpl owns parent_collection. Discarding it would
+    // free the collection before we dereference it below (UAF / SEGV).
+    std::unique_ptr<TabCollection> removed_holder =
+        RemoveTabCollectionImpl(parent_collection);
     return parent_collection->MaybeRemoveTab(tab_to_be_removed);
-  } else {
-    return RemoveTabImpl(tab_to_be_removed);
   }
+
+  std::unique_ptr<TabInterface> removed = RemoveTabImpl(tab_to_be_removed);
+  if (parent_collection->type() == Type::NESTED) {
+    MaybeDestroyNestedWithOnlyOwner(
+        static_cast<NestedTabCollection*>(parent_collection));
+  }
+  return removed;
 }
 
 std::unique_ptr<TabInterface> TabStripCollection::MaybeRemoveTab(
@@ -488,6 +620,196 @@ std::set<split_tabs::SplitTabId> TabStri
   return splits;
 }
 
+std::set<nested_tabs::NestedTabId> TabStripCollection::ListNesteds() const {
+  return nested_mapping_ |
+         std::views::transform([](const auto& n) { return n.first; }) |
+         std::ranges::to<std::set<nested_tabs::NestedTabId>>();
+}
+
+NestedTabCollection* TabStripCollection::GetNestedTabCollection(
+    const TabInterface* tab) {
+  // A tab owns a NESTED iff its direct parent is a NESTED whose owner_tab
+  // points back to it (owner is the NESTED's children[0]).
+  const TabCollection* parent = tab->GetParentCollection();
+  if (!parent || parent->type() != TabCollection::Type::NESTED) {
+    return nullptr;
+  }
+  auto* nested = const_cast<NestedTabCollection*>(
+      static_cast<const NestedTabCollection*>(parent));
+  return nested->data()->owner_tab().Get() == tab ? nested : nullptr;
+}
+
+NestedTabCollection* TabStripCollection::GetNestedTabCollection(
+    const nested_tabs::NestedTabId& nested_id) {
+  auto it = nested_mapping_.find(nested_id);
+  if (it != nested_mapping_.end()) {
+    return it->second;
+  }
+  return nullptr;
+}
+
+TabStripCollection::NestedCreationResult
+TabStripCollection::FindOrCreateNestedFor(
+    TabInterface* parent_tab,
+    std::optional<nested_tabs::NestedTabVisualData> visual_data,
+    std::optional<nested_tabs::NestedTabId> id_override) {
+  CHECK(!parent_tab->IsSplit())
+      << "It is illegal to create a NESTED inside a SPLIT";
+  CHECK(!parent_tab->IsPinned()) << "Cannot create a NESTED on a pinned tab";
+
+  NestedTabCollection* existing = GetNestedTabCollection(parent_tab);
+  if (existing) {
+    return {existing, false};
+  }
+
+  // Insert an empty NESTED at parent_tab's slot, then move parent_tab into
+  // it as children[0]. The NESTED is transiently empty between these steps;
+  // ValidateData only runs at CompleteModelUpdateTransaction so invariants
+  // hold by then.
+  TabCollection* parent_coll = parent_tab->GetParentCollection(GetPassKey());
+  CHECK(parent_coll);
+  const size_t owner_idx = parent_coll->GetIndexOfTab(parent_tab).value();
+
+  auto coll = std::make_unique<tabs::NestedTabCollection>(
+      id_override.value_or(nested_tabs::NestedTabId::GenerateNew()),
+      visual_data.value_or(nested_tabs::NestedTabVisualData{}),
+      parent_tab->GetHandle());
+  auto ptr = coll.get();
+
+  AddTabCollectionImpl(std::move(coll), {parent_coll->GetHandle(), owner_idx});
+
+  TabCollection::Position owner_pos{ptr->GetHandle(), 0};
+  MoveTabImpl(parent_tab, owner_pos);
+
+  return {ptr, true};
+}
+
+std::optional<TabStripCollection::NestedDestruction>
+TabStripCollection::MoveTabToNested(TabInterface* tab,
+                                    const nested_tabs::NestedTabId& id,
+                                    std::optional<size_t> target_child_idx) {
+  NestedTabCollection* coll = GetNestedTabCollection(id);
+  CHECK(coll);
+  CHECK(coll->data()->owner_tab().Get() != tab);
+
+  return MoveTabToCollectionWithNestedCleanup(tab, coll, target_child_idx);
+}
+
+std::optional<TabStripCollection::NestedDestruction>
+TabStripCollection::MoveTabToTopLevel(TabInterface* tab,
+                                      std::optional<size_t> target_child_idx) {
+  TabCollection* coll = tab->GetParentCollection(GetPassKey());
+  while (coll->GetParentCollection() &&
+         coll->GetParentCollection()->type() != Type::TABSTRIP) {
+    coll = coll->GetParentCollection();
+  }
+  return MoveTabToCollectionWithNestedCleanup(tab, coll, target_child_idx);
+}
+
+std::optional<TabStripCollection::NestedDestruction>
+TabStripCollection::MoveTabToCollectionWithNestedCleanup(
+    TabInterface* tab,
+    TabCollection* coll,
+    std::optional<size_t> target_child_idx) {
+  TabCollection* old_tab_parent = tab->GetParentCollection(GetPassKey());
+  NestedTabCollection* old_tab_nested = GetNestedTabCollection(tab);
+
+  size_t tgt_idx =
+      target_child_idx
+          .transform([&](auto i) { return std::min(i, coll->ChildCount()); })
+          .value_or(coll->ChildCount());
+  TabCollection::Position tp = {coll->GetHandle(), tgt_idx};
+
+  if (old_tab_nested) {
+    // `tab` owns a NESTED; move the whole NESTED as a unit (owner rides
+    // along as children[0]).
+    TabCollection* grandparent = old_tab_nested->GetParentCollection();
+    MoveCollectionImpl(old_tab_nested, tp);
+    if (grandparent && grandparent->type() == Type::NESTED) {
+      return MaybeDestroyNestedWithOnlyOwner(
+          static_cast<NestedTabCollection*>(grandparent));
+    }
+    return std::nullopt;
+  }
+
+  if (tab->IsSplit()) {
+    TabCollection* split_grandparent = old_tab_parent->GetParentCollection();
+    MoveCollectionImpl(old_tab_parent, tp);
+    if (split_grandparent && split_grandparent->type() == Type::NESTED) {
+      return MaybeDestroyNestedWithOnlyOwner(
+          static_cast<NestedTabCollection*>(split_grandparent));
+    }
+    return std::nullopt;
+  }
+
+  MoveTabImpl(tab, tp);
+
+  if (old_tab_parent->type() == Type::NESTED) {
+    return MaybeDestroyNestedWithOnlyOwner(
+        static_cast<NestedTabCollection*>(old_tab_parent));
+  }
+  return std::nullopt;
+}
+
+void TabStripCollection::UnnestCollection(NestedTabCollection* nested) {
+  TabCollection* parent = nested->GetParentCollection();
+  CHECK(parent);
+  size_t insertion_idx = parent->GetIndexOfCollection(nested).value();
+
+  // Snapshot children before mutating. Iteration order is preserved (owner is
+  // first), so the resulting layout in `parent` is [owner, c1, c2, ...] at
+  // `insertion_idx`.
+  std::vector<std::variant<TabInterface*, TabCollection*>> snapshot;
+  for (const auto& child : nested->GetChildren()) {
+    if (auto* t = std::get_if<std::unique_ptr<TabInterface>>(&child)) {
+      snapshot.emplace_back(t->get());
+    } else {
+      snapshot.emplace_back(
+          std::get<std::unique_ptr<TabCollection>>(child).get());
+    }
+  }
+
+  for (auto& item : snapshot) {
+    TabCollection::Position pos{parent->GetHandle(), insertion_idx};
+    if (auto** t = std::get_if<TabInterface*>(&item)) {
+      // Low-level move (skips the "owner moves with NESTED" semantic; the
+      // NESTED is being torn down here).
+      MoveTabImpl(*t, pos);
+    } else {
+      MoveCollectionImpl(std::get<TabCollection*>(item), pos);
+    }
+    insertion_idx++;
+  }
+
+  RemoveTabCollectionImpl(nested);
+}
+
+std::optional<TabStripCollection::NestedDestruction>
+TabStripCollection::MaybeDestroyNestedWithOnlyOwner(
+    NestedTabCollection* nested) {
+  // Destroy when shrunk to only the owner (or fewer, defensively).
+  if (nested->ChildCount() > 1) {
+    return std::nullopt;
+  }
+
+  NestedDestruction destroyed = {nested->data()->id(),
+                                 nested->data()->owner_tab()};
+  TabCollection* nested_parent = nested->GetParentCollection();
+  CHECK(nested_parent);
+  const size_t nested_idx = nested_parent->GetIndexOfCollection(nested).value();
+
+  if (nested->ChildCount() == 1) {
+    // Promote owner to NESTED's slot in the parent.
+    TabInterface* owner = nested->data()->owner_tab().Get();
+    CHECK(owner);
+    TabCollection::Position owner_pos{nested_parent->GetHandle(), nested_idx};
+    MoveTabImpl(owner, owner_pos);
+  }
+
+  RemoveTabCollectionImpl(nested);
+  return destroyed;
+}
+
 void TabStripCollection::ValidateData() const {
   for (const auto& [_, group] : group_mapping_) {
     CHECK_GT(group->ChildCount(), 0UL);
@@ -498,6 +820,31 @@ void TabStripCollection::ValidateData()
       CHECK(split->GetSplitTabId() == child->GetSplit().value());
     }
   }
+  for (const auto& [_, nested] : nested_mapping_) {
+    auto* owner = nested->data()->owner_tab().Get();
+    CHECK(owner) << "NESTED " << nested->data()->id() << " has dangling owner";
+    CHECK(!owner->IsSplit()) << "NESTED owner may not be split!";
+    CHECK(!owner->IsPinned()) << "NESTED owner may not be pinned!";
+    // Owner lives INSIDE the NESTED as the first child.
+    CHECK_EQ(owner->GetParentCollection(), nested)
+        << "NESTED " << nested->data()->id()
+        << " owner is not parented to the NESTED. Owner's actual parent type: "
+        << owner->GetParentCollection()->type();
+    CHECK_GE(nested->ChildCount(), 2u)
+        << "NESTED " << nested->data()->id()
+        << " has fewer than 2 children (should have been destroyed at <=1).";
+    std::optional<size_t> owner_idx = nested->GetIndexOfTab(owner);
+    CHECK(owner_idx) << "NESTED owner not found among its children!";
+    CHECK_EQ(*owner_idx, 0u)
+        << "NESTED " << nested->data()->id() << " owner is at index "
+        << *owner_idx << ", expected 0 (first child).";
+    auto* parent = nested->GetParentCollection();
+    CHECK(parent);
+    CHECK_NE(parent->type(), TabCollection::Type::SPLIT)
+        << "NESTED may not be childed under SPLIT";
+    CHECK_NE(parent->type(), TabCollection::Type::PINNED)
+        << "NESTED may not be childed under PINNED";
+  }
 }
 
 std::optional<const tab_groups::TabGroupId> TabStripCollection::FindGroupIdFor(
@@ -552,6 +899,11 @@ void TabStripCollection::AddCollectionMa
         static_cast<SplitTabCollection*>(root_collection);
     split_mapping_.insert(
         {split_collection->GetSplitTabId(), split_collection});
+  } else if (root_collection->type() == TabCollection::Type::NESTED) {
+    NestedTabCollection* nested_collection =
+        static_cast<NestedTabCollection*>(root_collection);
+    nested_mapping_.insert(
+        {nested_collection->GetNestedTabId(), nested_collection});
   }
 }
 
@@ -572,6 +924,10 @@ void TabStripCollection::RemoveCollectio
     SplitTabCollection* split_collection =
         static_cast<SplitTabCollection*>(root_collection);
     CHECK(split_mapping_.erase(split_collection->GetSplitTabId()));
+  } else if (root_collection->type() == TabCollection::Type::NESTED) {
+    NestedTabCollection* nested_collection =
+        static_cast<NestedTabCollection*>(root_collection);
+    CHECK(nested_mapping_.erase(nested_collection->GetNestedTabId()));
   }
 }
 
@@ -741,36 +1097,62 @@ TabCollection::Position TabStripCollecti
     int index,
     bool pinned,
     std::optional<tab_groups::TabGroupId> group) {
-  size_t direct_dst_index;
   TabCollection* insert_collection = nullptr;
 
+  auto descend_to_insertion_point =
+      [](TabCollection* container,
+         int target) -> std::pair<TabCollection*, size_t> {
+    while (true) {
+      int cur = 0;
+      TabCollection* descend_into = nullptr;
+      size_t inner_target = 0;
+      for (const auto& child : container->GetChildren()) {
+        int child_count = 1;
+        if (std::holds_alternative<std::unique_ptr<TabCollection>>(child)) {
+          auto* coll = std::get<std::unique_ptr<TabCollection>>(child).get();
+          child_count = coll->TabCountRecursive();
+          if (coll->type() == TabCollection::Type::NESTED && target > cur &&
+              target < cur + child_count) {
+            descend_into = coll;
+            inner_target = target - cur;
+            break;
+          }
+        }
+        cur += child_count;
+      }
+      if (!descend_into) {
+        return {container, container->ToDirectIndex(target)};
+      }
+      container = descend_into;
+      target = inner_target;
+    }
+  };
+
+  size_t target = 0;
   if (pinned) {
-    direct_dst_index = pinned_collection_->ToDirectIndex(index);
+    target = index;  // pinned starts at global 0
     insert_collection = pinned_collection_;
   } else if (group.has_value()) {
     TabGroupTabCollection* group_collection =
         GetTabGroupCollection(group.value());
     CHECK(group_collection);
-
     if (group_collection->TabCountRecursive() == 0) {
-      // Group has been created but not yet populated.
-      direct_dst_index = 0;
-    } else {
-      size_t offset =
-          GetIndexOfTabRecursive(group_collection->GetTabAtIndexRecursive(0))
-              .value();
-
-      direct_dst_index = group_collection->ToDirectIndex(index - offset);
+      return TabCollection::Position(group_collection->GetHandle(), 0);
     }
+    size_t offset =
+        GetIndexOfTabRecursive(group_collection->GetTabAtIndexRecursive(0))
+            .value();
+    target = index - offset;
     insert_collection = group_collection;
   } else {
-    size_t offset = pinned_collection_->TabCountRecursive();
-    direct_dst_index = unpinned_collection_->ToDirectIndex(index - offset);
+    target = index - pinned_collection_->TabCountRecursive();
     insert_collection = unpinned_collection_;
   }
 
-  return TabCollection::Position(insert_collection->GetHandle(),
-                                 direct_dst_index);
+  auto [container, direct_idx] =
+      descend_to_insertion_point(insert_collection, target);
+
+  return TabCollection::Position(container->GetHandle(), direct_idx);
 }
 
 TabCollection::Position TabStripCollection::GetNodePosition(
--- a/components/tabs/impl/unpinned_tab_collection.cc
+++ b/components/tabs/impl/unpinned_tab_collection.cc
@@ -15,7 +15,7 @@ namespace tabs {
 
 UnpinnedTabCollection::UnpinnedTabCollection()
     : TabCollection(TabCollection::Type::UNPINNED,
-                    {TabCollection::Type::GROUP, TabCollection::Type::SPLIT},
+                    {TabCollection::Type::GROUP, TabCollection::Type::SPLIT, TabCollection::Type::NESTED},
                     /*supports_tabs=*/true) {}
 
 UnpinnedTabCollection::~UnpinnedTabCollection() = default;
--- /dev/null
+++ b/components/tabs/public/nested_tab_collection.h
@@ -0,0 +1,64 @@
+// Copyright 2026 The Chromium Authors
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+#ifndef COMPONENTS_TABS_PUBLIC_NESTED_TAB_COLLECTION_H_
+#define COMPONENTS_TABS_PUBLIC_NESTED_TAB_COLLECTION_H_
+
+#include <cstddef>
+
+#include "base/observer_list.h"
+#include "components/nested_tabs/nested_tab_id.h"
+#include "components/split_tabs/split_tab_id.h"
+#include "components/tabs/public/tab_collection.h"
+#include "components/tabs/public/tab_interface.h"
+
+namespace nested_tabs {
+class NestedTabData;
+class NestedTabVisualData;
+}  // namespace nested_tabs
+
+namespace tabs {
+
+// A collection for nested tabs. A nested collection always has an owner (depth
+// 0 nested collection is illegal). The owner tab lives INSIDE the NESTED
+// collection as its first child; subsequent children are the NESTED's members.
+//
+// Invariants:
+// - Owner tab is children()[0]. `data()->owner_tab()` returns its handle.
+// - NESTED has at least 2 children in steady state (owner + at least one
+//   member). A NESTED that shrinks to only the owner is auto-destroyed and the
+//   owner is promoted back to NESTED's parent collection at NESTED's former
+//   position.
+// - NESTED is never a direct child of Pinned.
+// - NESTED's owner is never pinned, never in a SPLIT.
+// - NESTED can contain SPLITs; SPLITs cannot contain NESTEDs.
+class NestedTabCollection : public TabCollection {
+ public:
+  explicit NestedTabCollection(nested_tabs::NestedTabId nested_id,
+                               nested_tabs::NestedTabVisualData visual_data,
+                               tabs::TabHandle owner_tab);
+  ~NestedTabCollection() override;
+  NestedTabCollection(const NestedTabCollection&) = delete;
+  NestedTabCollection& operator=(const NestedTabCollection&) = delete;
+
+  nested_tabs::NestedTabData* data() const { return nested_tab_data_.get(); }
+
+  // Returns the NestedTabId this collection is associated with.
+  nested_tabs::NestedTabId GetNestedTabId() const;
+
+  // Depth of this NESTED in the tree. A NESTED directly under UNPINNED/PINNED
+  // has depth 1. Each additional NESTED ancestor adds 1. Non-NESTED ancestors
+  // (TABSTRIP, PINNED, UNPINNED, GROUP) don't contribute.
+  size_t Depth() const;
+
+  bool IsCollapsed() const;
+  void SetIsCollapsed(bool is_collapsed);
+
+ private:
+  std::unique_ptr<nested_tabs::NestedTabData> nested_tab_data_;
+};
+
+}  // namespace tabs
+
+#endif  // COMPONENTS_TABS_PUBLIC_NESTED_TAB_COLLECTION_H_
--- /dev/null
+++ b/components/tabs/public/nested_tab_data.h
@@ -0,0 +1,54 @@
+// Copyright 2025 The Chromium Authors
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+#ifndef COMPONENTS_TABS_PUBLIC_NESTED_TAB_DATA_H_
+#define COMPONENTS_TABS_PUBLIC_NESTED_TAB_DATA_H_
+
+#include <vector>
+
+#include "components/nested_tabs/nested_tab_id.h"
+#include "components/nested_tabs/nested_tab_visual_data.h"
+#include "components/tabs/public/nested_tab_collection.h"
+#include "components/tabs/public/tab_interface.h"
+#include "ui/gfx/range/range.h"
+
+namespace nested_tabs {
+
+// Contains metadata for a nested tab collection such as the id and split layout
+// orientation. Also provides a way to access a the list of tabs in the split.
+class NestedTabData {
+ public:
+  NestedTabData(tabs::NestedTabCollection* collection,
+                const nested_tabs::NestedTabId& id,
+                const NestedTabVisualData& visual_data,
+                tabs::TabHandle owner_tab);
+  ~NestedTabData() = default;
+
+  const nested_tabs::NestedTabId& id() const { return id_; }
+
+  NestedTabVisualData* visual_data() { return &visual_data_; }
+
+  const NestedTabVisualData* visual_data() const { return &visual_data_; }
+
+  const tabs::TabHandle& owner_tab() const { return owner_tab_; }
+
+  std::vector<tabs::TabInterface*> ListTabs() const;
+
+  // Returns the TabCollection handle associated with this nested collection.
+  tabs::TabCollection::Handle GetCollectionHandle() const;
+
+  // Returns [start, end) where the leftmost tab in the split has index start
+  // and the rightmost tab in the split has index end - 1.
+  gfx::Range GetIndexRange() const;
+
+ private:
+  raw_ptr<tabs::NestedTabCollection> collection_;
+  tabs::TabHandle owner_tab_;
+  NestedTabVisualData visual_data_;
+  nested_tabs::NestedTabId id_;
+};
+
+}  // namespace nested_tabs
+
+#endif  // COMPONENTS_TABS_PUBLIC_NESTED_TAB_DATA_H_
--- a/components/tabs/public/tab_collection.h
+++ b/components/tabs/public/tab_collection.h
@@ -123,6 +123,7 @@ class TabCollection : public SupportsHan
   // - UNPINNED:  A container for unpinned tabs.
   // - GROUP:     A container to grouped tabs.
   // - SPLIT:     A container for split tabs.
+  // - NESTED:    A container for nested tabs.
   // LINT.IfChange(TYPE)
   enum class Type {
     MIN,
@@ -131,9 +132,22 @@ class TabCollection : public SupportsHan
     UNPINNED,
     GROUP,
     SPLIT,
-    MAX = SPLIT  // Update value of MAX if adding a new type.
+    NESTED,
+    MAX = NESTED,  // Update value of MAX if adding a new type.
   };
 
+  friend std::ostream& operator<<(std::ostream& os, Type& ty) {
+      switch (ty) {
+          case Type::TABSTRIP: os << "Tabstrip"; break;
+          case Type::PINNED: os << "Pinned"; break;
+          case Type::UNPINNED: os << "Unpinned"; break;
+          case Type::GROUP: os << "Group"; break;
+          case Type::SPLIT: os << "Split"; break;
+          case Type::NESTED: os << "Nested"; break;
+      }
+      return os;
+  }
+
   using TypeEnumSet = base::EnumSet<Type, Type::MIN, Type::MAX>;
   // LINT.ThenChange(chrome/browser/ui/views/tabs/vertical/tab_collection_node.h:TYPE)
 
--- a/components/tabs/public/tab_collection_observer_unittest.cc
+++ b/components/tabs/public/tab_collection_observer_unittest.cc
@@ -970,7 +970,7 @@ TEST_F(TabCollectionObserverTest, MoveTa
 
     const tabs::TabCollection::TypeEnumSet retain_collection_types = {
         tabs::TabCollection::Type::GROUP};
-    collection->MoveTabsRecursive({0, 1}, 1, std::nullopt, false,
+    collection->MoveTabsRecursive({0, 1}, 1, std::nullopt, std::nullopt, false,
                                   retain_collection_types);
   }
 }
--- a/components/tabs/public/tab_strip_collection.h
+++ b/components/tabs/public/tab_strip_collection.h
@@ -10,11 +10,15 @@
 #include <set>
 #include <unordered_map>
 
+#include "base/memory/raw_ptr.h"
 #include "base/types/pass_key.h"
+#include "components/nested_tabs/nested_tab_id.h"
+#include "components/nested_tabs/nested_tab_visual_data.h"
 #include "components/split_tabs/split_tab_id.h"
 #include "components/tab_groups/tab_group_id.h"
 #include "components/tabs/public/split_tab_data.h"
 #include "components/tabs/public/tab_collection.h"
+#include "components/tabs/public/tab_interface.h"
 
 class TabStripModel;
 
@@ -31,6 +35,8 @@ class SplitTabCollection;
 // collection which then contain different tabs and group.
 class TabStripCollection : public TabCollection {
  public:
+  std::string DebugDump() const;
+
   explicit TabStripCollection(bool send_notifications_immediately = true);
   ~TabStripCollection() override;
   TabStripCollection(const TabStripCollection&) = delete;
@@ -50,10 +56,12 @@ class TabStripCollection : public TabCol
                         size_t final_index,
                         std::optional<tab_groups::TabGroupId> new_group_id,
                         bool new_pinned_state);
+  // Callers must pass post-removal destination semantics
   void MoveTabsRecursive(
       const std::vector<int>& tab_indices,
       size_t destination_index,
       std::optional<tab_groups::TabGroupId> new_group_id,
+      std::optional<nested_tabs::NestedTabId> enclosing_nested,
       bool new_pinned_state,
       const TabCollection::TypeEnumSet retain_collection_types);
 
@@ -110,6 +118,48 @@ class TabStripCollection : public TabCol
   void Unsplit(split_tabs::SplitTabId split_id);
   std::set<split_tabs::SplitTabId> ListSplits() const;
 
+  std::set<nested_tabs::NestedTabId> ListNesteds() const;
+  // Gets a NestedTabCollection if |tab| is a parent
+  NestedTabCollection* GetNestedTabCollection(const TabInterface* tab);
+  NestedTabCollection* GetNestedTabCollection(
+      const nested_tabs::NestedTabId& nested_id);
+  struct NestedCreationResult {
+    NestedTabCollection* coll;
+    bool did_create;
+  };
+  NestedCreationResult FindOrCreateNestedFor(
+      TabInterface* owner_tab,
+      std::optional<nested_tabs::NestedTabVisualData> visual_data,
+      std::optional<nested_tabs::NestedTabId> id_override);
+  struct NestedDestruction {
+    nested_tabs::NestedTabId old_id;
+    TabHandle old_owner;
+  };
+  // Returns any destroyed nested
+  std::optional<NestedDestruction> MoveTabToNested(
+      TabInterface* tab,
+      const nested_tabs::NestedTabId& id,
+      std::optional<size_t> target_child_idx = {});
+  // Returns any destroyed nested
+  std::optional<NestedDestruction> MoveTabToTopLevel(
+      TabInterface* tab,
+      std::optional<size_t> target_child_idx = {});
+  std::optional<NestedDestruction> MoveTabToCollectionWithNestedCleanup(
+      TabInterface* tab,
+      TabCollection* coll,
+      std::optional<size_t> target_child_idx = {});
+
+  // If `nested` has shrunk to only its owner, promote the owner back to
+  // `nested`'s parent at `nested`'s current slot and destroy the NESTED.
+  // Returns destruction info iff the NESTED was destroyed.
+  std::optional<NestedDestruction> MaybeDestroyNestedWithOnlyOwner(
+      NestedTabCollection* nested);
+
+  // Destroys `nested` and promotes its children to `nested`'s parent at
+  // `nested`'s former slot, preserving order. Bypasses the
+  // "owner-can't-move-alone" guard since the NESTED is being torn down.
+  void UnnestCollection(NestedTabCollection* nested);
+
   void ValidateData() const;
 
   std::optional<const tab_groups::TabGroupId> FindGroupIdFor(
@@ -130,6 +180,13 @@ class TabStripCollection : public TabCol
       ChildrenPtrs tab_or_collections,
       size_t destination_index,
       std::optional<tab_groups::TabGroupId> new_group_id,
+      // Optional destination NESTED. When set, the move targets a position
+      // inside the named NESTED collection. When unset (the common case), moves
+      // treat NESTEDs as opaque single-position units: destination indices that
+      // would fall inside a NESTED resolve to a position adjacent to it
+      // instead. Callers must explicitly opt in via this hint to land tabs
+      // inside a NESTED.
+      std::optional<nested_tabs::NestedTabId> enclosing_nested,
       bool new_pinned_state);
 
   // Returns the list of tabs and collection to remove for `MoveTabsRecursive`.
@@ -140,9 +197,9 @@ class TabStripCollection : public TabCol
       const std::vector<int>& tab_indices,
       const TabCollection::TypeEnumSet retain_collection_types);
 
-  // Helper to centralize updates to `group_mapping_` and `split_mapping_`. If
-  // `root_collection` is a group, the appropriate splits need to group need to
-  // be updated in the `split_mapping_`.
+  // Helper to centralize updates to `group_mapping_`, `split_mapping_`, and
+  // `nested_mapping_`. If `root_collection` is a group, the appropriate splits
+  // need to group need to be updated in the `split_mapping_`.
   void AddCollectionMapping(TabCollection* root_collection);
   void RemoveCollectionMapping(TabCollection* root_collection);
 
@@ -217,6 +274,13 @@ class TabStripCollection : public TabCol
                      split_tabs::SplitTabIdHash>
       split_mapping_;
 
+  // Lookup table to find nested collections by their nested ID.
+  // TODO: handle reattach recursion when DetachNestedCollection exists
+  std::unordered_map<nested_tabs::NestedTabId,
+                     raw_ptr<NestedTabCollection>,
+                     nested_tabs::NestedTabIdHash>
+      nested_mapping_;
+
   // `tab_strip_model` creates this to allow extension of lifetime for groups to
   // allow for group_model_ updates and observation methods.
   std::vector<std::unique_ptr<tabs::TabGroupTabCollection>>
