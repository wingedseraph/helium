--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -315,6 +315,10 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kTabCyclingMRU] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kVisualTabSwitcher] =
+      settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kVisualTabSwitcherList] =
+      settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kShiftRightClickContextMenu] =
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kShowZoomIndicator] =
--- a/chrome/browser/ui/browser_ui_prefs.cc
+++ b/chrome/browser/ui/browser_ui_prefs.cc
@@ -128,6 +128,8 @@ void RegisterBrowserUserPrefs(user_prefs
   registry->RegisterBooleanPref(prefs::kNewTabNextToActive, false);

   registry->RegisterBooleanPref(prefs::kTabCyclingMRU, false);
+  registry->RegisterBooleanPref(prefs::kVisualTabSwitcher, false);
+  registry->RegisterBooleanPref(prefs::kVisualTabSwitcherList, false);

   registry->RegisterBooleanPref(prefs::kShiftRightClickContextMenu, true);

--- a/chrome/common/pref_names.h
+++ b/chrome/common/pref_names.h
@@ -607,6 +607,10 @@ inline constexpr char kNewTabNextToActiv
 // A boolean pref set to true if MRU (Most Recently Used) order should be used
 // when cycling through tabs with Ctrl+Tab.
 inline constexpr char kTabCyclingMRU[] = "helium.browser.mru_tab_cycling";
+inline constexpr char kVisualTabSwitcher[] =
+    "helium.browser.visual_tab_switcher";
+inline constexpr char kVisualTabSwitcherList[] =
+    "helium.browser.visual_tab_switcher_list";

 // A boolean pref set to true if Shift+right-click should bypass page context
 // menu handlers and show the browser context menu.
--- a/chrome/browser/ui/tabs/tab_strip_model.cc
+++ b/chrome/browser/ui/tabs/tab_strip_model.cc
@@ -4332,6 +4332,14 @@ void TabStripModel::StopMRUCycling() {
   mru_cycle_list_.clear();
 }

+bool TabStripModel::IsTabEligibleForSequentialCycling(int index) const {
+  const auto focused_group = GetFocusedGroup();
+  return !IsTabCollapsed(index) &&
+         (!focused_group ||
+          tabs::TabStripModelSelectionState::IsTabValidInFocusedGroup(
+              GetTabAtIndex(index), focused_group));
+}
+
 void TabStripModel::SelectRelativeTab(TabRelativeDirection direction,
                                       TabStripUserGestureDetails detail) {
   // This may happen during automated testing or if a user somehow buffers
@@ -4348,27 +4356,14 @@ void TabStripModel::SelectRelativeTab(Ta
   // fallback on activating the active tab.
   DCHECK(!start_group.has_value() || !IsGroupCollapsed(start_group.value()));

-  std::optional<tab_groups::TabGroupId> focused_group = GetFocusedGroup();
   const int delta = direction == TabRelativeDirection::kNext ? 1 : -1;
   int index = (start_index + count() + delta) % count();

-  auto is_tab_invalid = [this, &focused_group](int i) {
-    // Do not select the tab if it is in a collapsed group.
-    if (IsTabCollapsed(i)) {
-      return true;
-    }
-
-    // Do not select the tab if it is not part of the focused state.
-    return focused_group.has_value() &&
-           !tabs::TabStripModelSelectionState::IsTabValidInFocusedGroup(
-               GetTabAtIndex(i), focused_group);
-  };
-
-  while (index != start_index && is_tab_invalid(index)) {
+  while (index != start_index && !IsTabEligibleForSequentialCycling(index)) {
     index = (index + count() + delta) % count();
   }

-  if (!is_tab_invalid(index)) {
+  if (IsTabEligibleForSequentialCycling(index)) {
     ActivateTabAt(index, detail);
   }
 }
--- a/chrome/browser/ui/tabs/tab_strip_model.h
+++ b/chrome/browser/ui/tabs/tab_strip_model.h
@@ -509,6 +509,7 @@ class TabStripModel {
   bool IsTabPinned(int index) const;

   bool IsTabCollapsed(int index) const;
+  bool IsTabEligibleForSequentialCycling(int index) const;

   bool IsGroupCollapsed(const tab_groups::TabGroupId& group) const;

--- a/chrome/browser/helium_flag_choices.h
+++ b/chrome/browser/helium_flag_choices.h
@@ -47,6 +47,9 @@ constexpr const char kHeliumCompactLocat

 constexpr const char kHeliumNativeFrameMaterialsCommandLine[] =
     "helium-native-frame-materials";
+
+constexpr const char kVisualTabSwitcherMaxTabsCommandLine[] =
+    "visual-tab-switcher-max-tabs";
 }  // namespace helium

 #endif /* CHROME_BROWSER_HELIUM_FLAG_CHOICES_H_ */
--- a/chrome/browser/helium_flag_entries.h
+++ b/chrome/browser/helium_flag_entries.h
@@ -75,6 +75,13 @@
      "prevent access to networks that require a proxy. Helium flag.",
      kOsAll, SINGLE_VALUE_TYPE(switches::kNoProxyServer)},
     // ui
+    {helium::kVisualTabSwitcherMaxTabsCommandLine,
+     "Visual tab switcher maximum tabs",
+     "Limits the thumbnail switcher to the most recent tabs. Enter a positive "
+     "number, or -1 for all tabs. Defaults to 5. Does not affect list mode. "
+     "Helium flag.",
+     kOsDesktop,
+     STRING_VALUE_TYPE(helium::kVisualTabSwitcherMaxTabsCommandLine, "5")},
     {helium::kHeliumCompactLocationWidthCommandLine,
      "Automatic address bar width in compact layout",
      "Allows the location bar to automatically reduce its width in the compact "
